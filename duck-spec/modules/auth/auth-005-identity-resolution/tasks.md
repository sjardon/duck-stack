# AUTH-005 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: ServiceUnavailableError shape | test | R007 | In `apps/services/tests/unit/shared/errors.test.ts` add a case asserting `new ServiceUnavailableError()` has `statusCode 503`, `code 'SERVICE_UNAVAILABLE'`, and `retryAfterSeconds` defaulting to `2` (and accepts a custom value). |
| T002 | Implement `ServiceUnavailableError` | implement | R007 | In `apps/services/src/shared/errors.ts` add `export class ServiceUnavailableError extends DomainError` with constructor `(retryAfterSeconds: number = 2)` calling `super('SERVICE_UNAVAILABLE', 'Identity resolution is still in progress. Please retry shortly.', 503)` and storing `retryAfterSeconds` as a public readonly field. |
| T003 | Test: errorHandler serializes ServiceUnavailableError with Retry-After | test | R007 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts` add a case asserting that when the handler throws `ServiceUnavailableError`, the Fastify error handler replies `503`, sets the `Retry-After` response header to `retryAfterSeconds`, and serializes `{ code, message }` in the body. |
| T004 | Implement errorHandler `ServiceUnavailableError` branch | implement | R007 | In `apps/services/src/shared/plugins/errorHandler.ts` `errorHandlerPlugin`'s `setErrorHandler` callback, add a branch (before the generic `DomainError` branch) that detects `ServiceUnavailableError` and calls `reply.status(503).header('Retry-After', String(error.retryAfterSeconds)).send({ code: error.code, message: error.message })`. |
| T005 | Test: `clerkClient` singleton fail-fast and construction | test | R008, R009 | In `apps/services/tests/unit/shared/infrastructure/clerkClient.test.ts` assert the module throws synchronously when `CLERK_SECRET_KEY` is absent at import time, and that it calls `createClerkClient({ secretKey })` and exports the resulting client when the variable is present. |
| T006 | Implement `clerkClient` singleton | implement | R008, R009 | Create `apps/services/src/shared/infrastructure/clerkClient.ts`: read `CLERK_SECRET_KEY` from `process.env`, throw a descriptive `Error` synchronously if absent (mirroring `db.ts`'s fail-fast pattern), and export `clerkClient: ClerkClient` built via `createClerkClient({ secretKey })` from `@clerk/backend`. |
| T007 | Test: `IdentityDBRepository` lookups | test | R001, R002, R006 | In `apps/services/tests/unit/shared/repositories/identityDBRepository.test.ts` assert `findUserIdByClerkUserId` and `findOrgIdByClerkOrgId` return the row's `id` when found, `null` when no row matches, and that a thrown SQL error is logged and re-thrown as `ProviderError`. |
| T008 | Implement `IIdentityRepository` + `IdentityDBRepository` | implement | R001, R002, R006 | Create `apps/services/src/shared/repositories/interfaces/iIdentityRepository.ts` declaring `IIdentityRepository { findUserIdByClerkUserId(clerkUserId: string): Promise<string \| null>; findOrgIdByClerkOrgId(clerkOrgId: string): Promise<string \| null>; }`. Create `apps/services/src/shared/repositories/identityDBRepository.ts` with `IdentityDBRepository implements IIdentityRepository`, constructor-injected `Sql`, `SELECT id FROM users WHERE clerk_user_id = ${clerkUserId} LIMIT 1` and `SELECT id FROM organizations WHERE clerk_org_id = ${clerkOrgId} LIMIT 1`, following the try/catch + `ProviderError` convention used by `UserDBRepository`. |
| T009 | Test: `ClerkMetadataProvider` writes | test | R008, R009 | In `apps/services/tests/unit/shared/providers/clerkMetadataProvider.test.ts` assert `setUserAppId` calls `clerkClient.users.updateUserMetadata(clerkUserId, { privateMetadata: { appUserId } })`, `setOrgAppId` calls `clerkClient.organizations.updateOrganizationMetadata(clerkOrgId, { privateMetadata: { appOrgId } })`, and that a rejected SDK call is logged at `error` and re-thrown as `ProviderError(502)`. |
| T010 | Implement `IClerkMetadataProvider` + `ClerkMetadataProvider` | implement | R008, R009 | Create `apps/services/src/shared/providers/interfaces/iClerkMetadataProvider.ts` declaring `IClerkMetadataProvider { setUserAppId(clerkUserId: string, appUserId: string): Promise<void>; setOrgAppId(clerkOrgId: string, appOrgId: string): Promise<void>; }`. Create `apps/services/src/shared/providers/clerkMetadataProvider.ts` with `ClerkMetadataProvider implements IClerkMetadataProvider`, constructor-injected `ClerkClient`, implementing both methods with the required external-call try/catch and `ProviderError` re-throw. |
| T011 | Test: `resolveIdentityClaim` fast path, retry/backoff, backfill, and statelessness | test | R001, R006, R007, R008, NF001, NF002, NF003, NF004, EC002, EC003, EC006 | In `apps/services/tests/unit/shared/plugins/resolveIdentityClaim.test.ts` assert: (1) WHEN `claimValue` is provided THEN the function returns it immediately and `lookupById`/`backfill` are never called (R001, NF001, EC003); (2) WHEN `claimValue` is absent and `lookupById` resolves after N failed attempts within the budget THEN it returns the resolved id and the delay between attempts grows (R006, NF002, NF003); (3) WHEN `lookupById` never resolves before the budget elapses THEN it returns `null` (R007); (4) WHEN a degraded-path lookup succeeds THEN `backfill` is invoked without being awaited by the caller, and a rejected `backfill` promise is caught and logged via `logger.warn` without throwing (R008, EC002, NF004); (5) two concurrent invocations with the same `clerkId` run independent retry loops with no shared state (EC006). |
| T012 | Implement `resolveIdentityClaim` + `withRetryBackoff` | implement | R001, R006, R007, R008, NF001, NF002, NF003, NF004, EC002, EC003, EC006 | Create `apps/services/src/shared/plugins/resolveIdentityClaim.ts` exporting `resolveIdentityClaim({ claimValue, clerkId, lookupById, backfill, budgetMs = 2000 })`: returns `claimValue` immediately when present; otherwise calls an internal `withRetryBackoff(lookup, budgetMs)` that retries `lookupById(clerkId)` with an exponential backoff starting at 100ms (capped to the remaining budget) until it resolves or the budget is exhausted; on success, fire-and-forgets `backfill(clerkId, internalId)` with a `.catch` that calls `logger.warn` (comment justifying the silent fail per NF004); returns the resolved id or `null`. |
| T013 | Test: clerkAuthPlugin decorates raw Clerk IDs | test | R004 | In `apps/services/tests/unit/shared/plugins/clerkAuthPlugin.test.ts` add a case asserting that on a valid JWT with `sub` and `org_id` claims, the request handler observes `request.clerkUserId === payload.sub` and `request.clerkOrgId === payload.org_id`. |
| T014 | Implement clerkAuthPlugin raw-ID decoration + type augmentation | implement | R004 | In `apps/services/src/shared/plugins/clerkAuthPlugin.ts`'s `onRequest` hook, after a successful `verifyToken`, set `request.clerkUserId = payload.sub` and `request.clerkOrgId = (payload as Record<string, unknown>)['org_id'] as string \| null ?? null`. In `apps/services/src/types/fastify.d.ts` add `clerkUserId: string \| undefined` and `clerkOrgId: string \| null \| undefined` to the `FastifyRequest` augmentation. |
| T015 | Test: clerkAuthPlugin resolves `request.userId` via claim fast path | test | R001, NF001 | In `apps/services/tests/unit/shared/plugins/clerkAuthPlugin.test.ts` add a case asserting that WHEN the JWT payload includes `app_user_id` THEN `request.userId` equals that claim value and neither the identity repository nor the metadata provider is invoked. |
| T016 | Implement clerkAuthPlugin `userId` fast-path wiring | implement | R001, NF001 | In `apps/services/src/shared/plugins/clerkAuthPlugin.ts` instantiate module-scope singletons `const identityRepo = new IdentityDBRepository(db)` and `const metadataProvider = new ClerkMetadataProvider(clerkClient)` at plugin registration, then call `resolveIdentityClaim({ claimValue: payload['app_user_id'], clerkId: request.clerkUserId, lookupById: (id) => identityRepo.findUserIdByClerkUserId(id), backfill: (id, internalId) => metadataProvider.setUserAppId(id, internalId) })` and assign the result to `request.userId` when non-null. |
| T017 | Test: clerkAuthPlugin degraded-path resolution and 503 fallback for `userId` | test | R006, R007, R008, NF002 | In `apps/services/tests/unit/shared/plugins/clerkAuthPlugin.test.ts` add cases asserting: (1) WHEN `app_user_id` is absent and the identity repository resolves the user within budget THEN `request.userId` is set to the resolved id and the response is not 503; (2) WHEN the identity repository never resolves within budget THEN the response is `503` with a `Retry-After` header. |
| T018 | Implement clerkAuthPlugin `userId` 503 fallback | implement | R006, R007, R008, NF002 | In `apps/services/src/shared/plugins/clerkAuthPlugin.ts`'s `onRequest` hook, WHEN `resolveIdentityClaim` for the user returns `null` THEN `throw new ServiceUnavailableError()` (thrown outside the `verifyToken` `try/catch` so it propagates to `errorHandler`); otherwise assign `request.userId` and continue to organization resolution. |
| T019 | Test: clerkAuthPlugin `orgId` resolution and no-org case | test | R002, R003, EC005 | In `apps/services/tests/unit/shared/plugins/clerkAuthPlugin.test.ts` add cases asserting: (1) WHEN the JWT has no `org_id` claim THEN `request.orgId` is `null` and the identity repository's org lookup is never called (R003); (2) WHEN `org_id` and `app_org_id` are both present THEN `request.orgId` equals `app_org_id` with no DB call (R002, NF001); (3) WHEN `org_id` is present but `app_org_id` is absent THEN `request.orgId` resolves via the identity repository, or the response is `503` after the budget elapses (R002, R006, R007); (4) two sequential requests carrying different `org_id` claims for the same user each produce the matching `request.orgId` with no stale value from the first request (EC005). |
| T020 | Implement clerkAuthPlugin `orgId` resolution branch | implement | R002, R003, EC005 | In `apps/services/src/shared/plugins/clerkAuthPlugin.ts`'s `onRequest` hook, WHEN `request.clerkOrgId === null` THEN set `request.orgId = null` and return; otherwise call `resolveIdentityClaim` with `clerkId: request.clerkOrgId`, `lookupById: identityRepo.findOrgIdByClerkOrgId`, `backfill: metadataProvider.setOrgAppId`, throw `ServiceUnavailableError` when unresolved, otherwise assign `request.orgId`. |
| T021 | Test: getUserProfileHandler resolves by Clerk ID | test | R005 | Create `apps/services/tests/unit/modules/users/handlers/getUserProfileHandler.test.ts` asserting the handler calls `GetUserProfileUseCase.execute` with `request.clerkUserId`, not `request.userId`. |
| T022 | Implement getUserProfileHandler Clerk-ID resolution | implement | R005 | In `apps/services/src/modules/users/handlers/getUserProfileHandler.ts`, change `useCase.execute(request.userId!)` to `useCase.execute(request.clerkUserId!)`. |
| T023 | Test: updateUserProfileHandler resolves by Clerk ID | test | R005 | In `apps/services/tests/unit/modules/users/handlers/updateUserProfileHandler.test.ts`, update `makeRequest` to set `clerkUserId` (in addition to `userId`) and change the assertion on the successful-update case to `expect(mockExecute).toHaveBeenCalledWith('clerk_abc', { locale: 'en-US', timezone: 'America/New_York' })` sourced from `request.clerkUserId`. |
| T024 | Implement updateUserProfileHandler Clerk-ID resolution | implement | R005 | In `apps/services/src/modules/users/handlers/updateUserProfileHandler.ts`, change `useCase.execute(request.userId!, body)` to `useCase.execute(request.clerkUserId!, body)`. |
| T025 | Test: completeOnboardingHandler resolves by Clerk ID | test | R005 | In `apps/services/tests/unit/users/completeOnboardingHandler.test.ts`, update `makeRequest` to set `clerkUserId` (in addition to `userId`) and change the successful-submission assertion to expect `mockExecute` called with `request.clerkUserId`. |
| T026 | Implement completeOnboardingHandler Clerk-ID resolution | implement | R005 | In `apps/services/src/modules/users/handlers/completeOnboardingHandler.ts`, change `useCase.execute(request.userId!, body)` to `useCase.execute(request.clerkUserId!, body)`. |
| T027 | Test: `ClerkSyncRepository.upsertOrganization` returns internal UUID | test | R009 | In `apps/services/tests/unit/modules/webhooks/repositories/clerkSyncRepository.test.ts` add a case asserting `upsertOrganization` returns `{ id: string }` containing the internal UUID from the `RETURNING id` clause. |
| T028 | Implement `upsertOrganization` `RETURNING id` | implement | R009 | In `apps/services/src/modules/webhooks/repositories/clerkSyncRepository.ts`, change `upsertOrganization` to add `RETURNING id` to its `INSERT ... ON CONFLICT` statement and return `rows[0]` (`{ id: string }`) instead of `void`. |
| T029 | Test: dispatchClerkEvent blocking metadata write on created events | test | R009, NF005, EC004 | In `apps/services/tests/unit/modules/webhooks/clerk/clerkEventHandlers.test.ts` add cases asserting: (1) WHEN `event.type` is `user.created` THEN `metadataProvider.setUserAppId` is awaited with the Clerk user ID and the internal id returned by `upsertUser`, called after the repository upsert; (2) WHEN `event.type` is `organization.created` THEN `metadataProvider.setOrgAppId` is awaited with the Clerk org ID and the internal id returned by `upsertOrganization`; (3) WHEN `metadataProvider.setUserAppId` rejects THEN `dispatchClerkEvent` rejects with the same error (not swallowed) (R009, NF005); (4) WHEN `event.type` is `user.updated` THEN `metadataProvider.setUserAppId` is NOT called (scope limited to `*.created`, consistent with R009's wording). |
| T030 | Implement `dispatchClerkEvent` blocking metadata write | implement | R009, NF005, EC004 | In `apps/services/src/modules/webhooks/clerk/clerkEventHandlers.ts`, add a `metadataProvider: IClerkMetadataProvider` parameter to `dispatchClerkEvent`. In the `user.created` branch, after `const { id } = await handleUserUpsert(event, repo)`, add `await metadataProvider.setUserAppId(event.data.id, id)` before the trial-subscription side effect. In the `organization.created` branch, change `handleOrganizationUpsert` to return `{ id }` (propagating `upsertOrganization`'s new return value) and add `await metadataProvider.setOrgAppId(event.data.id, id)` after the upsert. Leave `user.updated` and `organizationMembership.created` branches unchanged. |
| T031 | Test: clerkWebhookRoutes wires ClerkMetadataProvider into dispatchClerkEvent | test | R009 | In `apps/services/tests/unit/modules/webhooks/clerk/routes.test.ts` add a case (mocking `clerkClient.js` and `clerkMetadataProvider.js`) asserting `dispatchClerkEvent` is called with a `ClerkMetadataProvider` instance as its fourth argument, and that a rejection from `dispatchClerkEvent` (simulating a metadata-write failure) results in a non-2xx response. |
| T032 | Implement clerkWebhookRoutes metadataProvider wiring | implement | R009 | In `apps/services/src/modules/webhooks/clerk/routes.ts`, instantiate `const metadataProvider = new ClerkMetadataProvider(clerkClient)` alongside the existing `repository`/`subscriptionRepository` singletons, and pass it as the fourth argument to `dispatchClerkEvent(event, repository, subscriptionRepository, metadataProvider)`, matching the updated `dispatchClerkEvent` signature from T030. |
