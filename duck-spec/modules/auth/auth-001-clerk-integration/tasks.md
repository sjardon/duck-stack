# AUTH-001 — Tasks

| ID | Title | Covers | Description |
|---|---|---|---|
| T001 | Add `@clerk/backend` to services dependencies | R011, NF001 | In `apps/services/package.json` add `@clerk/backend` to the `dependencies` field so the package is available for import in the Fastify plugin. |
| T002 | Add `@clerk/clerk-react` and `react-router-dom` to web dependencies | R001, R002, R003, R004, R005, R006, R007, R008, R009, R010 | In `apps/web/package.json` add `@clerk/clerk-react` and `react-router-dom` to the `dependencies` field. |
| T003 | Add `ForbiddenError` to shared errors | R014 | In `apps/services/src/shared/errors.ts` add and export `ForbiddenError extends DomainError` with `code = 'FORBIDDEN'` and `statusCode = 403`. |
| T004 | Augment `FastifyRequest` with `userId` and `orgId` | R011, R012, EC008 | In `apps/services/src/types/fastify.d.ts` create a module augmentation for `fastify` that adds `userId: string \| undefined` and `orgId: string \| null \| undefined` to the `FastifyRequest` interface. |
| T005 | Create `clerk-auth.plugin.ts` — startup guard and Clerk client init | R011, NF001, EC007 | In the default-exported Fastify plugin function in `apps/services/src/shared/plugins/clerk-auth.plugin.ts`, read `process.env.CLERK_SECRET_KEY` and throw an `Error` with a descriptive message if it is absent; then call `createClerkClient({ secretKey })` from `@clerk/backend` to obtain a client that caches JWKS internally. |
| T006 | Create `clerk-auth.plugin.ts` — `onRequest` hook for JWT verification | R011, R012, NF001, EC002, EC003, EC007, EC008 | In the `onRequest` hook registered inside `clerk-auth.plugin.ts`, extract the `Authorization` header; if absent, return without setting decorations (EC008); if present but lacking the `Bearer` prefix, return without setting decorations (EC002); otherwise call `verifyToken(token, { secretKey })` from `@clerk/backend`, set `request.userId = payload.sub` and `request.orgId = payload.org_id ?? null` on success (R012), or return without setting decorations on any thrown error (EC003). |
| T007 | Create `require-auth.ts` preHandler | R013, EC002, EC003 | In `apps/services/src/shared/plugins/require-auth.ts` export `requireAuth(request, reply)` that checks `request.userId`; if it is `undefined`, throws `UnauthorizedError` so the error-handler plugin converts it to HTTP 401. |
| T008 | Create `require-org.ts` preHandler | R014, R015, NF002 | In `apps/services/src/shared/plugins/require-org.ts` export `requireOrg(request, reply)` that first calls `requireAuth(request, reply)` and then checks `request.orgId`; if it is `null`, throws `ForbiddenError` (HTTP 403); otherwise returns to allow the request through (R015). |
| T009 | Register `clerkAuthPlugin` in `app.ts` | R011, R012, EC007, EC008 | In `apps/services/src/app.ts` import `clerkAuthPlugin` from `./shared/plugins/clerk-auth.plugin.js` and call `await fastify.register(clerkAuthPlugin)` after the existing `helmetPlugin` registration so the `onRequest` hook is applied to all routes. |
| T010 | Create `apps/services/.env.example` | EC007 | In `apps/services/.env.example` document all required and optional environment variables: `CLERK_SECRET_KEY`, `PORT`, `HOST`, `LOG_LEVEL`, `CORS_ORIGIN`, `SUPABASE_URL`, and `SUPABASE_ANON_KEY`. |
| T011 | Fail-fast guard and `ClerkProvider` in `main.tsx` | R001, EC006 | In `apps/web/src/main.tsx` read `import.meta.env.VITE_CLERK_PUBLISHABLE_KEY`; throw an `Error` with a descriptive message if it is absent or empty; then wrap the existing `QueryClientProvider` tree in `<ClerkProvider publishableKey={...}>` from `@clerk/clerk-react`. |
| T012 | Create application router in `router.tsx` | R002, R003, R004, R005, R008, R009 | In `apps/web/src/router.tsx` create and export `router` using `createBrowserRouter` from `react-router-dom`, defining routes: `/sign-in` → `SignInPage`, `/sign-up` → `SignUpPage`, and an `AuthGuard`-wrapped layout route containing `/org/create` → `CreateOrgPage`, `/org/profile` → `OrgProfilePage`, and `/*` → `AppLayout` with nested content routes. |
| T013 | Replace `App.tsx` with `RouterProvider` | R002, R003, R004, R005, R008, R009 | In `apps/web/src/App.tsx` replace the current `HealthPage` render with `<RouterProvider router={router} />` imported from `react-router-dom`, using the `router` exported from `router.tsx`. |
| T014 | Create `AuthGuard` component | R004, R005, EC004 | In `apps/web/src/components/auth/AuthGuard.tsx` create and export `AuthGuard` as a React component that calls `useAuth()` from `@clerk/clerk-react`; if `!isLoaded`, renders a loading indicator; if `!isSignedIn`, returns `<Navigate to="/sign-in" replace />`; otherwise returns `<Outlet />`. |
| T015 | Create `SignInPage` | R002 | In `apps/web/src/pages/auth/SignInPage.tsx` create and export a page component that renders Clerk's `<SignIn />` component from `@clerk/clerk-react` centred in the viewport. |
| T016 | Create `SignUpPage` | R003, EC001 | In `apps/web/src/pages/auth/SignUpPage.tsx` create and export a page component that renders Clerk's `<SignUp />` component from `@clerk/clerk-react` centred in the viewport (Clerk's built-in flow handles OTP email verification, satisfying EC001). |
| T017 | Create `CreateOrgPage` | R008 | In `apps/web/src/pages/org/CreateOrgPage.tsx` create and export a page component that renders Clerk's `<CreateOrganization />` component from `@clerk/clerk-react`. |
| T018 | Create `OrgProfilePage` | R009 | In `apps/web/src/pages/org/OrgProfilePage.tsx` create and export a page component that renders Clerk's `<OrganizationProfile />` component from `@clerk/clerk-react`. |
| T019 | Create `AppLayout` with `UserButton` | R010, EC004 | In `apps/web/src/components/layout/AppLayout.tsx` create and export `AppLayout` as a React layout component that renders `<UserButton />` from `@clerk/clerk-react` in the header and `<Outlet />` from `react-router-dom` for the page content area. |
| T020 | Create `useCurrentUser` hook | R006 | In `apps/web/src/hooks/use-current-user.ts` create and export `useCurrentUser` that calls `useUser()` from `@clerk/clerk-react` and returns `user ?? null` so callers always receive `UserResource \| null`. |
| T021 | Create `useCurrentOrg` hook | R007, EC005 | In `apps/web/src/hooks/use-current-org.ts` create and export `useCurrentOrg` that calls `useOrganization()` from `@clerk/clerk-react` and returns `organization ?? null` (null when unauthenticated or no active org, satisfying EC005). |
| T022 | Extend `useSessionStore` with `userId` and `token` | R006, NF001 | In `apps/web/src/store/session.store.ts` extend `SessionState` to include `userId: string \| null` and `token: () => Promise<string \| null>`; update the `create` call to initialise `userId` to `null` and `token` to an async function returning `null` (callers can override `token` with Clerk's `getToken` after auth is established). |
| T023 | Create `apps/web/.env.example` | R001, EC006 | In `apps/web/.env.example` document `VITE_CLERK_PUBLISHABLE_KEY` and `VITE_API_URL` as required environment variables with placeholder values. |
