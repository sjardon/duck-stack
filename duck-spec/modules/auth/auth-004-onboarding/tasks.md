# AUTH-004 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: migration adds onboarding columns | test | R001 | In `apps/services/tests/unit/users/getUserProfile.test.ts`, write a test asserting that `UserDBRepository.findByClerkUserId` returns `onboarding_completed`, `job_role`, `company_size`, and `primary_use_case` fields (schema-level contract check using a mock `sql` result). |
| T002 | Create onboarding migration | implement | R001 | Create `apps/services/supabase/migrations/20260622224341_users_onboarding_fields.sql`: ALTER TABLE users ADD COLUMN job_role TEXT, company_size TEXT, primary_use_case TEXT, onboarding_completed BOOLEAN NOT NULL DEFAULT FALSE. |
| T003 | Extend UserProfile shared interface | implement | R001, R005 | In `packages/types/src/index.ts`, add `job_role: string \| null`, `company_size: string \| null`, `primary_use_case: string \| null`, and `onboarding_completed: boolean` to the `UserProfile` interface. |
| T004 | Extend UserEntity with onboarding fields | implement | R001 | In `apps/services/src/modules/users/entities/user.entity.ts`, add `job_role`, `company_size`, `primary_use_case` (all `string \| null`), and `onboarding_completed` (`boolean`) to `UserEntity`. |
| T005 | Test: GET /users/me returns onboarding_completed | test | R005 | In `apps/services/tests/unit/users/getUserProfile.test.ts`, write a test for `GetUserProfileUseCase.execute` asserting that the returned `UserProfile` includes `onboarding_completed: false` (and the three nullable segmentation fields) when the repository mock returns a row with those columns. |
| T006 | Extend findByClerkUserId to select onboarding columns | implement | R005 | In `apps/services/src/modules/users/repositories/UserDBRepository.ts`, extend `findByClerkUserId` to SELECT `job_role`, `company_size`, `primary_use_case`, `onboarding_completed` and include them in the returned `UserProfile` object. |
| T007 | Test: CompleteOnboardingUseCase persists and returns updated profile | test | R003, R004, EC004, EC005, EC006 | In `apps/services/tests/unit/users/completeOnboarding.test.ts`, write tests for `CompleteOnboardingUseCase.execute`: (a) WHEN repo resolves, THEN returns UserProfile with onboarding_completed true (R003, R004); (b) WHEN repo throws NotFoundError, THEN use case propagates it (EC004); (c) WHEN repo throws DB error, THEN use case propagates it (EC005); (d) WHEN called on already-completed user, THEN resolves with updated profile (EC006). |
| T008 | Add completeOnboarding to IUserRepository | implement | R002, R003 | In `apps/services/src/modules/users/repositories/interfaces/IUserRepository.ts`, add method signature `completeOnboarding(clerkUserId: string, data: { job_role: string; company_size: string; primary_use_case: string }): Promise<UserProfile>`. |
| T009 | Implement UserDBRepository.completeOnboarding | implement | R003, R004, EC004, EC005, EC006 | In `apps/services/src/modules/users/repositories/UserDBRepository.ts`, implement `completeOnboarding`: execute a single `UPDATE users SET job_role, company_size, primary_use_case, onboarding_completed=TRUE WHERE clerk_user_id=? RETURNING all profile columns`; if `rows.length === 0` throw `NotFoundError('User')`; return mapped `UserProfile`. |
| T010 | Create CompleteOnboardingBody DTO | implement | NF001, EC002, EC003 | Create `apps/services/src/modules/users/dtos/completeOnboarding.dto.ts` exporting `CompleteOnboardingBody` as a strict Zod object with `job_role`, `company_size`, and `primary_use_case` each typed `z.string().min(1)`. |
| T011 | Create CompleteOnboardingUseCase | implement | R003, R004, EC004, EC005, EC006 | Create `apps/services/src/modules/users/useCases/CompleteOnboardingUseCase.ts` with `execute(clerkUserId, data)` that calls `this.repo.completeOnboarding(clerkUserId, data)` and returns the result; no additional business logic needed (repo handles not-found and DB errors). |
| T012 | Test: completeOnboardingHandler validates body | test | NF001, EC001, EC002, EC003 | In `apps/services/tests/unit/users/completeOnboardingHandler.test.ts`, write tests asserting: (a) missing field → 400 VALIDATION_ERROR (EC002); (b) empty string field → 400 VALIDATION_ERROR (EC003); (c) non-string field → 400 VALIDATION_ERROR (EC003); (d) valid body reaches use case (NF001). |
| T013 | Create completeOnboardingHandler | implement | R002, R004, NF001, EC001, EC002, EC003 | Create `apps/services/src/modules/users/handlers/completeOnboardingHandler.ts`: parse `request.body` with `CompleteOnboardingBody.parse`, catch `ZodError` → 400 `{ code: 'VALIDATION_ERROR', message }`, instantiate `UserDBRepository` and `CompleteOnboardingUseCase`, call `execute(request.userId!, body)`, return `reply.send({ data: profile })`. |
| T014 | Register POST /users/me/onboarding route | implement | R002, EC001 | In `apps/services/src/modules/users/routes.ts`, add `fastify.post('/users/me/onboarding', { preHandler: requireAuth }, completeOnboardingHandler)`. |
| T015 | Add postOnboarding API function | implement | R010 | In `apps/web/src/api/users.ts`, add `postOnboarding(token: string, body: { job_role: string; company_size: string; primary_use_case: string }): Promise<UserProfile>` that calls `apiFetch<{ data: UserProfile }>('/users/me/onboarding', { method: 'POST', body: JSON.stringify(body), token })` and returns `response.data`. |
| T016 | Add useCompleteOnboarding hook | implement | R010, R011 | In `apps/web/src/hooks/use-user-profile.ts`, add `useCompleteOnboarding()` using `useMutation` that calls `postOnboarding(token, body)` on `mutationFn` and invalidates `['users', 'me']` in `onSuccess`. |
| T017 | Test: AuthGuard redirects based on onboarding flag | test | R007, R008, NF002, EC007, EC008, EC009 | In `apps/web/tests/AuthGuard.test.tsx`, write Vitest/RTL tests: (a) unauthenticated user → redirected to /sign-in before onboarding check (EC009); (b) authenticated + onboarding_completed=false on protected route → redirected to /onboarding, no protected page rendered (R007, NF002); (c) authenticated + onboarding_completed=true on /onboarding → redirected to / (R008); (d) authenticated + profile loading → render loading state, no redirect (EC007); (e) authenticated + profile error → render neutral state, no redirect (EC008). |
| T018 | Extend AuthGuard with onboarding gate | implement | R007, R008, NF002, EC007, EC008, EC009 | In `apps/web/src/components/auth/AuthGuard.tsx`, import and call `useUserProfile`; after the existing `isSignedIn` check, add: if profile loading or error → return loading/neutral state (before `<Outlet />`); if `onboarding_completed === false` and path !== `/onboarding` → `<Navigate to="/onboarding" replace />`; if `onboarding_completed === true` and path === `/onboarding` → `<Navigate to="/" replace />`; else → `<Outlet />`. The Navigate is returned synchronously before any protected child mounts (NF002). |
| T019 | Test: OnboardingPage renders form and submits | test | R009, R010, R011, EC002, EC003 | In `apps/web/tests/OnboardingPage.test.tsx`, write Vitest/RTL tests: (a) renders welcome heading and three input fields plus submit button (R009); (b) submitting calls mutation with correct payload (R010); (c) on mutation success navigates to / (R011); (d) missing field shows validation error / submit disabled (EC002); (e) empty string field shows validation error (EC003). |
| T020 | Create OnboardingPage | implement | R006, R009, R010, R011 | Create `apps/web/src/pages/onboarding/OnboardingPage.tsx`: render a welcome `<h1>` and a `<form>` with controlled inputs for `job_role`, `company_size`, `primary_use_case` and a submit `<button>`; call `useCompleteOnboarding()` on submit; on mutation success call `navigate('/')`. |
| T021 | Register /onboarding route in router | implement | R006, EC009 | In `apps/web/src/router.tsx`, import `OnboardingPage` and add `{ path: '/onboarding', element: <OnboardingPage /> }` as a child of the `AuthGuard` route element, alongside the existing `/profile` and org routes. |
