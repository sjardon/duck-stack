# AUTH-006 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: confirm baseline TS2554 build failure and write acceptance suite | test | R001, R002, R003, R004, EC001, EC002 | In `apps/services/tests/unit/shared/plugins/requireOrg.test.ts` (new file): first run `pnpm --filter services build` against the current `requireOrg.ts` and confirm it fails with `TS2554: Expected 3 arguments, but got 1` on the `requireAuth(request)` call, establishing the R001 baseline. Then write the acceptance suite for `requireOrg(request, reply, done)`: (a) a case with `request.userId === undefined` asserting it throws `UnauthorizedError` and the `done` spy is never called (R002, EC002 — the org check must not run); (b) a case with `request.userId` set and `request.orgId === null` asserting it throws `ForbiddenError` and the `done` spy is never called (R003, EC001 — Fastify must not be signaled to proceed); (c) a case with `request.userId` set and `request.orgId` a non-null string asserting the `done` spy is called exactly once and no error is thrown (R004). |
| T002 | Implement three-argument composition in `requireOrg` | implement | R001, R002, R003, R004, NF001, EC001, EC002 | In `requireOrg` (`apps/services/src/shared/plugins/requireOrg.ts`), change the signature to `(request: FastifyRequest, reply: FastifyReply, done: () => void): void` and delegate to `requireAuth(request, reply, () => { if (request.orgId === null) throw new ForbiddenError(); done(); })`. This makes `requireAuth`'s own `userId` check and `UnauthorizedError` throw run first, short-circuiting before the org check (R002, EC002); the org check throws `ForbiddenError` before `requireOrg`'s own `done()` is invoked, so Fastify is never signaled to proceed (R003, EC001); and the happy path calls `done()` to continue the chain (R004). The composition stays fully synchronous over already-decorated request fields, adding no measurable latency (NF001). |
| T003 | Test: verify suite passes and build is restored post-fix | test | R001, R002, R003, R004, EC001, EC002 | In `apps/services/tests/unit/shared/plugins/requireOrg.test.ts`, re-run the three cases from T001 and confirm all pass against the fixed `requireOrg`. Re-run `pnpm --filter services build` and confirm it completes with no `TS2554` (or any other) error, closing out R001. |
