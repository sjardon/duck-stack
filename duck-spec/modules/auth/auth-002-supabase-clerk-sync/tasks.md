# AUTH-002 — Tasks

| ID | Title | Covers | Description |
|---|---|---|---|
| T001 | Create identity schema migration | R001, R002, R003, R004 | In `apps/services/supabase/migrations/20240101000000_identity_schema.sql` define the `uuid-ossp` extension, a reusable `set_updated_at()` trigger function, the `users` table (id, clerk_user_id, email, name, avatar_url, created_at, updated_at), the `organizations` table (id, clerk_org_id, name, slug, created_at, updated_at), the `organization_members` table (user_id FK, org_id FK, role, created_at, composite PK), and attach the `updated_at` trigger to `users` and `organizations` |
| T002 | Create seed data file | R013 | In `apps/services/supabase/seed.sql` write idempotent `INSERT ... ON CONFLICT DO NOTHING` statements that populate two example users, one organization, and two organization_members rows using hardcoded UUIDs and representative Clerk IDs |
| T003 | Add svix dependency | R006, NF001 | In `apps/services/package.json` add `svix` as a runtime dependency so `@clerk/backend/webhooks` `verifyWebhook` has its required peer available at runtime |
| T004 | Create ClerkSyncRepository | R009, R010, R011, R012, EC003, EC004, EC005, EC007 | In `apps/services/src/modules/webhooks/clerk/repository.ts` implement the `ClerkSyncRepository` class with: `upsertUser(data: { clerkUserId, email, name, avatarUrl })` that runs `INSERT INTO users ... ON CONFLICT (clerk_user_id) DO UPDATE SET email, name, avatar_url, updated_at`; `upsertOrganization(data: { clerkOrgId, name, slug })` that runs `INSERT INTO organizations ... ON CONFLICT (clerk_org_id) DO UPDATE SET name, slug, updated_at`; `createMembership(data: { clerkUserId, clerkOrgId, role })` that first resolves local user UUID and org UUID via Clerk ID lookups, logs a warning and returns early if either is not found (EC005), then runs `INSERT INTO organization_members ... ON CONFLICT (user_id, org_id) DO NOTHING` |
| T005 | Create webhook event handlers | R009, R010, R011, R012, EC002, EC003, EC004, EC005, EC007 | In `apps/services/src/modules/webhooks/clerk/handlers.ts` implement `handleUserUpsert(event, repo: ClerkSyncRepository)` that extracts `id`, `email_addresses[0].email_address`, `first_name + last_name`, and `image_url` from the event payload and calls `repo.upsertUser`; `handleOrganizationUpsert(event, repo)` that extracts `id`, `name`, `slug` and calls `repo.upsertOrganization`; `handleMembershipCreate(event, repo)` that extracts `public_user_data.user_id`, `organization.id`, and `role` and calls `repo.createMembership`; and `dispatchClerkEvent(event, repo)` that maps `event.type` to the correct handler or returns immediately for unrecognised types (EC002) |
| T006 | Create webhook Fastify plugin | R005, R006, R007, R008, NF001, NF002, EC001, EC006 | In `apps/services/src/modules/webhooks/clerk/routes.ts` implement a `fastify-plugin` export that: (1) reads `CLERK_WEBHOOK_SIGNING_SECRET` at registration time and throws `Error` if absent (EC006); (2) calls `fastify.addContentTypeParser('application/json', { parseAs: 'buffer' }, (_req, body, done) => done(null, body))` so the route handler receives a raw `Buffer` in `request.body` (NF001); (3) registers `POST /webhooks/clerk` with a handler that extracts `svix-id`, `svix-timestamp`, `svix-signature` from `request.headers`, calls `verifyWebhook(body as Buffer, headers)` from `@clerk/backend/webhooks`, catches any throw and replies `400` (R006, R007, EC001), then calls `dispatchClerkEvent` and replies `200` on success (R008, NF002); constructs a `ClerkSyncRepository` instance using the shared `supabase` singleton |
| T007 | Register webhook plugin in app.ts | R005 | In `apps/services/src/app.ts` import `clerkWebhookRoutes` from `./modules/webhooks/clerk/routes.js` and register it with `fastify.register(clerkWebhookRoutes)` before the `clerkAuthPlugin` registration so the raw body parser is set up before the global auth hook runs |
