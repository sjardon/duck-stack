# AUTH-003 — Tasks

| ID | Title | Covers | Description |
|---|---|---|---|
| T001 | Add locale and timezone migration | R001 | In `20260622000000_users_locale_timezone.sql` write the SQL ALTER TABLE statement adding nullable `locale` (TEXT) and `timezone` (TEXT) columns to the `users` table |
| T002 | Export UserProfile type | R012 | In `packages/types/src/index.ts` add and export the `UserProfile` interface with fields `name`, `email`, `avatar_url`, `locale`, and `timezone` |
| T003 | Define user entity | R003, R005 | In `apps/services/src/modules/users/entities/user.entity.ts` define an internal `UserEntity` type matching the full `users` table row shape including `id`, `clerk_user_id`, and the new `locale`/`timezone` columns |
| T004 | Define IUserRepository interface | R002, R003, R004, R005 | In `apps/services/src/modules/users/repositories/interfaces/IUserRepository.ts` declare `IUserRepository` with methods `findByClerkUserId(clerkUserId: string): Promise<UserProfile \| null>` and `updatePreferences(clerkUserId: string, patch: { locale?: string \| null; timezone?: string \| null }): Promise<UserProfile>` |
| T005 | Implement UserDBRepository.findByClerkUserId | R003, NF001, EC001, EC002 | In `UserDBRepository.findByClerkUserId` query the Supabase `users` table using `.eq('clerk_user_id', clerkUserId).maybeSingle()`, map the row to `UserProfile`, and return `null` when no row is found |
| T006 | Implement UserDBRepository.updatePreferences | R005, EC004, EC006 | In `UserDBRepository.updatePreferences` issue a Supabase UPDATE on `users` setting only supplied `locale`/`timezone` fields (spread only defined keys), select the updated row with `.select().single()`, and throw on DB error |
| T007 | Define UpdateProfile DTO schema | R006, NF002, EC005 | In `apps/services/src/modules/users/dtos/updateProfile.dto.ts` define and export `UpdateProfileBody` as a Zod object with `locale: z.string().nullable().optional()` and `timezone: z.string().nullable().optional()` using `.strict()` to reject unknown fields |
| T008 | Implement GetUserProfileUseCase | R003, EC002 | In `GetUserProfileUseCase.execute(clerkUserId: string)` call `this.repo.findByClerkUserId(clerkUserId)` and throw a `NotFoundError('User')` when the result is `null` |
| T009 | Implement UpdateUserProfileUseCase | R005, EC003, EC006 | In `UpdateUserProfileUseCase.execute(clerkUserId: string, patch)` detect an empty patch (no locale or timezone key present) and return the current profile via `this.repo.findByClerkUserId`; otherwise call `this.repo.updatePreferences(clerkUserId, patch)` and return the result |
| T010 | Implement getUserProfileHandler | R002, R003, EC001, EC002 | In `getUserProfileHandler` instantiate `UserDBRepository` and `GetUserProfileUseCase`, extract `request.userId` (guaranteed by `requireAuth`), call `useCase.execute`, and reply with `{ data: profile }` |
| T011 | Implement updateUserProfileHandler | R004, R005, R006, EC003, EC004, EC005 | In `updateUserProfileHandler` parse the request body with `UpdateProfileBody.parse(request.body)` (Zod throws on invalid input), instantiate `UserDBRepository` and `UpdateUserProfileUseCase`, call `useCase.execute`, and reply with `{ data: profile }` |
| T012 | Register users routes plugin | R002, R004, EC001 | In `apps/services/src/modules/users/routes.ts` create a `fastify-plugin` that registers `GET /users/me` and `PATCH /users/me`, each with `preHandler: requireAuth`, delegating to the respective handlers |
| T013 | Register users plugin in app | R002, R004 | In `apps/services/src/app.ts` import `usersRoutes` and call `await fastify.register(usersRoutes)` after the clerk-auth plugin registration |
| T014 | Implement fetchUserProfile API function | R008 | In `apps/web/src/api/users.ts` implement `fetchUserProfile(token: string): Promise<UserProfile>` calling `apiFetch<{ data: UserProfile }>('/users/me', { token })` and returning `.data` |
| T015 | Implement patchUserProfile API function | R009 | In `apps/web/src/api/users.ts` implement `patchUserProfile(token: string, body: { locale?: string \| null; timezone?: string \| null }): Promise<UserProfile>` calling `apiFetch<{ data: UserProfile }>('/users/me', { method: 'PATCH', body: JSON.stringify(body), token })` and returning `.data` |
| T016 | Implement useUserProfile hook | R008, NF001 | In `apps/web/src/hooks/use-user-profile.ts` implement `useUserProfile()` using `useQuery` with key `['users', 'me']`; obtain the Clerk token via `useAuth().getToken()` inside `queryFn` before calling `fetchUserProfile` |
| T017 | Implement useUpdateProfile hook | R009, R010, R011, EC006 | In `apps/web/src/hooks/use-user-profile.ts` implement `useUpdateProfile()` using `useMutation` calling `patchUserProfile`; in `onSuccess` invalidate `['users', 'me']`; expose `isSuccess` and `isError` states for feedback rendering |
| T018 | Build ProfilePage component | R007, R008, R009, R010, R011, EC007 | In `apps/web/src/pages/profile/ProfilePage.tsx` implement the page: render `name`, `email`, avatar (with a fallback `<div>` placeholder when `avatar_url` is null), `locale`, and `timezone` from `useUserProfile()`; render a controlled form for `locale` and `timezone` that calls `useUpdateProfile().mutate(...)` on submit; display a visible success message when `isSuccess` is true and a visible error message when `isError` is true |
| T019 | Add /profile route to router | R007 | In `apps/web/src/router.tsx` add `{ path: '/profile', element: <ProfilePage /> }` as a child of the existing `AuthGuard` element |
