# INFRA-008 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: spec file exists and is valid YAML | test | R001 | In `.do/tests/app-spec.test.sh` assert `.do/app.yaml` exists, parses as valid YAML, and its top-level `name` field is the literal placeholder `${DO_APP_NAME}`. |
| T002 | Implement spec skeleton | implement | R001 | Create `.do/app.yaml` with a header comment explaining the templating mechanism, `name: ${DO_APP_NAME}`, `region: nyc`, and an empty `services:` list. |
| T003 | Test: exactly one service component with public ingress | test | R002 | In `.do/tests/app-spec.test.sh` assert `services` has exactly one entry named `services`, and it declares a non-empty `http_port`. |
| T004 | Implement service component identity and ingress | implement | R002 | In `.do/app.yaml` add the single `services[0]` entry (`name: services`), `instance_count: 1`, `instance_size_slug: apps-s-1vcpu-1gb`, and a placeholder `http_port`. |
| T005 | Test: root build context and Dockerfile path | test | R004, R005, EC001 | In `.do/tests/app-spec.test.sh` assert `services[0].source_dir` is `/` and `services[0].dockerfile_path` is `apps/services/Dockerfile`; additionally, if `docker` is available, run `docker build -f apps/services/Dockerfile --target builder .` from the repo root and assert it exits 0. |
| T006 | Implement build context and source configuration | implement | R004, R005, EC001 | In `.do/app.yaml` set `services[0].source_dir: /`, `services[0].dockerfile_path: apps/services/Dockerfile`, and `services[0].github` with `repo: ${GITHUB_REPO}`, `branch: ${GIT_BRANCH}`, `deploy_on_push: false`. |
| T007 | Test: every R006 environment variable is declared | test | R006 | In `.do/tests/app-spec.test.sh` assert `services[0].envs` contains exactly the 19 keys from R006 (`NODE_ENV`, `LOG_LEVEL`, `HOST`, `PORT`, `CORS_ORIGIN`, `DATABASE_URL`, `CLERK_SECRET_KEY`, `CLERK_JWT_KEY`, `CLERK_WEBHOOK_SIGNING_SECRET`, `EMAIL_SENDER_ADDRESS`, `SES_REGION`, `BILLING_PROVIDER`, `MOBBEX_API_KEY`, `MOBBEX_ACCESS_TOKEN`, `MOBBEX_TEST_MODE`, `MOBBEX_TIMEOUT_MS`, `MOBBEX_WEBHOOK_SECRET`, `SIGNUP_MODE`, `FREE_TRIAL_DAYS`, `STRICT_ENTITLEMENTS_ON_PAST_DUE`), each with `scope: RUN_TIME` and value `${<KEY>}`. |
| T008 | Implement envs list | implement | R006, R007 | In `.do/app.yaml` add `services[0].envs` with one entry per R006 variable, `scope: RUN_TIME`, `type: general`, `value: ${<KEY>}` for every key. |
| T009 | Test: sensitive variables are typed secret | test | R008 | In `.do/tests/app-spec.test.sh` assert `type: secret` on `DATABASE_URL`, `CLERK_SECRET_KEY`, `CLERK_JWT_KEY`, `CLERK_WEBHOOK_SIGNING_SECRET`, `MOBBEX_API_KEY`, `MOBBEX_ACCESS_TOKEN`, `MOBBEX_WEBHOOK_SECRET`, and `type: general` on every other declared key. |
| T010 | Implement secret typing | implement | R008 | In `.do/app.yaml` change `type` to `secret` for the 7 keys R008 classifies as sensitive; leave the remaining keys `type: general`. |
| T011 | Test: health check aligned with the service port | test | R009, EC006 | In `.do/tests/app-spec.test.sh` assert `services[0].health_check.http_path` is `/health` and both `services[0].health_check.port` and `services[0].http_port` resolve to the same placeholder token as the `PORT` env entry's value. |
| T012 | Implement health check and port alignment | implement | R009, EC006 | In `.do/app.yaml` set `services[0].http_port: ${PORT}`, add `services[0].health_check` with `http_path: /health`, `port: ${PORT}`, `initial_delay_seconds: 10`, `period_seconds: 10`, `timeout_seconds: 5`, `success_threshold: 1`, `failure_threshold: 3`. |
| T013 | Test: SES placeholder is annotated | test | EC004 | In `.do/tests/app-spec.test.sh` assert the `SES_REGION` entry in `.do/app.yaml` is immediately preceded by a comment referencing the decommissioned AWS SES integration. |
| T014 | Implement SES placeholder annotation | implement | EC004 | In `.do/app.yaml` add a comment above the `SES_REGION` env entry stating it is an inert placeholder pending migration of the email adapter off SES. |
| T015 | Refactor spec for readability | refactor | R001, R006, R007 | In `.do/app.yaml` group `envs` entries by the config-file scopes from `duck-spec/docs/BACKEND.md` (`serverConfig`, `authConfig`, email, `mobbexConfig`, `subscriptionsConfig`) with section comments; ensure indentation and key ordering are consistent throughout. |
| T016 | Test: deploy reconciles via upsert | test | R010, NF001 | In `.do/tests/deploy-script.test.sh` assert `.do/deploy.sh` invokes `doctl apps create` with both `--spec` and `--upsert` and `--wait` flags (via a stub `doctl` on `PATH` that records its argv). |
| T017 | Implement deploy.sh core | implement | R010, NF001 | Create `.do/deploy.sh`: accept a values-file path argument, `source` it (`set -a`/`set +a`), render `.do/app.yaml` with `envsubst` into a `mktemp` file cleaned up via `trap`, and call `doctl apps create --spec <rendered> --upsert --wait --format ID,DefaultIngress --no-header`. |
| T018 | Test: fails fast on a missing required variable | test | EC002, R006 | In `.do/tests/deploy-script.test.sh` assert that running `.do/deploy.sh` with a values file missing `EMAIL_SENDER_ADDRESS` exits non-zero with a message identifying the missing variable, without invoking the stub `doctl`. |
| T019 | Implement required-variable validation | implement | EC002, R006 | In `.do/deploy.sh`, after sourcing the values file and before rendering, validate every placeholder referenced by `.do/app.yaml` is non-empty using `${VAR:?missing $VAR — see .do/README.md}`. |
| T020 | Test: public URL is printed after deploy | test | R011, R003 | In `.do/tests/deploy-script.test.sh` assert that, given the stub `doctl` returns a fixed `ID` and `DefaultIngress` value, `.do/deploy.sh`'s stdout contains that URL and a reminder to record it in `.do/README.md`. |
| T021 | Implement public URL output | implement | R011, R003 | In `.do/deploy.sh`, parse the `ID`/`DefaultIngress` columns from the `doctl` output and print `Public URL: <url>` plus a reminder to record it under "Current deployments" in `.do/README.md`. |
| T022 | Refactor deploy script | refactor | R010, NF002 | In `.do/deploy.sh`, extract validation and rendering into named functions, add a `usage()` help block for missing arguments, and quote all variable expansions. |
| T023 | Test: example file enumerates every placeholder | test | R007 | In `.do/tests/env-example.test.sh` assert every `${VAR}` token found in `.do/app.yaml` has a corresponding `VAR=` line in `.do/.env.deploy.example`. |
| T024 | Implement values-file example | implement | R007 | Create `.do/.env.deploy.example` with `DO_APP_NAME`, `GITHUB_REPO`, `GIT_BRANCH`, and all 19 R006 variables, grouped with comments and safe example values for non-secret fields. |
| T025 | Test: secret keys carry no example value | test | NF003 | In `.do/tests/env-example.test.sh` assert the 7 keys R008 classifies as sensitive are declared with an empty value (`KEY=`) in `.do/.env.deploy.example`. |
| T026 | Implement blank secret placeholders | implement | NF003, R008 | In `.do/.env.deploy.example` leave `DATABASE_URL`, `CLERK_SECRET_KEY`, `CLERK_JWT_KEY`, `CLERK_WEBHOOK_SIGNING_SECRET`, `MOBBEX_API_KEY`, `MOBBEX_ACCESS_TOKEN`, and `MOBBEX_WEBHOOK_SECRET` blank with a comment naming what belongs there. |
| T027 | Test: per-environment values files are git-ignored | test | NF003 | In `.do/tests/gitignore.test.sh` assert `git check-ignore .do/.env.deploy.dev` exits 0 and `git check-ignore .do/.env.deploy.example` exits 1 (not ignored). |
| T028 | Implement gitignore rule | implement | NF003 | In `.gitignore` add `.do/.env.deploy.*` followed by `!.do/.env.deploy.example`. |
| T029 | Test: README documents the full procedure | test | NF002, R010, R011, EC003, EC005 | In `.do/tests/readme.test.sh` assert `.do/README.md` contains a "Prerequisites" section, a step-by-step deploy procedure invoking `.do/deploy.sh`, a statement that console edits are overwritten by the next run, the exact EC002 error string, the `doctl apps delete` command, and a "Current deployments" table with `Environment`, `Public URL`, and `Last updated` columns. |
| T030 | Implement deploy runbook | implement | NF002, R010, R011, EC002, EC003, EC005 | Create `.do/README.md` with: prerequisites (`doctl`, `envsubst`/`gettext`, DigitalOcean App Platform team access, GitHub App installed, externally provisioned runtime credentials); the procedure to copy `.env.deploy.example`, fill it in, and run `.do/deploy.sh`; the EC005 drift statement; the EC002 failure signature (`Missing required env var: EMAIL_SENDER_ADDRESS`) documented as a missing-configuration failure; the EC003 cost note and `doctl apps delete <app-id>` command; and an empty "Current deployments" table (`Environment | Public URL | Last updated`) for operators to fill in after each deploy. |
| T031 | Refactor documentation for consistency | refactor | NF002, R007 | Cross-check `.do/README.md` and `.do/.env.deploy.example` use identical variable names, ordering, and section headings; fix any wording gaps so every step in the procedure is explicit and no step relies on unstated prior knowledge. |
