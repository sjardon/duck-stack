# INFRA-013 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: provider catalogue | test | R001, R002, R003, EC003 | Write failing assertions in `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN it has a "Provider catalogue" section containing one row per provider (GitHub, DigitalOcean App Platform, Cloudflare Pages, Supabase, Clerk, Mobbex, Resend, Better Stack — Logs & Uptime, Better Stack — error tracking, PostHog) with non-empty "Used for", "Resources to create" and "Credentials issued" cells, and that the Better Stack — Logs & Uptime row references `.do/monitoring/README.md`. |
| T002 | Implement provider catalogue | implement | R001, R002, R003, EC003 | In `duck-spec/docs/RUNBOOK.md`, write the "Provider catalogue" section with the table and Better Stack limits note exactly as specified in design.md's "1. Provider catalogue". |
| T003 | Refactor provider catalogue | refactor | R001, R002, R003, EC003 | In `duck-spec/docs/RUNBOOK.md`, tighten the "Provider catalogue" wording, verify no cell duplicates procedural detail already owned by a component README, and confirm the table renders (consistent column count per row). |
| T004 | Test: environment variable inventory | test | R004, R005, R006, NF002, NF003 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN it has an "Environment variable inventory" section with a subsection per component (`services`, `web`, `landing`, "Deploy tooling only"), each variable name from design.md's inventory list appears at least once, a "Verifiability" paragraph names `.do/app.yaml`, `.cloudflare/.env.deploy.web.example`/`.env.deploy.landing.example` and `.github/README.md`, and no line in the file matches a real-secret-looking pattern (e.g. `sk_live_[A-Za-z0-9]{10,}`, `postgres://[^$]`). |
| T005 | Implement environment variable inventory | implement | R004, R005, R006, NF002, NF003 | In `duck-spec/docs/RUNBOOK.md`, write the "Environment variable inventory" section with the four component subsections, "Consumed by" and "Origin" columns, and the "Verifiability (NF002)" paragraph exactly as specified in design.md's "2. Environment variable inventory". |
| T006 | Refactor environment variable inventory | refactor | R004, R005, R006, NF002, NF003 | In `duck-spec/docs/RUNBOOK.md`, cross-check every variable name against `.do/app.yaml`, `.cloudflare/.env.deploy.web.example`, `.cloudflare/.env.deploy.landing.example` and `.github/README.md`'s `vars.*`/`secrets.*` lists, fixing any mismatch or omission. |
| T007 | Test: from-scratch provisioning sequence | test | R007, EC001, EC002, EC005 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN it has a "From-scratch provisioning sequence" section whose table has "Scope" and "Blocking" columns, at least one row per scope value (`per-account`, `per-environment`), at least two rows marked `blocking`, and at least three rows containing the text "Depends on". |
| T008 | Implement from-scratch provisioning sequence | implement | R007, EC001, EC002, EC005 | In `duck-spec/docs/RUNBOOK.md`, write the "From-scratch provisioning sequence" section with the 17-step ordered table and the mermaid dependency diagram exactly as specified in design.md's "3. From-scratch provisioning sequence". |
| T009 | Refactor from-scratch provisioning sequence | refactor | R007, EC001, EC002, EC005 | In `duck-spec/docs/RUNBOOK.md`, verify the step numbers referenced by later "Recurring operations" and "Manual steps enumeration" content stay consistent with the table, and tighten step wording for a first-time reader (NF001). |
| T010 | Test: deploy and deploy-specific-commit operations | test | R008, R009 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN it has a "Recurring operations" section containing a "Deploy to an environment" subsection referencing `.do/deploy.sh` and `.cloudflare/deploy.sh`, and a "Deploy a specific commit" subsection referencing `deploy-manual.yml`. |
| T011 | Implement deploy and deploy-specific-commit operations | implement | R008, R009 | In `duck-spec/docs/RUNBOOK.md`, write the "Deploy to an environment" and "Deploy a specific commit" subsections exactly as specified in design.md's "4. Recurring operations". |
| T012 | Refactor deploy and deploy-specific-commit operations | refactor | R008, R009 | In `duck-spec/docs/RUNBOOK.md`, confirm both subsections reference `.do/README.md`, `.cloudflare/README.md` and `.github/README.md` instead of restating their command-level detail. |
| T013 | Test: rollback operation | test | R010 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN its "Recurring operations" section has a "Rollback" subsection referencing `rollback.yml` and stating that already-applied data changes are not reverted. |
| T014 | Implement rollback operation | implement | R010 | In `duck-spec/docs/RUNBOOK.md`, write the "Rollback" subsection exactly as specified in design.md's "4. Recurring operations". |
| T015 | Refactor rollback operation | refactor | R010 | In `duck-spec/docs/RUNBOOK.md`, align the "Rollback" subsection's wording with `.github/README.md`'s data-change caveat so both state the same operator responsibility without contradiction. |
| T016 | Test: credential rotation operation | test | R011 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN its "Recurring operations" section has a "Credential rotation" subsection with a per-category table (backend secret, public `VITE_*` value, deploy-tooling-only secret) and the three universal sub-steps (revoke/reissue, update every stored location, redeploy). |
| T017 | Implement credential rotation operation | implement | R011 | In `duck-spec/docs/RUNBOOK.md`, write the "Credential rotation" subsection with the per-category table and the three universal sub-steps exactly as specified in design.md's "4. Recurring operations". |
| T018 | Refactor credential rotation operation | refactor | R011 | In `duck-spec/docs/RUNBOOK.md`, verify every variable listed in the environment variable inventory (T005) maps to exactly one row of the credential-rotation category table, adding any missing variable. |
| T019 | Test: manual steps enumeration | test | R012 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN its "Recurring operations" section has a "Manual steps enumeration" subsection whose table cross-references at least four step numbers from the provisioning sequence. |
| T020 | Implement manual steps enumeration | implement | R012 | In `duck-spec/docs/RUNBOOK.md`, write the "Manual steps enumeration" subsection exactly as specified in design.md's "4. Recurring operations", cross-referencing the step numbers from the provisioning sequence (T008). |
| T021 | Refactor manual steps enumeration | refactor | R012 | In `duck-spec/docs/RUNBOOK.md`, verify every `blocking` step and every provider-console-only action from the provisioning sequence (T008) has a corresponding row in the manual steps enumeration, adding any missing one. |
| T022 | Test: maintenance rule and document-level readability | test | NF001, EC004 | Extend `duck-spec/docs/tests/runbook.test.sh` asserting: WHEN `duck-spec/docs/RUNBOOK.md` is read THEN a "Maintenance rule" section appears before "Provider catalogue" and states that adding, renaming or removing an environment variable, a provider, or a deploy step requires updating the runbook in the same change, and that the document contains no credentials/no real environment-specific values. |
| T023 | Implement maintenance rule and document-level readability | implement | NF001, EC004 | In `duck-spec/docs/RUNBOOK.md`, write the "Maintenance rule" section as the first section of the document, exactly as specified in design.md's "Document skeleton". |
| T024 | Refactor maintenance rule and document-level readability | refactor | NF001, EC004 | Read `duck-spec/docs/RUNBOOK.md` start to finish as a first-time reader would, fixing any forward reference, inconsistent step numbering, or undefined term across all five sections; run `bash duck-spec/docs/tests/runbook.test.sh` and fix every failing assertion. |
