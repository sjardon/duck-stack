# INFRA-012 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: pin-do-deploy-ref.sh moves the environment's deploy ref | test | R001, R002, R003, R004 | Write failing acceptance assertions in `.github/tests/pin-deploy-ref-script.test.sh` using a stub `git` on `PATH` that records its argv: running `.github/scripts/pin-do-deploy-ref.sh dev <sha>` invokes `git push --force origin <sha>:refs/heads/_deploy/dev` and prints `_deploy/dev` on stdout; running it with `prod`/a different sha does the equivalent for `_deploy/prod`. |
| T002 | Implement pin-do-deploy-ref.sh | implement | R001, R002, R003, R004 | Create `.github/scripts/pin-do-deploy-ref.sh` (`set -euo pipefail`, usage validation for the two positional args `<environment> <commit_sha>`) implementing `main()`: `git push --force origin "${commit_sha}:refs/heads/_deploy/${environment}"` then `echo "_deploy/${environment}"`. |
| T003 | Refactor pin-do-deploy-ref.sh error handling | refactor | R001, R002, R003, R004 | Extract a `usage()` function mirroring `.do/deploy.sh`'s style, add a header comment referencing the DigitalOcean App Spec branch-only limitation this script works around, keep `.github/tests/pin-deploy-ref-script.test.sh` green. |
| T004 | Test: write-values-file.sh derives the services manifest from app.yaml placeholders | test | R008 | Write failing acceptance assertions in `.github/tests/write-values-file-script.test.sh`: against a fixture file containing `${FOO}`/`${BAR}` placeholders (standing in for `.do/app.yaml`), with `FOO`/`BAR` exported in the environment, `write-values-file.sh services <out> <fixture-path>` (test-only third arg overriding the default `.do/app.yaml` path) writes exactly `FOO=<value>` and `BAR=<value>` to `<out>`, sorted and de-duplicated the same way `.do/deploy.sh`'s `validate_placeholders()` already does. |
| T005 | Test: write-values-file.sh writes the fixed web/landing manifests | test | R008 | Extend `.github/tests/write-values-file-script.test.sh`: `write-values-file.sh web <out>` writes `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `GIT_BRANCH`, `VITE_API_URL`, `VITE_CLERK_PUBLISHABLE_KEY`, `VITE_LANDING_URL`, `VITE_PROVIDER_PORTAL_URL`, plus the shared optional set (`VITE_ERROR_TRACKING_DSN`, `VITE_RELEASE`, `VITE_ENVIRONMENT`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_URL`, `VITE_POSTHOG_KEY`, `VITE_POSTHOG_HOST`) — present with their exported values or empty when unset; `write-values-file.sh landing <out>` writes the same common+optional set plus `VITE_API_URL`, `VITE_WEB_URL` only (no `web`-only keys). |
| T006 | Implement write-values-file.sh | implement | R008 | Create `.github/scripts/write-values-file.sh` implementing `main()` dispatch on `$1` (`services\|web\|landing`): `write_services_values()` (grep `\$\{[A-Z0-9_]+\}` from the given/default `.do/app.yaml` path, sorted unique, emit `NAME=${!NAME:-}` per token) and `write_web_values()`/`write_landing_values()` (fixed arrays per the design's manifest, emit `NAME=${!NAME:-}` per entry) to the given output path. |
| T007 | Refactor write-values-file.sh shared emit helper | refactor | R008 | Extract a shared `emit_values(output_path, names...)` helper used by all three write functions to remove duplicated `NAME=${!NAME:-}` loop logic, keeping `.github/tests/write-values-file-script.test.sh` green. |
| T008 | Test: report-deploy-summary.sh prints per-app status and fails on any non-success outcome | test | R006, NF002 | Write failing acceptance assertions in `.github/tests/report-deploy-summary-script.test.sh`: given `environment=dev`, `commit_sha=<sha>`, three `(outcome, log-file)` pairs where each log file contains a `Public URL: https://…` line, running `report-deploy-summary.sh` prints one line per app naming the app, `dev`, `<sha>`, published/not-published, and the parsed URL when published; the script exits 0 when all three outcomes are `success`, and exits non-zero — printing which app(s) failed — when any outcome is `failure`. |
| T009 | Implement report-deploy-summary.sh | implement | R006, NF002 | Create `.github/scripts/report-deploy-summary.sh` implementing `main()`: parse the `environment`, `commit_sha`, and three `(outcome, log_path)` pairs; for each app print `app=<name> environment=<environment> commit=<commit_sha> status=<published|not-published> url=<parsed-or-empty>`; track and `exit 1` if any outcome is not `success`. |
| T010 | Refactor report-deploy-summary.sh URL parsing | refactor | R006 | Extract a `parse_public_url(log_path)` helper (reusing the same `grep -oE` idiom `.cloudflare/deploy.sh`/`.do/deploy.sh` output already contains) shared by all three apps, keeping `.github/tests/report-deploy-summary-script.test.sh` green. |
| T011 | Test: apps/landing/vite.config.ts rethrows a failed source-map upload | test | R005, EC002 | Write a failing acceptance test in `apps/landing/tests/vite.config.test.ts` (mirroring the equivalent `apps/web` test if one exists, otherwise a new minimal test importing `resolveSentryVitePluginOptions`-equivalent config shape) asserting the exported Sentry plugin options include an `errorHandler` that rethrows the error it receives, matching `apps/web/vite.config.ts`'s behavior. |
| T012 | Implement errorHandler in apps/landing/vite.config.ts | implement | R005, EC002 | In `apps/landing/vite.config.ts`'s `resolveSentryVitePluginOptions()`, add `errorHandler: (error: Error) => { throw error; }` with the same explanatory comment `apps/web/vite.config.ts` carries, closing the EC002 asymmetry so a failed upload fails `vite build` instead of being logged and swallowed. |
| T013 | Refactor vite.config.ts comment parity | refactor | R005, EC002 | Align `apps/landing/vite.config.ts`'s comments/formatting around the new `errorHandler` with `apps/web/vite.config.ts`'s wording, keeping both files' structure easy to diff against each other. |
| T014 | Test: deploy-apps.yml resolves per-environment config with no hardcoded branching | test | R008, NF002, EC006 | Write failing acceptance assertions in `.github/tests/deploy-apps-workflow.test.sh` (PyYAML-parsed, mirroring `.do/tests/app-spec.test.sh`'s `STRUCT_OUTPUT` pattern): `on.workflow_call.inputs` declares `environment` and `commit_sha` as required strings; the `deploy` job's `environment` key equals the literal expression `${{ inputs.environment }}`; `permissions.contents == "write"`; no step content matches `environment == 'prod'` / `environment == 'dev'` (no per-environment branching). |
| T015 | Test: deploy-apps.yml runs three continue-on-error steps then an unconditional report step | test | R001, R002, R003, R004, R006, NF002, EC001, EC006 | Extend `.github/tests/deploy-apps-workflow.test.sh`: exactly three steps with `continue-on-error: true` exist, one invoking `.do/deploy.sh` (preceded by a call to `.github/scripts/pin-do-deploy-ref.sh`), one invoking `.cloudflare/deploy.sh web`, one invoking `.cloudflare/deploy.sh landing`; a final step has `if: always()` and invokes `.github/scripts/report-deploy-summary.sh`; the three deploy steps appear in the job's step list before the report step (sequential, not parallel jobs). |
| T016 | Implement deploy-apps.yml | implement | R001, R002, R003, R004, R005, R006, R008, NF001, NF002, EC001, EC002, EC005, EC006 | Create `.github/workflows/deploy-apps.yml` per the design: `workflow_call` inputs, `permissions: contents: write`, single `deploy` job with `environment: ${{ inputs.environment }}`, checkout at `commit_sha`, `pnpm/action-setup` + `actions/setup-node` + `digitalocean/action-doctl@v2`, three `continue-on-error` steps (services via `pin-do-deploy-ref.sh` + `write-values-file.sh services` + `.do/deploy.sh`; web and landing via `write-values-file.sh` + `.cloudflare/deploy.sh <app>`, each with `VITE_RELEASE`/`SERVICE_VERSION` set to `inputs.commit_sha`), and the final `if: always()` call to `report-deploy-summary.sh`. |
| T017 | Refactor deploy-apps.yml step naming and env blocks | refactor | R006, R008 | Align each step's `env:` block ordering/comments with the design's `vars`/`secrets` grouping (general vs secret-classified, mirroring `.do/app.yaml`'s own section comments), keeping `.github/tests/deploy-apps-workflow.test.sh` green. |
| T018 | Test: deploy-dev.yml triggers on push to develop and serializes per environment | test | R001, R007, EC004 | Write failing acceptance assertions in `.github/tests/trigger-workflows.test.sh`: `deploy-dev.yml`'s `on.push.branches == ["develop"]`; the calling job's `concurrency == { group: "deploy-dev", cancel-in-progress: false }`; it calls `deploy-apps.yml` with `with: { environment: "dev", commit_sha: "${{ github.sha }}" }`. |
| T019 | Implement deploy-dev.yml | implement | R001, R007 | Create `.github/workflows/deploy-dev.yml`: `on: push: branches: [develop]`, one job `uses: ./.github/workflows/deploy-apps.yml` with `concurrency: { group: deploy-dev, cancel-in-progress: false }` and `with: { environment: dev, commit_sha: ${{ github.sha }} }`. |
| T020 | Test: deploy-prod.yml triggers on push to main and serializes per environment | test | R002, R007, EC004 | Extend `.github/tests/trigger-workflows.test.sh`: `deploy-prod.yml`'s `on.push.branches == ["main"]`; concurrency group `deploy-prod`, `cancel-in-progress: false`; calls `deploy-apps.yml` with `environment: "prod"`, `commit_sha: "${{ github.sha }}"`. |
| T021 | Implement deploy-prod.yml | implement | R002, R007 | Create `.github/workflows/deploy-prod.yml`, mirroring `deploy-dev.yml` with `branches: [main]`, `group: deploy-prod`, `environment: prod`. |
| T022 | Refactor deploy-dev.yml / deploy-prod.yml shared shape | refactor | R001, R002, R007 | Verify and, if needed, align both files' step/job naming and comment header so their only differences are the branch, the concurrency group, and the `environment` input, keeping `.github/tests/trigger-workflows.test.sh` green. |
| T023 | Test: deploy-manual.yml accepts an arbitrary commit and target environment | test | R003, R007, R008 | Extend `.github/tests/trigger-workflows.test.sh`: `deploy-manual.yml`'s `on.workflow_dispatch.inputs` declares `environment` (`type: choice`, `options: [dev, prod]`, required) and `commit_sha` (`type: string`, required); concurrency group is the literal expression `deploy-${{ inputs.environment }}` with `cancel-in-progress: false`; it calls `deploy-apps.yml` with `with: { environment: ${{ inputs.environment }}, commit_sha: ${{ inputs.commit_sha }} }`. |
| T024 | Implement deploy-manual.yml | implement | R003, R007, R008 | Create `.github/workflows/deploy-manual.yml` per the assertions in T023, `uses: ./.github/workflows/deploy-apps.yml`. |
| T025 | Refactor deploy-manual.yml input validation messaging | refactor | R003 | Add `description:` text to both `workflow_dispatch` inputs (matching the operator-facing wording in `.github/README.md`), keeping `.github/tests/trigger-workflows.test.sh` green. |
| T026 | Test: rollback.yml warns about irreversible data changes before deploying | test | R004, EC003 | Extend `.github/tests/trigger-workflows.test.sh`: `rollback.yml` has the same `workflow_dispatch` inputs/concurrency shape as `deploy-manual.yml`; its job declares a step, ordered before the step that calls `deploy-apps.yml`, whose `run:` content matches (case-insensitively) both "not reverted" and "database state". |
| T027 | Implement rollback.yml | implement | R004, EC003 | Create `.github/workflows/rollback.yml`: same inputs/concurrency as `deploy-manual.yml`, an unconditional first step printing the EC003 warning (`::warning::…not reverted…database state…`), then `uses: ./.github/workflows/deploy-apps.yml` with the same `with:` mapping. |
| T028 | Refactor rollback.yml / deploy-manual.yml shared shape | refactor | R003, R004, R007, R008 | Align `rollback.yml` and `deploy-manual.yml`'s input declarations/comments so their only functional difference is the warning step, keeping `.github/tests/trigger-workflows.test.sh` green. |
| T029 | Test: .github/README.md documents the CI-configuration contract and edge-case behaviors | test | NF001, NF002, EC001, EC003, EC004, EC005, EC006 | Write failing acceptance assertions in `.github/tests/readme.test.sh` (mirroring `.do/tests/readme.test.sh`): the file lists every `vars.*`/`secrets.*` name from the design's per-environment contract; states the EC001 partial-failure recovery procedure (re-run `deploy-manual.yml` with the same commit, or `rollback.yml` with the previous one); states the EC003 rollback data-caveat verbatim (case-insensitive "not reverted" + "database state"); states the EC004/R007 concurrency queueing behavior; states the EC005 cross-reference (new placeholders must be added to `app.yaml`/the values contract and to the environment's CI configuration); states the EC006 wait-for-all-three-apps guarantee; and states that no secret is ever committed (NF001) and a partial delivery always fails the run (NF002). |
| T030 | Implement .github/README.md | implement | NF001, NF002, EC001, EC003, EC004, EC005, EC006 | Create `.github/README.md` with the sections asserted by T029: triggers overview, the full per-environment `vars`/`secrets` contract table, the `_deploy/<environment>` ref mechanism and its branch-protection caveat, concurrency behavior, the partial-failure recovery procedure, the rollback data caveat, and the EC005 cross-reference. |
| T031 | Refactor .github/README.md cross-links | refactor | NF001, EC005 | Cross-link `.github/README.md` from `.do/README.md`'s and `.cloudflare/README.md`'s "Contents" tables (see T032/T033) and vice versa, so an operator following either existing runbook discovers the pipeline doc. |
| T032 | Test: .do/README.md cross-references the CI/CD runbook | test | R001, R002, R003 | Extend `.do/tests/readme.test.sh` (or add an assertion block if the suite is otherwise unrelated) asserting `.do/README.md`'s "Contents" table contains a row referencing `.github/README.md`, and that the file states automatic deploys now exist on merge alongside the still-valid manual procedure. |
| T033 | Implement .do/README.md cross-reference | implement | R001, R002, R003 | Add a "Contents" row to `.do/README.md` pointing to `.github/README.md`, and a short note that `dev`/`prod` now deploy automatically on merge, with `.do/deploy.sh`'s manual procedure remaining valid and equivalent for local/ad hoc use. |
| T034 | Test: .cloudflare/README.md cross-references the CI/CD runbook | test | R001, R002, R003 | Extend `.cloudflare/tests/readme.test.sh` asserting `.cloudflare/README.md`'s "Contents" table contains a row referencing `.github/README.md`. |
| T035 | Implement .cloudflare/README.md cross-reference | implement | R001, R002, R003 | Add the same "Contents" row to `.cloudflare/README.md` pointing to `.github/README.md`. |
