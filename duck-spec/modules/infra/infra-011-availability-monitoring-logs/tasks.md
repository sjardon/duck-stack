# INFRA-011 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: log_destinations block in app.yaml | test | R005, R006, R007, R008 | Write failing acceptance assertions in `.do/tests/app-spec.test.sh` (extend the PyYAML `STRUCT_OUTPUT` block): `services[0].log_destinations` contains exactly one entry with `name` set and `logtail.token == "${BETTERSTACK_LOGS_TOKEN}"`. |
| T002 | Implement log_destinations in app.yaml | implement | R005, R006, R007, R008 | In `.do/app.yaml`, add a `log_destinations` list under `services[0]` with one entry (`name: betterstack`, `logtail.token: ${BETTERSTACK_LOGS_TOKEN}`), preceded by a comment referencing R005/R006/R008, matching the existing header-comment style of the file. |
| T003 | Refactor app.yaml log_destinations comment | refactor | R005, R006 | Align the new block's comment wording/placement with the file's existing per-section comment convention (see the `envs` groups) so `.do/tests/app-spec.test.sh` and `.do/tests/env-example.test.sh` both keep passing. |
| T004 | Test: example values file documents new placeholders | test | R007, R008 | Write failing acceptance assertions in `.do/tests/env-example.test.sh`: extend the empty-value check list with `BETTERSTACK_LOGS_TOKEN`, `BETTERSTACK_API_TOKEN`, `BETTERSTACK_ALERT_EMAIL`, asserting each has an empty value in `.do/.env.deploy.example` (T023's existing generic `${VAR}` scan already covers `BETTERSTACK_LOGS_TOKEN` once it appears in `app.yaml`). |
| T005 | Implement new placeholders in the example values file | implement | R007, R008 | In `.do/.env.deploy.example`, add a "--- monitoring & log forwarding (INFRA-011) ---" section: `BETTERSTACK_LOGS_TOKEN=`, `BETTERSTACK_API_TOKEN=`, `BETTERSTACK_ALERT_EMAIL=` left empty; `BETTERSTACK_MONITOR_URL=`, `BETTERSTACK_CHECK_FREQUENCY_SECONDS=`, `BETTERSTACK_CHECK_TIMEOUT_SECONDS=` with safe example values and inline comments explaining each. |
| T006 | Refactor example file section grouping | refactor | R007, R008 | Reorder/comment the new section so it reads consistently with the existing `serverConfig`/`authConfig`/etc. groups (one comment header per group, values in the same order they appear in the corresponding spec), keeping `.do/tests/env-example.test.sh` green. |
| T007 | Test: monitor.json structural assertions | test | R001, R002, NF003 | Write failing acceptance assertions in `.do/monitoring/tests/monitor-spec.test.sh` (Python/json-based, mirroring `.do/tests/app-spec.test.sh`'s structure): `monitor_type == "status"`, `url == "${BETTERSTACK_MONITOR_URL}"`, `check_frequency == "${BETTERSTACK_CHECK_FREQUENCY_SECONDS}"` before rendering, `request_timeout == "${BETTERSTACK_CHECK_TIMEOUT_SECONDS}"`. |
| T008 | Implement monitor.json template | implement | R001, R002, R007, NF003 | Create `.do/monitoring/monitor.json` with `monitor_type: "status"`, `url`, `check_frequency`, `request_timeout` as `${VAR}` placeholders per the design. |
| T009 | Test: monitor.json notification content assertions | test | R003, R004 | Extend `.do/monitoring/tests/monitor-spec.test.sh` with assertions: `email == true`, `pronounceable_name` contains the literal substring `${DO_APP_NAME}`. |
| T010 | Implement monitor.json notification fields | implement | R003, R004 | In `.do/monitoring/monitor.json`, set `"email": true` and `"pronounceable_name": "duck-stack backend (${DO_APP_NAME})"`. |
| T011 | Refactor monitor.json header comment | refactor | R001, R002, R003, R004, R007 | Add a short header comment (as a sibling `.md`/inline JSON-adjacent note is not valid JSON, so document the placeholder contract in `.do/monitoring/README.md` instead — cross-reference it) ensuring the file stays parseable JSON with no comments, consistent with strict JSON constraints; verify no trailing placeholder is left undocumented. |
| T012 | Test: deploy.sh fails fast on missing placeholders | test | R007, R008 | Write failing acceptance assertions in `.do/monitoring/tests/deploy-script.test.sh`: running `.do/monitoring/deploy.sh` with a values file missing `BETTERSTACK_ALERT_EMAIL` exits non-zero, prints a message identifying the missing variable, and never invokes the stub `curl`. |
| T013 | Implement deploy.sh load_values and validate_placeholders | implement | R007, R008 | In `.do/monitoring/deploy.sh`, implement `load_values()` (source the values file with `set -a`/`set +a`) and `validate_placeholders()` (checks every `${VAR}` token in `monitor.json` plus `BETTERSTACK_API_TOKEN` and `BETTERSTACK_ALERT_EMAIL` are non-empty; lists every missing variable and exits non-zero before any network call). |
| T014 | Test: deploy.sh renders and idempotently upserts the monitor | test | R001, R007 | Write failing acceptance assertions in `.do/monitoring/tests/deploy-script.test.sh` using a stub `curl` on `PATH`: (a) when the stub's `GET .../monitors` response contains no matching `url`, `deploy.sh` issues a `POST .../monitors` with the rendered body; (b) when the stub's `GET` response contains a matching `url`, `deploy.sh` issues a `PATCH .../monitors/<id>` instead, never a second `POST`. |
| T015 | Implement deploy.sh render_monitor_spec, find_existing_monitor, upsert_monitor | implement | R001, R002, R003, R004, R007 | In `.do/monitoring/deploy.sh`, implement `render_monitor_spec()` (envsubst `monitor.json` to a cleaned-up temp file), `find_existing_monitor()` (GET the Better Stack Uptime monitors list, parse with `python3`'s stdlib `json`, match `attributes.url == $BETTERSTACK_MONITOR_URL`), and `upsert_monitor()` (PATCH if found else POST, non-zero exit and response body echoed on a non-2xx result). |
| T016 | Test: deploy.sh invites the configured recipient | test | R003, R004, R007 | Extend `.do/monitoring/tests/deploy-script.test.sh`: the stub `curl` log shows a `POST .../team-members` call with a JSON body containing `BETTERSTACK_ALERT_EMAIL`'s value; a stub response indicating the address is already a member still yields exit 0. |
| T017 | Implement deploy.sh invite_recipient and main orchestration | implement | R003, R004, EC005 | In `.do/monitoring/deploy.sh`, implement `invite_recipient()` (POST to the Better Stack team-members endpoint, tolerating an "already a member" response) and the `main` orchestration calling `load_values`, `validate_placeholders`, `render_monitor_spec`, `upsert_monitor`, `invite_recipient` in order, printing the resulting monitor id/URL and a reminder to perform the EC005 end-to-end delivery test. |
| T018 | Refactor deploy.sh shared API-call helper | refactor | R001, R003, R004, R007 | Extract a shared `api_request()` helper (method, URL, body) used by `find_existing_monitor`, `upsert_monitor`, and `invite_recipient` to remove duplicated `curl`/`Authorization` boilerplate, keeping `.do/monitoring/tests/deploy-script.test.sh` green. |
| T019 | Test: monitoring README documents prerequisites, procedure and edge cases | test | EC001, EC002, EC003, EC004, EC005, EC006, NF001, NF002, NF003 | Write failing acceptance assertions in `.do/monitoring/tests/readme.test.sh` (mirroring `.do/tests/readme.test.sh`): a "Prerequisites" heading exists; the procedure references `.do/monitoring/deploy.sh`; the file states the EC001 shallow-health-check blind spot, the EC002 undetected-outage-window formula, the EC003 interval/monthly-request-count/`/health`-exclusion query, the EC004 ingestion quota and `LOG_LEVEL` mitigation, the EC005 mandatory end-to-end delivery test, the EC006 default query restricting results to structured JSON lines, the NF001 one-time stream inspection step, and the NF002 platform-level isolation guarantee. |
| T020 | Implement monitoring README | implement | EC001, EC002, EC003, EC004, EC005, EC006, NF001, NF002, NF003 | Create `.do/monitoring/README.md` with Prerequisites, Setup procedure (extend the shared values file, run `.do/monitoring/deploy.sh`), and a documented-behaviors section covering EC001–EC006 and NF001–NF003 exactly as asserted by `.do/monitoring/tests/readme.test.sh`. |
| T021 | Refactor: cross-link .do/README.md to the monitoring runbook | refactor | R007, R008 | In `.do/README.md`, add a row to the "Contents" table pointing to `.do/monitoring/README.md`, keeping `.do/tests/readme.test.sh` green and giving the monitoring setup a discoverable entry point from the existing deploy runbook. |
