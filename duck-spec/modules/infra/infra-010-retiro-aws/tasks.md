# INFRA-010 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: Terraform tree is absent | test | R001, EC001 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/terraform-tree-removed.test.sh` asserting: WHEN the repository is inspected THEN `infra/terraform` does not exist (`[ ! -e infra/terraform ]`) AND `infra` itself contains no tracked file (`git ls-files infra/` is empty). Expected to FAIL until T002. |
| T002 | Delete the Terraform tree | implement | R001, R009, EC001 | Delete `infra/terraform/main.tf`, `variables.tf`, `outputs.tf`, `terraform.tfvars.example`, `bootstrap/main.tf`, `bootstrap/variables.tf`, `bootstrap/outputs.tf`, and `modules/{vpc,ecr,app_runner,static_site}/{main,variables,outputs}.tf` — no `terraform destroy`, no state migration. |
| T003 | Refactor: confirm no stray Terraform files remain | refactor | R001, EC001 | Run `git status --porcelain infra/` and `find infra -type f` (if `infra/` still exists) to confirm zero tracked or untracked leftovers; remove the now-empty `infra/` directory entry from any tooling config that assumed its presence (none expected). Re-run T001; it must PASS. |
| T004 | Test: no workflow authenticates against or deploys to AWS | test | R002, R003, R009 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/aws-workflows-removed.test.sh` asserting: WHEN `.github/workflows/` is inspected THEN none of `deploy.yml`, `deploy-manual.yml`, `rollback.yml`, `_deploy-services.yml`, `_deploy-web.yml`, `_deploy-landing.yml` exist, AND a recursive grep of `.github/workflows/` for `aws-actions|AWS_OIDC_ROLE_ARN|AWS_REGION|ECR_REPOSITORY|S3_BUCKET|CLOUDFRONT_DISTRIBUTION_ID` returns no match. Expected to FAIL until T005. |
| T005 | Delete the six AWS-authenticating workflows | implement | R002, R003, R009 | Delete `.github/workflows/deploy.yml`, `deploy-manual.yml`, `rollback.yml`, `_deploy-services.yml`, `_deploy-web.yml`, `_deploy-landing.yml`. |
| T006 | Refactor: confirm no reachable AWS deploy path remains | refactor | R002, R003, R009 | Run `find .github/workflows -type f` to confirm it is empty and `git ls-files .github/workflows/` returns nothing; re-run T004, it must PASS. |
| T007 | Test: retained SES-adjacent artifacts are unchanged | test | R010, EC004 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/retained-artifacts-unchanged.test.sh` asserting: `.do/app.yaml` still declares `SES_REGION` and `EMAIL_SENDER_ADDRESS`, `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts` still exists, and `apps/services/package.json` still lists `@aws-sdk/client-sesv2` as a dependency. Expected to PASS immediately (no production code changes in this feature) — run it before and after every other task to guard against accidental regressions. |
| T008 | Verify retained artifacts stay untouched | implement | R010, EC004 | Run T007 after T002/T005/T012/T015 (Terraform, workflow, and doc changes) to confirm none of them touched `apps/`, `packages/`, or `.do/app.yaml`; no code change expected — this task is a checkpoint, not an edit. |
| T009 | Test: INFRASTRUCTURE.md describes only the current topology | test | R004 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/infrastructure-doc.test.sh` asserting: WHEN `duck-spec/docs/INFRASTRUCTURE.md` is grepped for `Terraform|VPC|ECR|App Runner|CloudFront|DynamoDB|OIDC` THEN there are zero matches, AND the headings `DigitalOcean App Platform`, `Cloudflare Pages`, `Not managed here` are still present. Expected to FAIL until T010. |
| T010 | Rewrite INFRASTRUCTURE.md to drop the AWS sections | implement | R004 | In `duck-spec/docs/INFRASTRUCTURE.md`, delete the `## AWS resources`, `## Terraform`, and `## CI/CD` sections in full, and reword the opening line ("Living document describing AWS resources, Terraform setup, and CI/CD pipeline...") to describe the DigitalOcean App Platform + Cloudflare Pages topology instead. Keep `## DigitalOcean App Platform`, `## Cloudflare Pages`, `## Not managed here` unchanged. |
| T011 | Refactor: tidy INFRASTRUCTURE.md heading order and separators | refactor | R004 | Confirm remaining `---` separators and heading levels are consistent after the deletions (no orphaned separator before/after a removed section). Re-run T009; it must PASS. |
| T012 | Test: ARCHITECTURE.md names no AWS deploy target | test | R006 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/architecture-doc.test.sh` asserting: WHEN `duck-spec/docs/ARCHITECTURE.md` is grepped for `AWS App Runner|App Runner URL` THEN there are zero matches, AND the `apps/services` row of the `## Services` table mentions DigitalOcean App Platform. Expected to FAIL until T013. |
| T013 | Fix ARCHITECTURE.md's stale AWS references | implement | R006 | In `duck-spec/docs/ARCHITECTURE.md`: rewrite the `apps/services` row of the `## Services` table (currently "deployed to AWS App Runner via ECR") to name DigitalOcean App Platform (INFRA-008); in `## Inter-service communication`, drop the trailing clause "not the previously planned App Runner URL". Leave the `AWS SES v2` row in `## External integrations` unchanged. |
| T014 | Refactor: confirm ARCHITECTURE.md table formatting is intact | refactor | R006 | Verify the `## Services` and `## External integrations` Markdown tables still render correctly (column count and alignment preserved) after the edit. Re-run T012; it must PASS. |
| T015 | Test: SPEC.md files (global, infra, services) present no AWS design as built/planned/pending | test | R005, NF001, EC002 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/spec-docs.test.sh` asserting: `duck-spec/docs/SPEC.md`'s `infra` entry contains no "pending removal (INFRA-010)" sentence; `duck-spec/modules/infra/SPEC.md` has no `## AWS Base Infrastructure (INFRA-002)`, `## Static Hosting — S3 + CloudFront (INFRA-003)`, or `## CI/CD Pipeline — GitHub Actions (INFRA-004)` heading, and its INFRA-008/009 "What is out of scope here" paragraphs no longer reference INFRA-002/003/004 or a pending INFRA-010 removal; `duck-spec/modules/services/SPEC.md` contains no non-SES "App Runner" reference. Expected to FAIL until T016. |
| T016 | Realign SPEC.md files with the current topology | implement | R005, NF001, EC002 | In `duck-spec/docs/SPEC.md`, remove the "superseded and pending removal (INFRA-010)" sentence from the `infra` entry. In `duck-spec/modules/infra/SPEC.md`, delete the `## AWS Base Infrastructure (INFRA-002)`, `## Static Hosting — S3 + CloudFront (INFRA-003)`, and `## CI/CD Pipeline — GitHub Actions (INFRA-004)` sections (with all their subsections) in full, and edit the "What is out of scope here" paragraphs under INFRA-008 and INFRA-009 to drop the stale forward-references to INFRA-002/004 and to a pending INFRA-010. In `duck-spec/modules/services/SPEC.md`, reword the `### Health module` sentence ("...satisfies the App Runner health-check path.") and the `### Container image` sentence ("structured for deployment on AWS App Runner...") to remove the AWS App Runner reference, citing DigitalOcean App Platform (INFRA-008) where a platform name is needed. |
| T017 | Refactor: verify no dangling INFRA-002/003/004 cross-references remain in SPEC.md files | refactor | R005, NF001, EC002 | Grep the three edited SPEC.md files for `INFRA-002`, `INFRA-003`, `INFRA-004` to confirm no leftover cross-reference points at a now-deleted section. Re-run T015; it must PASS. |
| T018 | Test: six AWS feature entries are DEPRECATED with required notes | test | R007, R008, NF002, EC002, EC003 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/features-deprecation.test.sh` asserting: WHEN `duck-spec/modules/infra/FEATURES.md` is inspected THEN the `## INFRA-002`, `## INFRA-003`, `## INFRA-004`, `## INFRA-005`, `## INFRA-006`, `## INFRA-007` entries each have `**Estado:** DEPRECATED`; INFRA-002 and INFRA-003's bodies contain a "never applied"/"never operated" statement; INFRA-005's body names `NOTIFICATIONS-002`; INFRA-006 and INFRA-007's bodies name `INFRA-011`; and every original section heading of each entry (`### Contexto`, `### Objetivo`, etc.) is still present (content not deleted). Expected to FAIL until T019. |
| T019 | Deprecate the six AWS feature entries in FEATURES.md | implement | R007, R008, NF002, EC002, EC003 | In `duck-spec/modules/infra/FEATURES.md`, change `**Estado:**` to `DEPRECATED` for INFRA-002, INFRA-003, INFRA-004, INFRA-005, INFRA-006, INFRA-007, and append a deprecation note to each entry's body: INFRA-002/003 state the design was defined but never applied and never operated (superseded by INFRA-008/INFRA-009 respectively); INFRA-004 states deploy automation is withdrawn with no automatic replacement yet (INFRA-012, forward-linked); INFRA-005 names NOTIFICATIONS-002 as the feature now carrying email delivery; INFRA-006 and INFRA-007 name INFRA-011 as the feature now carrying delivery observability and alerting. Do not delete or shorten any entry's existing content. |
| T020 | Refactor: verify FEATURES.md entries remain reconstructable from the registry alone | refactor | R007, R008, NF002 | Read each of the six updated entries end-to-end and confirm a reader with no git history access can determine why the piece was abandoned and, where applicable, what replaces it, purely from the entry body. Re-run T018; it must PASS. |
| T021 | Test: repository-wide NF001 full-text sweep | test | NF001 | Write `duck-spec/modules/infra/infra-010-retiro-aws/tests/nf001-fulltext-sweep.test.sh` asserting: WHEN `duck-spec/docs/*.md`, `duck-spec/modules/*/SPEC.md`, and `.github/workflows/` are grepped (recursively) for `Terraform|VPC|ECR|App Runner|CloudFront|DynamoDB|AWS OIDC|S3 bucket` THEN there are zero matches outside lines explicitly flagged as SES-related/pending NOTIFICATIONS-002 migration. Expected to FAIL until every prior implement task (T002, T005, T010, T013, T016, T019) lands, then PASS. |
| T022 | Final sweep: run the full acceptance suite | refactor | R001, R002, R003, R004, R005, R006, R007, R008, R009, R010, NF001, NF002 | Run all eight test scripts under `duck-spec/modules/infra/infra-010-retiro-aws/tests/` in sequence and confirm every one exits 0; fix any residual reference the sweep surfaces before closing out the feature. |
