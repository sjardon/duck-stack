# INFRA-009 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: deploy.sh validates app selector and values-file arg | test | R001 | In `.cloudflare/tests/deploy-script.test.sh` assert `.cloudflare/deploy.sh` exits non-zero with a usage message when called with zero args, an app arg other than `web`/`landing`, or a values-file path that does not exist. |
| T002 | Implement deploy.sh skeleton | implement | R001 | Create `.cloudflare/deploy.sh`: `usage()` helper, validate `$1` is `web` or `landing`, validate `$2` is an existing file, else exit 1 with the usage message. |
| T003 | Test: build runs from the repo root through the pnpm workspace | test | R003, EC001, NF001 | In `.cloudflare/tests/deploy-script.test.sh`, using a minimal stub values file, run `.cloudflare/deploy.sh web <values-file>` (with `wrangler` stubbed on `PATH`) and assert it exits 0 and `apps/web/dist/index.html` exists afterward — proving the build resolved `workspace:*` packages from the monorepo root. |
| T004 | Implement build step | implement | R003, EC001, NF001 | In `.cloudflare/deploy.sh`, after arg validation, `cd` to the repo root and run `pnpm install --frozen-lockfile && pnpm --filter "$APP" build`, failing the script if either command fails. |
| T005 | Test: build-time VITE_ variables are sourced and inlined | test | R006, R007 | In `.cloudflare/tests/deploy-script.test.sh`, run `deploy.sh web <values-file>` with a values file setting `VITE_API_URL=https://api.example.test` (plus the other required `web` keys), then grep the built `apps/web/dist/assets/*.js` for the literal string `https://api.example.test`. |
| T006 | Implement values-file sourcing | implement | R006, R007 | In `.cloudflare/deploy.sh`, before the build step, `set -a; source "$VALUES_FILE"; set +a` so every variable in the values file (including `VITE_*` keys) is exported into the build's environment. |
| T007 | Test: required variables are validated before any build or deploy | test | R006, NF001 | In `.cloudflare/tests/deploy-script.test.sh`, assert that running `deploy.sh web <values-file>` with a values file missing `VITE_CLERK_PUBLISHABLE_KEY` exits non-zero identifying the missing variable, without invoking `pnpm build` or the stub `wrangler`. |
| T008 | Implement required-variable validation | implement | R006 | In `.cloudflare/deploy.sh`, after sourcing the values file and before building, validate `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `GIT_BRANCH`, and the app-specific `VITE_*` keys (`web`: `VITE_API_URL`, `VITE_CLERK_PUBLISHABLE_KEY`, `VITE_LANDING_URL`, `VITE_PROVIDER_PORTAL_URL`; `landing`: `VITE_API_URL`, `VITE_WEB_URL`) are non-empty using `${VAR:?missing $VAR — see .cloudflare/README.md}`; default `CLOUDFLARE_PROJECT_NAME` to `duck-stack-$APP` and `PRODUCTION_BRANCH` to `main` when unset. |
| T009 | Test: SPA fallback file is written into the build output | test | R005, EC003 | In `.cloudflare/tests/deploy-script.test.sh`, after running `deploy.sh web <values-file>`, assert `apps/web/dist/_redirects` exists and its content is exactly `/* /index.html 200`. |
| T010 | Implement SPA fallback generation | implement | R005, EC003 | In `.cloudflare/deploy.sh`, after the build succeeds, write `/* /index.html 200` to `apps/$APP/dist/_redirects`. |
| T011 | Test: exactly the app's dist directory is deployed | test | R004, R001 | In `.cloudflare/tests/deploy-script.test.sh`, using a stub `wrangler` on `PATH` that records its argv, run `deploy.sh web <values-file>` and assert the recorded `wrangler pages deploy` invocation's directory argument is exactly `apps/web/dist` and `--project-name` equals `duck-stack-web`. |
| T012 | Implement deploy invocation | implement | R004, R001 | In `.cloudflare/deploy.sh`, run `pnpm exec wrangler pages deploy "apps/$APP/dist" --project-name "$CLOUDFLARE_PROJECT_NAME" --branch "$GIT_BRANCH"`, capturing stdout for later parsing. |
| T013 | Test: project creation is idempotent | test | R002 | In `.cloudflare/tests/deploy-script.test.sh`, configure the stub `wrangler pages project create` to exit non-zero with an "already exists" message on its second invocation across two consecutive `deploy.sh` runs for the same app, and assert the second run still completes successfully (exit 0). |
| T014 | Implement idempotent project creation | implement | R002 | In `.cloudflare/deploy.sh`, before the deploy step, run `pnpm exec wrangler pages project create "$CLOUDFLARE_PROJECT_NAME" --production-branch "$PRODUCTION_BRANCH"`, ignoring a non-zero exit (`|| true`) so a pre-existing project does not fail the run. |
| T015 | Test: resulting public URL is printed | test | R008, R002 | In `.cloudflare/tests/deploy-script.test.sh`, configure the stub `wrangler pages deploy` to print a fixed `https://abc123.duck-stack-web.pages.dev` line to stdout, and assert `deploy.sh`'s own stdout contains that URL and a reminder to record it in `.cloudflare/README.md`. |
| T016 | Implement public URL output | implement | R008, R002 | In `.cloudflare/deploy.sh`, parse the `https://*.pages.dev` URL from the captured `wrangler pages deploy` stdout and print `Public URL: <url>` plus a reminder to record it under "Current deployments" in `.cloudflare/README.md`. |
| T017 | Refactor deploy.sh | refactor | R001, R002, R003, R004, R005, R006, R007, R008 | In `.cloudflare/deploy.sh`, extract `validate_args`, `build_app`, `write_redirects`, `ensure_project`, and `deploy_and_print_url` as named functions, quote every variable expansion, and consolidate the `usage()` block. |
| T018 | Test: web example file declares exactly its required keys, no secrets | test | R006, R007, EC002 | In `.cloudflare/tests/env-example.test.sh` assert `.cloudflare/.env.deploy.web.example` declares `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `GIT_BRANCH`, `VITE_API_URL`, `VITE_CLERK_PUBLISHABLE_KEY`, `VITE_LANDING_URL`, `VITE_PROVIDER_PORTAL_URL`, and that none of `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SIGNING_SECRET`, `MOBBEX_API_KEY`, `MOBBEX_ACCESS_TOKEN`, `MOBBEX_WEBHOOK_SECRET`, `DATABASE_URL` appear anywhere in the file. |
| T019 | Implement web example file | implement | R006, R007, EC002 | Create `.cloudflare/.env.deploy.web.example` with commented, safe example values for every key from T018, `CLOUDFLARE_API_TOKEN` left blank and labeled as a deploy credential, and commented-out `CLOUDFLARE_PROJECT_NAME`/`PRODUCTION_BRANCH` defaults. |
| T020 | Test: landing example file declares exactly its required keys, no secrets | test | R006, R007, EC002 | In `.cloudflare/tests/env-example.test.sh` assert `.cloudflare/.env.deploy.landing.example` declares `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `GIT_BRANCH`, `VITE_API_URL`, `VITE_WEB_URL`, and that the same secret-classified keys from T018 do not appear anywhere in the file. |
| T021 | Implement landing example file | implement | R006, R007, EC002 | Create `.cloudflare/.env.deploy.landing.example` with commented, safe example values for every key from T020, `CLOUDFLARE_API_TOKEN` left blank and labeled as a deploy credential, and commented-out `CLOUDFLARE_PROJECT_NAME`/`PRODUCTION_BRANCH` defaults. |
| T022 | Test: per-environment values files are git-ignored | test | NF002, NF003 | In `.cloudflare/tests/gitignore.test.sh` assert `git check-ignore .cloudflare/.env.deploy.web.dev` exits 0, `git check-ignore .cloudflare/.env.deploy.web.example` exits 1, and `git check-ignore .cloudflare/.env.deploy.landing.example` exits 1. |
| T023 | Implement gitignore rule | implement | NF002, NF003 | In `.gitignore` add `.cloudflare/.env.deploy.*` followed by `!.cloudflare/.env.deploy.*.example`. |
| T024 | Test: CORS_ORIGIN with a comma splits into a trimmed array; a single value is unchanged | test | EC004 | In `apps/services/tests/unit/shared/configs/serverConfig.test.ts` add a case: `process.env.CORS_ORIGIN = ' https://web.example.com , https://landing.example.com '` yields `serverConfig.corsOrigin` equal to `['https://web.example.com', 'https://landing.example.com']`; keep the existing single-value case (`'https://example.com'` stays that exact string) passing unmodified. |
| T025 | Implement multi-origin CORS_ORIGIN parsing | implement | EC004 | In `apps/services/src/shared/configs/serverConfig.ts`, change `corsOrigin` to: read `CORS_ORIGIN` once, and if the raw value includes a comma, split on `,`, trim each element, filter empty strings, and assign the resulting array; otherwise keep the raw string (including the `'*'` default) unchanged. |
| T026 | Test: README documents the full procedure and every edge-case note | test | R008, EC002, EC004, EC005, NF002, NF003 | In `.cloudflare/tests/readme.test.sh` assert `.cloudflare/README.md` contains a "Prerequisites" section naming `wrangler`/`pnpm exec wrangler`, a step-by-step procedure invoking `.cloudflare/deploy.sh`, the exact EC002 secret-restriction sentence, a documented `CORS_ORIGIN` comma-separated multi-origin format example, the EC005 statement that each environment's `CORS_ORIGIN` must list only that environment's own SPA URLs, and a "Current deployments" table with `App`, `Environment`, `Public URL`, and `Last updated` columns. |
| T027 | Implement deploy runbook | implement | R008, EC002, EC004, EC005, NF002, NF003 | Create `.cloudflare/README.md` with: prerequisites (`wrangler` via `pnpm exec wrangler`, a Cloudflare account/API token with Pages permissions); the procedure to copy an app's `*.example` file to `.cloudflare/.env.deploy.<app>.<environment>`, fill it in, and run `.cloudflare/deploy.sh <app> <values-file>`; the EC002 restriction that only public-classified values belong in these files; the EC004 `CORS_ORIGIN` format (`https://<url1>,https://<url2>`, no trailing slash); the EC005 per-environment note; and an empty "Current deployments" table (`App | Environment | Public URL | Last updated`). |
| T028 | Test: .do CORS_ORIGIN placeholder documents the multi-origin format | test | EC004, EC005 | In `.cloudflare/tests/readme.test.sh` (or a dedicated assertion in the same file) assert the `CORS_ORIGIN` entry's comment in `.do/.env.deploy.example` mentions a comma-separated list of origins. |
| T029 | Implement .do CORS_ORIGIN documentation update | implement | EC004, EC005 | In `.do/.env.deploy.example`, update the comment above the `CORS_ORIGIN` line to state it accepts a comma-separated list of full origins (e.g. `https://duck-stack-web.pages.dev,https://duck-stack-landing.pages.dev`), with no trailing slash. |
| T030 | Add wrangler as a pinned root devDependency | implement | R003, NF001 | In root `package.json`, add `wrangler` under `devDependencies` at a pinned version, so `.cloudflare/deploy.sh`'s `pnpm exec wrangler` calls resolve from the workspace lockfile rather than an ad hoc network fetch. |
| T031 | Refactor documentation for consistency | refactor | R007, R008, EC004, EC005, NF002 | Cross-check `.cloudflare/README.md`, `.cloudflare/.env.deploy.web.example`, `.cloudflare/.env.deploy.landing.example`, and `.do/.env.deploy.example` use identical variable names, `CORS_ORIGIN` format wording, and section headings; fix any gap so no step in the procedure relies on unstated prior knowledge. |
