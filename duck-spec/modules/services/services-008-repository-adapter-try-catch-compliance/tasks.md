# SERVICES-008 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: UserDBRepository SQL error paths | test | R001, R002, R003, R007, NF001, NF002, NF003 | In `tests/unit/modules/users/repositories/userDBRepository.test.ts` write failing tests asserting: WHEN `findByClerkUserId`, `updatePreferences`, or `completeOnboarding` is called and the sql mock rejects with a raw `Error` THEN `logger.error` is called with `repository: 'UserDBRepository'` and the method name AND the method re-throws a `ProviderError` with `statusCode 502` and `originalError` equal to the raw error; AND WHEN `updatePreferences` or `completeOnboarding` throws a `NotFoundError` (domain error), the same error propagates without re-wrapping |
| T002 | Implement: UserDBRepository try/catch | implement | R001, R002, R003, R007, NF001, NF002, NF003, NF004 | In `apps/services/src/modules/users/repositories/userDBRepository.ts` add imports for `DomainError` and `ProviderError` from `shared/errors.ts`; wrap each of `findByClerkUserId`, `updatePreferences`, and `completeOnboarding` in a method-level `try/catch` that re-throws `DomainError` instances unchanged and otherwise calls `logger.error` with `{ err, repository: 'UserDBRepository', method, clerkUserId }` then throws `new ProviderError('Database error in UserDBRepository.<method>', 502, err)`; the existing SQL text, return values, and latency logging inside the `try` block are unchanged |
| T003 | Test: SubscriptionDBRepository SQL error paths | test | R001, R002, R007, NF001, NF002, NF003 | In `tests/unit/modules/subscriptions/subscriptionDBRepository.test.ts` add test cases asserting: WHEN each of `findActiveByScopeStatus`, `findByIdAndScope`, `findPlanByCode`, `create`, `setCancelAtPeriodEnd`, and `cancelImmediately` receives a sql mock that rejects THEN `logger.error` is called with `repository: 'SubscriptionDBRepository'` and the method name AND a `ProviderError` with `statusCode 502` and `originalError` populated is thrown |
| T004 | Implement: SubscriptionDBRepository try/catch | implement | R001, R002, R007, NF001, NF002, NF003, NF004 | In `apps/services/src/modules/subscriptions/repositories/subscriptionDBRepository.ts` add imports for `DomainError` and `ProviderError`; wrap each method body (`findActiveByScopeStatus`, `findByIdAndScope`, `findPlanByCode`, `create`, `setCancelAtPeriodEnd`, `cancelImmediately`) in a `try/catch` that re-throws `DomainError` instances and otherwise calls `logger.error` with `{ err, repository: 'SubscriptionDBRepository', method }` and throws `new ProviderError('Database error in SubscriptionDBRepository.<method>', 502, err)`; the existing SQL text, return values, and latency logging inside the `try` block are unchanged |
| T005 | Test: SubscriptionPlanDBRepository SQL error path | test | R001, R002, R007, NF001, NF002, NF003 | In `tests/unit/modules/subscriptions/subscriptionPlanDBRepository.test.ts` add a test case asserting: WHEN `listActive` is called and the sql mock rejects THEN `logger.error` is called with `repository: 'SubscriptionPlanDBRepository'` and method `'listActive'` AND a `ProviderError` with `statusCode 502` and `originalError` is thrown |
| T006 | Implement: SubscriptionPlanDBRepository try/catch | implement | R001, R002, R007, NF001, NF002, NF003, NF004 | In `apps/services/src/modules/subscriptions/repositories/subscriptionPlanDBRepository.ts` add imports for `DomainError` and `ProviderError`; wrap the `listActive` method body in a `try/catch` that re-throws `DomainError` instances and otherwise calls `logger.error` with `{ err, repository: 'SubscriptionPlanDBRepository', method: 'listActive' }` and throws `new ProviderError('Database error in SubscriptionPlanDBRepository.listActive', 502, err)`; the existing SQL text, return values, and latency logging inside the `try` block are unchanged |
| T007 | Test: TransactionDBRepository SQL error paths | test | R001, R002, R007, NF001, NF002, NF003 | In `tests/unit/billing/transactionDBRepository.test.ts` add test cases asserting: WHEN each of `create`, `findById`, `findByIdempotencyKey`, `updateFailureReason`, `updateProviderData`, `list`, and `getRefundsByTransactionId` receives a rejecting sql mock THEN `logger.error` is called with `repository: 'TransactionDBRepository'` and the method name AND a `ProviderError` with `statusCode 502` and `originalError` is thrown |
| T008 | Implement: TransactionDBRepository try/catch | implement | R001, R002, R007, NF001, NF002, NF003, NF004 | In `apps/services/src/modules/billing/repositories/transactionDBRepository.ts` add imports for `DomainError` and `ProviderError`; wrap each method body (`create`, `findById`, `findByIdempotencyKey`, `updateFailureReason`, `updateProviderData`, `list`, `getRefundsByTransactionId`) in a `try/catch` that re-throws `DomainError` instances and otherwise calls `logger.error` with `{ err, repository: 'TransactionDBRepository', method }` and throws `new ProviderError('Database error in TransactionDBRepository.<method>', 502, err)`; the existing SQL text, return values, cursor logic, and latency logging inside the `try` block are unchanged |
| T009 | Test: ClerkSyncRepository SQL error paths | test | R001, R002, R007, NF001, NF002, NF003 | In `tests/unit/modules/webhooks/repositories/clerkSyncRepository.test.ts` write failing tests asserting: WHEN `upsertUser`, `upsertOrganization`, or `createMembership` is called and the sql mock rejects THEN `logger.error` is called with `repository: 'ClerkSyncRepository'` and the method name AND a `ProviderError` with `statusCode 502` and `originalError` is thrown; WHEN `createMembership` finds no user or no org the existing `warn`-and-return-early sentinel path is preserved without entering the error catch |
| T010 | Implement: ClerkSyncRepository try/catch | implement | R001, R002, R003, R007, NF001, NF002, NF003, NF004 | In `apps/services/src/modules/webhooks/repositories/clerkSyncRepository.ts` add imports for `DomainError` and `ProviderError`; wrap each method body (`upsertUser`, `upsertOrganization`, `createMembership`) in a `try/catch` that re-throws `DomainError` instances and otherwise calls `logger.error` with `{ err, repository: 'ClerkSyncRepository', method }` and throws `new ProviderError('Database error in ClerkSyncRepository.<method>', 502, err)`; the existing SQL text, sentinel return paths, and latency logging inside the `try` block are unchanged |
| T011 | Test: MobbexBillingSyncRepository SQL error paths (recordEvent + transactional sub-queries) | test | R001, R002, R004, R007, NF001, NF002, NF003, EC001, EC002 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add test cases asserting: (a) WHEN `recordEvent` sql mock rejects THEN `logger.error` is called with `repository: 'MobbexBillingSyncRepository'` and `method: 'recordEvent'` AND `ProviderError(502)` with `originalError` is thrown; (b) WHEN the SELECT-by-provider_transaction_id sub-query inside `updateTransactionStatus` rejects THEN `logger.error` is called once (not twice) AND `ProviderError(502)` is thrown; (c) WHEN the UPDATE sub-query inside `updateTransactionStatus` rejects THEN `logger.error` is called once AND `ProviderError(502)` is thrown; (d) WHEN the SELECT-transaction sub-query inside `upsertRefundAndMaybeMarkTransactionRefunded` rejects THEN `logger.error` is called once AND `ProviderError(502)` is thrown; (e) WHEN the reference fallback SELECT inside `updateTransactionStatus` rejects THEN `logger.error` includes `reference` in the payload |
| T012 | Implement: MobbexBillingSyncRepository try/catch (recordEvent + outer + inner for transactions) | implement | R001, R002, R004, R007, NF001, NF002, NF003, NF004, EC001, EC002 | In `apps/services/src/modules/webhooks/repositories/mobbexBillingSyncRepository.ts` add imports for `DomainError` and `ProviderError`; (a) wrap `recordEvent` method body in a `try/catch`; (b) for `updateTransactionStatus` and `upsertRefundAndMaybeMarkTransactionRefunded`, wrap the entire `sql.begin(...)` call in an outer `try/catch` that re-throws `DomainError` and otherwise logs and wraps, AND wrap each individual sub-query step inside the transaction callback in an inner `try/catch` that logs with `{ err, repository, method, step }` and `{ reference }` where applicable (EC002) and re-throws `new ProviderError(..., 502, err)`; outer catch uses `if (err instanceof DomainError) throw err` to avoid double-logging (EC001); the existing SQL text, happy-path return values, and idempotency logic inside the `try` blocks are unchanged |
| T013 | Test: MobbexProvider fetchWithTimeout logs originalError on failure | test | R005, R006, R007, NF001, NF002, EC003 | In `tests/unit/billing/mobbexProvider.test.ts` add test cases asserting: WHEN `createCheckout` or `queryTransaction` causes a network error THEN the thrown `ProviderError` has `originalError` set to the original `Error` instance; WHEN a timeout occurs THEN the `ProviderError` has `originalError` set to the `AbortError`; WHEN a 401 response is received THEN `ProviderError(502)` has `originalError` set to the original `ProviderError` thrown by `handleErrorResponse` |
| T014 | Implement: MobbexProvider fetchWithTimeout log augmentation | implement | R005, R007, NF001, NF002, NF004, EC003 | In `apps/services/src/modules/billing/providers/mobbexProvider.ts` augment the `fetchWithTimeout` `catch` block: add `logger.error({ err }, 'MobbexProvider.fetchWithTimeout timed out')` before the `AbortError` re-throw and `logger.error({ err }, 'MobbexProvider.fetchWithTimeout network error')` before the generic network re-throw; attach `err` as the third argument to each `new ProviderError(...)` call so `originalError` is populated; the existing timeout and network error mapping logic is unchanged |
| T015 | Test: MobbexProvider handleErrorResponse warn on JSON parse failure | test | R008, NF001, EC004 | In `tests/unit/billing/mobbexProvider.test.ts` add a test case asserting: WHEN `createCheckout` is called and Mobbex returns a non-OK response whose body is not valid JSON THEN `logger.warn` is called with a message referencing the discarded body AND the method still throws a `ProviderError` with the correct `statusCode` |
| T016 | Implement: MobbexProvider handleErrorResponse warn + justifying comment | implement | R006, R008, NF001, NF004, EC004 | In `apps/services/src/modules/billing/providers/mobbexProvider.ts` augment `handleErrorResponse`: in the JSON-parse `catch` block add `logger.warn({ err: parseErr, status: response.status }, 'MobbexProvider.handleErrorResponse: failed to parse error body; using HTTP status fallback')` and a code comment explaining the intentional silent-fail; import `logger` from `shared/infrastructure/logger.ts`; the existing status-to-statusCode mapping logic (401/5xx → 502, other 4xx → 400) is unchanged |
