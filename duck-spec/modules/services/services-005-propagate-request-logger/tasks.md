# SERVICES-005 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: TransactionDBRepository methods accept logger and emit through it | test | R003, NF001, EC003 | In `tests/unit/billing/transactionDBRepository.test.ts` add a `makeLogger()` helper that returns a `jest.fn()`-backed `pino.BaseLogger` stub; update all existing calls to `repo.create`, `repo.findById`, `repo.findByIdempotencyKey`, `repo.updateFailureReason`, `repo.updateProviderData`, `repo.list`, and `repo.getRefundsByTransactionId` to pass the fake logger as the last argument; assert `logger.info` is called at least once per method invocation |
| T002 | Implement: add logger param to ITransactionRepository methods | implement | R003, NF001 | In `src/modules/billing/repositories/interfaces/iTransactionRepository.ts` add `logger: BaseLogger` as the last parameter on `create`, `findById`, `findByIdempotencyKey`, `updateFailureReason`, `updateProviderData`, `list`, and `getRefundsByTransactionId`; import `BaseLogger` from `pino` |
| T003 | Implement: replace static logger in TransactionDBRepository | implement | R003, R007, R008, NF001, EC001, EC004 | In `src/modules/billing/repositories/transactionDBRepository.ts` remove the `import { logger }` statement; add `logger: BaseLogger` as the last parameter on every method; replace all `logger.*` call sites with the parameter; import `BaseLogger` from `pino`; do not alter any log message text, level, or structured field name |
| T004 | Test: CheckoutUseCase.execute accepts and forwards logger | test | R001, R002, EC003 | In `tests/unit/billing/checkoutUseCase.test.ts` add a fake `BaseLogger` and pass it as the final argument to every `useCase.execute` call; assert that the fake logger is forwarded (verify the repo mock receives a logger-shaped object, or assert `logger.info` was called through a spy in a dedicated test case) |
| T005 | Implement: add logger param to CheckoutUseCase.execute | implement | R001, R002, NF001 | In `src/modules/billing/useCases/checkoutUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.findByIdempotencyKey`, `repo.create`, `repo.updateProviderData`, and `repo.updateFailureReason`; import `BaseLogger` from `pino` |
| T006 | Test: GetTransactionUseCase.execute accepts and forwards logger | test | R001, R002, EC003 | In `tests/unit/billing/getTransactionUseCase.test.ts` add a fake `BaseLogger` and pass it as the final argument to every `useCase.execute` call |
| T007 | Implement: add logger param to GetTransactionUseCase.execute | implement | R001, R002, NF001 | In `src/modules/billing/useCases/getTransactionUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.findById`; import `BaseLogger` from `pino` |
| T008 | Test: ListTransactionsUseCase.execute accepts and forwards logger | test | R001, R002, EC003 | In `tests/unit/billing/listTransactionsUseCase.test.ts` add a fake `BaseLogger` and pass it as the final argument to every `useCase.execute` call |
| T009 | Implement: add logger param to ListTransactionsUseCase.execute | implement | R001, R002, NF001 | In `src/modules/billing/useCases/listTransactionsUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.list`; import `BaseLogger` from `pino` |
| T010 | Test: GetRefundsUseCase.execute accepts and forwards logger | test | R001, R002, EC003 | In `tests/unit/billing/getRefundsUseCase.test.ts` add a fake `BaseLogger` and pass it as the final argument to every `useCase.execute` call |
| T011 | Implement: add logger param to GetRefundsUseCase.execute | implement | R001, R002, NF001 | In `src/modules/billing/useCases/getRefundsUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.findById` and `repo.getRefundsByTransactionId`; import `BaseLogger` from `pino` |
| T012 | Implement: wire request.log in billing handlers | implement | R001, R006, NF002 | In `src/modules/billing/handlers/checkoutHandler.ts`, `getTransactionHandler.ts`, `listTransactionsHandler.ts`, and `getRefundsHandler.ts` pass `request.log` as the final argument to the corresponding `useCase.execute` call in each file |
| T013 | Test: UserDBRepository methods accept logger and emit through it | test | R003, NF001, EC003 | In a new test file `tests/unit/users/userDBRepository.test.ts` add a `makeLogger()` helper; instantiate `UserDBRepository` with a sql mock; call `findByClerkUserId`, `updatePreferences`, and `completeOnboarding` passing the fake logger; assert `logger.info` is called at least once per method |
| T014 | Implement: add logger param to IUserRepository methods | implement | R003, NF001 | In `src/modules/users/repositories/interfaces/iUserRepository.ts` add `logger: BaseLogger` as the last parameter on `findByClerkUserId`, `updatePreferences`, and `completeOnboarding`; import `BaseLogger` from `pino` |
| T015 | Implement: replace static logger in UserDBRepository | implement | R003, R007, R008, NF001, EC001, EC004 | In `src/modules/users/repositories/userDBRepository.ts` remove the `import { logger }` statement; add `logger: BaseLogger` as the last parameter on `findByClerkUserId`, `updatePreferences`, and `completeOnboarding`; replace all `logger.*` call sites with the parameter; import `BaseLogger` from `pino`; do not alter any log message text, level, or structured field name |
| T016 | Test: user use cases accept and forward logger | test | R001, R002, EC003 | In `tests/unit/users/completeOnboarding.test.ts` and `tests/unit/users/getUserProfile.test.ts` add a fake `BaseLogger` and pass it as the final argument to every `useCase.execute` call; add equivalent fake-logger argument to any `UpdateUserProfileUseCase` tests that exist or create them inline |
| T017 | Implement: add logger param to CompleteOnboardingUseCase.execute | implement | R001, R002, NF001 | In `src/modules/users/useCases/completeOnboardingUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.completeOnboarding`; import `BaseLogger` from `pino` |
| T018 | Implement: add logger param to GetUserProfileUseCase.execute | implement | R001, R002, NF001 | In `src/modules/users/useCases/getUserProfileUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.findByClerkUserId`; import `BaseLogger` from `pino` |
| T019 | Implement: add logger param to UpdateUserProfileUseCase.execute | implement | R001, R002, NF001 | In `src/modules/users/useCases/updateUserProfileUseCase.ts` add `logger: BaseLogger` as the last parameter of `execute`; forward it to `repo.findByClerkUserId` and `repo.updatePreferences`; import `BaseLogger` from `pino` |
| T020 | Implement: wire request.log in user handlers | implement | R001, R006, NF002 | In `src/modules/users/handlers/completeOnboardingHandler.ts`, `getUserProfileHandler.ts`, and `updateUserProfileHandler.ts` pass `request.log` as the final argument to the corresponding `useCase.execute` call in each file |
| T021 | Test: SubscriptionPlanDBRepository.listActive accepts logger | test | R003, NF001, EC003 | In `tests/unit/modules/subscriptions/subscriptionPlanDBRepository.test.ts` add a fake `BaseLogger` and pass it as the argument to `repo.listActive`; assert `logger.info` was called |
| T022 | Implement: add logger param to ISubscriptionPlanRepository.listActive | implement | R003, NF001 | In `src/modules/subscriptions/repositories/interfaces/iSubscriptionPlanRepository.ts` add `logger: BaseLogger` as the parameter on `listActive`; import `BaseLogger` from `pino` |
| T023 | Implement: replace static logger in SubscriptionPlanDBRepository | implement | R003, R007, R008, NF001, EC001, EC004 | In `src/modules/subscriptions/repositories/subscriptionPlanDBRepository.ts` remove the `import { logger }` statement; add `logger: BaseLogger` as the parameter on `listActive`; replace the `logger.info` call with the parameter; import `BaseLogger` from `pino`; do not alter any log message text, level, or structured field name |
| T024 | Test: ListPlansUseCase.execute accepts and forwards logger | test | R001, R002, EC003 | In `tests/unit/modules/subscriptions/listPlansUseCase.test.ts` add a fake `BaseLogger` and pass it as the argument to `useCase.execute` |
| T025 | Implement: add logger param to ListPlansUseCase.execute | implement | R001, R002, NF001 | In `src/modules/subscriptions/useCases/listPlansUseCase.ts` add `logger: BaseLogger` as the parameter of `execute`; forward it to `repo.listActive`; import `BaseLogger` from `pino` |
| T026 | Implement: wire request.log in listPlansHandler | implement | R001, R006, NF002 | In `src/modules/subscriptions/handlers/listPlansHandler.ts` change `useCase.execute()` to `useCase.execute(request.log)` and rename the `_request` parameter to `request` |
| T027 | Test: MobbexBillingSyncRepository methods accept logger | test | R003, R005, NF001, EC003 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add a `makeLogger()` helper; pass the fake logger as the last argument to `repo.recordEvent`, `repo.updateTransactionStatus`, and `repo.upsertRefundAndMaybeMarkTransactionRefunded` in all existing test cases; assert `logger.info` or `logger.warn` is called at least once per relevant test |
| T028 | Implement: add logger param to IMobbexBillingSyncRepository methods | implement | R003, R005, NF001 | In `src/modules/webhooks/repositories/interfaces/iMobbexBillingSyncRepository.ts` add `logger: BaseLogger` as the last parameter on `recordEvent`, `updateTransactionStatus`, and `upsertRefundAndMaybeMarkTransactionRefunded`; import `BaseLogger` from `pino` |
| T029 | Implement: replace static logger in MobbexBillingSyncRepository | implement | R003, R005, R007, R008, NF001, EC001, EC004 | In `src/modules/webhooks/repositories/mobbexBillingSyncRepository.ts` remove the `import { logger }` statement; add `logger: BaseLogger` as the last parameter on `recordEvent`, `updateTransactionStatus`, and `upsertRefundAndMaybeMarkTransactionRefunded`; replace every `logger.*` call site with the parameter; import `BaseLogger` from `pino`; do not alter any log message text, level, or structured field name |
| T030 | Test: ClerkSyncRepository methods accept logger | test | R003, R004, NF001, EC003 | In a new test file `tests/unit/modules/webhooks/repositories/clerkSyncRepository.test.ts` add a `makeLogger()` helper and a sql mock; call `repo.upsertUser`, `repo.upsertOrganization`, and `repo.createMembership` with the fake logger; assert `logger.info` is called at least once per method and `logger.warn` is called when the user or org is not found in `createMembership` |
| T031 | Implement: replace static logger in ClerkSyncRepository | implement | R003, R004, R007, R008, NF001, EC001, EC004 | In `src/modules/webhooks/repositories/clerkSyncRepository.ts` remove the `import { logger }` statement; add `logger: BaseLogger` as the last parameter on `upsertUser`, `upsertOrganization`, and `createMembership`; replace every `logger.*` call site with the parameter; import `BaseLogger` from `pino`; do not alter any log message text, level, or structured field name |
| T032 | Test: dispatchMobbexEvent accepts and forwards logger | test | R002, R005, EC003 | In `tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` add a fake `BaseLogger` to `makeRepo` and `makeRefundRepo`; pass it as the third argument to every `dispatchMobbexEvent` call; the repo mock stubs can ignore the logger param (no assertion on logger internals needed — the repo mock already validates forwarding) |
| T033 | Implement: add logger param to dispatchMobbexEvent | implement | R002, R005, NF001 | In `src/modules/webhooks/mobbex/mobbexEventHandlers.ts` add `logger: BaseLogger` as the third parameter of `dispatchMobbexEvent`; forward `logger` to all `repo.*` calls (`recordEvent`, `updateTransactionStatus`, `upsertRefundAndMaybeMarkTransactionRefunded`); import `BaseLogger` from `pino` |
| T034 | Implement: wire request.log in Mobbex webhook route | implement | R005, R006, NF002 | In `src/modules/webhooks/mobbex/routes.ts` pass `request.log` as the third argument to `dispatchMobbexEvent` |
| T035 | Test: dispatchClerkEvent and helpers accept and forward logger | test | R002, R004, EC003 | In a new test file `tests/unit/modules/webhooks/clerk/clerkEventHandlers.test.ts` create a fake `BaseLogger` and a `ClerkSyncRepository` mock; call `dispatchClerkEvent` for each recognized event type passing the fake logger; verify `repo.upsertUser`, `repo.upsertOrganization`, and `repo.createMembership` are called with the fake logger as the last argument |
| T036 | Implement: add logger param to dispatchClerkEvent and handle* helpers | implement | R002, R004, NF001 | In `src/modules/webhooks/clerk/clerkEventHandlers.ts` add `logger: BaseLogger` as the third parameter of `dispatchClerkEvent`; add `logger: BaseLogger` as the third parameter of `handleUserUpsert`, `handleOrganizationUpsert`, and `handleMembershipCreate`; forward `logger` to all `repo.*` calls; import `BaseLogger` from `pino` |
| T037 | Implement: wire request.log in Clerk webhook route | implement | R004, R006, NF002 | In `src/modules/webhooks/clerk/routes.ts` pass `request.log` as the third argument to `dispatchClerkEvent` |
| T038 | Test: static logger retained for non-request paths | test | R007, R008, EC004 | In `tests/unit/billing/transactionDBRepository.test.ts` add a test that calls a repository method with the static `logger` imported from `src/shared/infrastructure/logger.ts` instead of a fake, asserting the call succeeds without throwing (verifies EC004, R007 compatibility with the real pino logger shape, and R008 — that the emitted message text and structured fields are unchanged) |
