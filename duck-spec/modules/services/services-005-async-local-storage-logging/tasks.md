# SERVICES-005 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Create `requestContext` module | implement | R005, EC001, EC002 | In `apps/services/src/shared/infrastructure/requestContext.ts` export the `RequestContext` interface (`{ requestId: string }`) and a single `AsyncLocalStorage<RequestContext>` instance named `requestContext`. |
| T002 | Test: `requestContext` store isolation | test | R005, EC001, EC002 | In `apps/services/tests/unit/shared/infrastructure/requestContext.test.ts` write failing tests asserting: WHEN two `requestContext.run` calls execute concurrently THEN each async chain reads only its own `requestId` from `getStore()`; WHEN code runs outside any `run` call THEN `getStore()` returns `undefined`. |
| T003 | Add `mixin` to static Pino logger | implement | R001, R002, R003, R006, NF001, NF002, EC004 | In `apps/services/src/shared/infrastructure/logger.ts` import `requestContext` from `./requestContext.js` and add a `mixin` option to the existing `pino({...})` call: `mixin() { const store = requestContext.getStore(); return store ? { requestId: store.requestId } : {}; }` — no other change to the file. |
| T004 | Test: logger mixin outside request scope | test | R003, R006, NF001 | In `apps/services/tests/unit/shared/infrastructure/logger.test.ts` write a failing test asserting: WHEN `logger.info(...)` is called outside any `requestContext.run` THEN the serialized log object does NOT contain a `requestId` field and all other fields are unchanged. |
| T005 | Test: logger mixin inside request scope | test | R001, R002, R006, EC004 | In `apps/services/tests/unit/shared/infrastructure/logger.test.ts` write a failing test asserting: WHEN `logger.info(...)` is called inside a `requestContext.run({ requestId: 'test-id' }, ...)` THEN the serialized log object contains `requestId: 'test-id'` and all other fields remain unchanged. |
| T006 | Register `onRequest` hook in `app.ts` | implement | R001, R002, R003, R004, R005, EC003 | In `apps/services/src/app.ts` import `requestContext` from `./shared/infrastructure/requestContext.js` and call `fastify.addHook('onRequest', (request, _reply, done) => { requestContext.run({ requestId: request.id }, done); })` immediately after the `Fastify(...)` instantiation and before any plugin or route registration. |
| T007 | Test: `requestId` present in log during request | test | R001, R002, R003, R005, EC003 | In `apps/services/tests/unit/shared/infrastructure/loggerRequestHook.test.ts` write failing tests using Fastify `inject` asserting: WHEN a request is handled THEN log lines emitted by the static logger during the handler include a `requestId` matching `request.id`; WHEN two requests are injected concurrently THEN each log line carries only the `requestId` of its originating request; WHEN no request is in flight THEN the static logger emits lines with no `requestId`. |
