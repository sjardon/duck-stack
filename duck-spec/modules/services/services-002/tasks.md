# SERVICES-002 — Tasks

| ID | Title | Covers | Description |
|---|---|---|---|
| T001 | Add postgres dependency | R001, NF001, NF002 | In `apps/services/package.json` remove `@supabase/supabase-js` from `dependencies` and add `postgres` as a runtime dependency |
| T002 | Create db singleton | R001, R009, NF001, EC005 | In the new module `db.ts`, export a `postgres.js` `Sql` singleton: read `DATABASE_URL` from `process.env`, throw a descriptive `Error` if absent or empty, then call `postgres(databaseUrl)` and export the result as `db` |
| T003 | Delete supabase.ts | R010, NF002 | Delete `apps/services/src/shared/infrastructure/supabase.ts` — it is fully replaced by `db.ts` |
| T004 | Rewrite UserDBRepository constructor | R001, NF001 | In `UserDBRepository`, change the constructor parameter type from `SupabaseClient` to `Sql` (imported from `postgres`) and remove the `@supabase/supabase-js` import |
| T005 | Rewrite findByClerkUserId | R002, EC004, EC006 | In `UserDBRepository.findByClerkUserId`, replace the Supabase chain with `sql\`SELECT name, email, avatar_url, locale, timezone FROM users WHERE clerk_user_id = ${clerkUserId} LIMIT 1\``; return `null` when the result array is empty; let driver errors propagate as thrown `Error` instances |
| T006 | Rewrite updatePreferences | R003, EC006 | In `UserDBRepository.updatePreferences`, replace the Supabase chain with a tagged-template `UPDATE users SET … WHERE clerk_user_id = ${clerkUserId} RETURNING name, email, avatar_url, locale, timezone`; build `SET` clause only for keys present in `patch`; return the first returned row mapped to `UserProfile`; let driver errors propagate |
| T007 | Rewrite ClerkSyncRepository constructor | R001, NF001 | In `ClerkSyncRepository`, change the constructor parameter type from `SupabaseClient` to `Sql` (imported from `postgres`) and remove the `@supabase/supabase-js` import |
| T008 | Rewrite upsertUser | R004, EC006 | In `ClerkSyncRepository.upsertUser`, replace the Supabase upsert chain with `sql\`INSERT INTO users (clerk_user_id, email, name, avatar_url, updated_at) VALUES (…) ON CONFLICT (clerk_user_id) DO UPDATE SET email=EXCLUDED.email, name=EXCLUDED.name, avatar_url=EXCLUDED.avatar_url, updated_at=EXCLUDED.updated_at\``; let driver errors propagate |
| T009 | Rewrite upsertOrganization | R005, EC006 | In `ClerkSyncRepository.upsertOrganization`, replace the Supabase upsert chain with `sql\`INSERT INTO organizations (clerk_org_id, name, slug, updated_at) VALUES (…) ON CONFLICT (clerk_org_id) DO UPDATE SET name=EXCLUDED.name, slug=EXCLUDED.slug, updated_at=EXCLUDED.updated_at\``; let driver errors propagate |
| T010 | Rewrite createMembership — user lookup | R006, R007, EC001 | In `ClerkSyncRepository.createMembership`, replace the first Supabase `.from('users').select('id')…` chain with `sql\`SELECT id FROM users WHERE clerk_user_id = ${clerkUserId} LIMIT 1\``; when result is empty emit the existing warning log message and return |
| T011 | Rewrite createMembership — org lookup | R006, R008, EC002 | In `ClerkSyncRepository.createMembership`, replace the second Supabase `.from('organizations').select('id')…` chain with `sql\`SELECT id FROM organizations WHERE clerk_org_id = ${clerkOrgId} LIMIT 1\``; when result is empty emit the existing warning log message and return |
| T012 | Rewrite createMembership — membership insert | R006, EC003, EC006 | In `ClerkSyncRepository.createMembership`, replace the final Supabase upsert chain with `sql\`INSERT INTO organization_members (user_id, org_id, role) VALUES (${userId}, ${orgId}, ${role}) ON CONFLICT (user_id, org_id) DO NOTHING\``; let driver errors propagate |
| T013 | Wire db into getUserProfileHandler | R001, R002, R010 | In `getUserProfileHandler.ts`, replace `import { supabase } from '…/supabase.js'` with `import { db } from '…/db.js'` and change `new UserDBRepository(supabase)` to `new UserDBRepository(db)` |
| T014 | Wire db into updateUserProfileHandler | R001, R003, R010 | In `updateUserProfileHandler.ts`, replace `import { supabase } from '…/supabase.js'` with `import { db } from '…/db.js'` and change `new UserDBRepository(supabase)` to `new UserDBRepository(db)` |
| T015 | Wire db into clerk webhook routes | R001, R004, R005, R006, R010 | In `modules/webhooks/clerk/routes.ts`, replace `import { supabase } from '…/supabase.js'` with `import { db } from '…/db.js'` and change `new ClerkSyncRepository(supabase)` to `new ClerkSyncRepository(db)` |
