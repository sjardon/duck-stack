# SERVICES-009 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: completeOnboardingHandler throws ValidationError on invalid body | test | R001, R003, R014 | In `apps/services/tests/unit/users/completeOnboardingHandler.test.ts`, update the existing ZodError validation tests to assert the handler throws a `ValidationError` (instead of calling `reply.status(400)`); mock `logger` to confirm no `reply.status` is called; assert error `code` is `'VALIDATION_ERROR'` and the test relies on `errorHandler` for the HTTP response |
| T002 | Implement: completeOnboardingHandler throws ValidationError on ZodError | implement | R001, R003, R014 | In `completeOnboardingHandler` in `apps/services/src/modules/users/handlers/completeOnboardingHandler.ts`, import `ValidationError` from `shared/errors.ts`; inside the ZodError catch branch replace `return reply.status(400).send(…)` with `throw new ValidationError(err.issues[0]?.message ?? 'Invalid request body', err)` |
| T003 | Test: updateUserProfileHandler throws ValidationError on invalid body | test | R002, R003, R014 | Create `apps/services/tests/unit/modules/users/handlers/updateUserProfileHandler.test.ts`; assert WHEN body fails schema THEN the handler throws `ValidationError` with `code === 'VALIDATION_ERROR'`; assert WHEN body is valid THEN `reply.send` is called with `{ data: profile }` and no `reply.status(400)` is called |
| T004 | Implement: updateUserProfileHandler throws ValidationError on ZodError | implement | R002, R003, R014 | In `updateUserProfileHandler` in `apps/services/src/modules/users/handlers/updateUserProfileHandler.ts`, import `ValidationError` from `shared/errors.ts`; replace `return reply.status(400).send(…)` in the ZodError catch branch with `throw new ValidationError(err.issues[0]?.message ?? 'Invalid request body', err)` |
| T005 | Test: Clerk webhook missing headers throws ValidationError | test | R004, R014, EC001 | Create `apps/services/tests/unit/modules/webhooks/clerk/routes.test.ts`; using `fastify.inject`, assert WHEN Svix headers are absent THEN response is HTTP 400 with body `{ code: 'VALIDATION_ERROR', message: 'Missing required Svix headers' }` via `errorHandler` (not a manual `reply`) |
| T006 | Implement: Clerk webhook missing headers throws ValidationError | implement | R004, R014, EC001 | In the route handler in `apps/services/src/modules/webhooks/clerk/routes.ts`, replace `return reply.status(400).send({ error: 'Missing required Svix headers' })` with `throw new ValidationError('Missing required Svix headers')`; import `ValidationError` from `shared/errors.ts` |
| T007 | Test: Clerk webhook signature failure throws ValidationError | test | R005, R014, EC002 | In `apps/services/tests/unit/modules/webhooks/clerk/routes.test.ts`, assert WHEN `verifyWebhook` throws THEN response is HTTP 400 with body `{ code: 'VALIDATION_ERROR', message: 'Webhook signature verification failed' }`; assert `logger.warn` is called once with the caught error |
| T008 | Implement: Clerk webhook signature failure throws ValidationError and logs warn | implement | R005, R007, R008, R014, EC002 | In `apps/services/src/modules/webhooks/clerk/routes.ts`, update the `verifyWebhook` catch block: add `logger.warn({ err }, 'Clerk webhook signature verification failed')` before throwing; replace `return reply.status(400).send({ error: '…' })` with `throw new ValidationError('Webhook signature verification failed', err instanceof Error ? err : undefined)`; import `logger` from `shared/infrastructure/logger.ts` and `ValidationError` |
| T009 | Test: checkoutUseCase logs before re-throw on provider failure | test | R007, R008, R009 | In `apps/services/tests/unit/billing/checkoutUseCase.test.ts`, mock `logger`; assert WHEN provider throws `ProviderError` with `statusCode 502` THEN `logger.error` is called with the error before the use case re-throws; assert WHEN provider throws `ProviderError` with `statusCode 400` THEN `logger.warn` is called |
| T010 | Implement: checkoutUseCase logs before re-throw | implement | R007, R008, R009 | In `CheckoutUseCase.execute` in `apps/services/src/modules/billing/useCases/checkoutUseCase.ts`, in the provider catch block add: if `err instanceof DomainError && err.statusCode < 500` then `logger.warn({ err }, 'CheckoutUseCase: provider checkout failed')` else `logger.error({ err }, 'CheckoutUseCase: provider checkout failed')`; import `logger` from `shared/infrastructure/logger.ts` and `DomainError` from `shared/errors.ts` |
| T011 | Test: cancelSubscriptionUseCase logs warn on ProviderError 400 and returns updated | test | R007, R008, R010, EC003 | In `apps/services/tests/unit/modules/subscriptions/cancelSubscriptionUseCase.test.ts`, mock `logger`; assert WHEN `cancelSubscription` throws `ProviderError` with `statusCode 400` THEN `logger.warn` is called with the error and the use case returns the locally-cancelled subscription |
| T012 | Test: cancelSubscriptionUseCase logs error before re-throw on non-400 failure | test | R007, R009 | In `apps/services/tests/unit/modules/subscriptions/cancelSubscriptionUseCase.test.ts`, assert WHEN `cancelSubscription` throws `ProviderError` with `statusCode 502` THEN `logger.error` is called before the use case re-throws the error |
| T013 | Implement: cancelSubscriptionUseCase logs before silent-fail and re-throw | implement | R007, R008, R009, R010, R013, EC003 | In `CancelSubscriptionUseCase.execute` in `apps/services/src/modules/subscriptions/useCases/cancelSubscriptionUseCase.ts`, update the provider catch block: for `ProviderError` with `statusCode === 400`, call `logger.warn({ err }, 'CancelSubscriptionUseCase: provider cancel rejected (400), returning local result')` and add an inline comment justifying the silent-fail before `return updated`; for all other errors call `logger.error({ err }, 'CancelSubscriptionUseCase: provider cancel failed with unexpected error')` before `throw err`; import `logger` |
| T014 | Test: listTransactionsUseCase logs warn in both cursor catch branches | test | R007, R008, EC005 | In `apps/services/tests/unit/billing/listTransactionsUseCase.test.ts`, mock `logger`; assert WHEN cursor decode throws `ValidationError` THEN `logger.warn` is called before re-throw; assert WHEN cursor decode throws a non-`ValidationError` THEN `logger.warn` is called before a new `ValidationError` is thrown |
| T015 | Implement: listTransactionsUseCase logs before cursor re-throw | implement | R007, R008, EC005 | In `ListTransactionsUseCase.execute` in `apps/services/src/modules/billing/useCases/listTransactionsUseCase.ts`, update the inner catch block of the cursor decode section: for the `ValidationError` branch add `logger.warn({ err }, 'ListTransactionsUseCase: invalid cursor (re-throwing)')` before `throw err`; for the other branch add `logger.warn({ err }, 'ListTransactionsUseCase: cursor decode/parse failed, throwing ValidationError')` before `throw new ValidationError('Invalid cursor', err instanceof Error ? err : undefined)`; import `logger` |
| T016 | Test: clerkAuthPlugin logs warn on JWT verification failure | test | R011, R013, EC004 | Create `apps/services/tests/unit/shared/plugins/clerkAuthPlugin.test.ts`; build a Fastify app with `clerkAuthPlugin` registered; mock `verifyToken` to throw; assert WHEN JWT verification throws THEN `logger.warn` is called with the error and `request.userId` remains `undefined` |
| T017 | Implement: clerkAuthPlugin logs warn on JWT failure | implement | R011, R013, EC004 | In the `onRequest` hook catch block in `apps/services/src/shared/plugins/clerkAuthPlugin.ts`, add `logger.warn({ err }, 'clerkAuthPlugin: JWT verification failed; request proceeds without userId')` and an inline comment explaining the silent-fail; import `logger` from `shared/infrastructure/logger.ts`; bind the catch variable (`catch (err)`) |
| T018 | Test: mobbexWebhookRoutes logs warn on JSON parse failure | test | R006, EC006 | In `apps/services/tests/unit/modules/webhooks/mobbex/routes.test.ts`, mock `logger`; assert WHEN request body is not valid JSON THEN `logger.warn` is called with the parse error before `ValidationError` is thrown and the HTTP response is 400 `VALIDATION_ERROR` |
| T019 | Implement: mobbexWebhookRoutes logs warn before throwing ValidationError on parse failure | implement | R006, EC006 | In the JSON parse catch block in `apps/services/src/modules/webhooks/mobbex/routes.ts`, add `logger.warn({ err: parseErr }, 'mobbexWebhookRoutes: failed to parse request body as JSON')` before the existing `throw new ValidationError('Request body is not valid JSON')`; bind the catch variable (`catch (parseErr)`); import `logger` |
| T020 | Test: mobbexProvider.handleErrorResponse logs warn on body parse failure | test | R012, R013, NF004 | In `apps/services/tests/unit/billing/mobbexProvider.test.ts`, assert WHEN the provider error response body is not valid JSON THEN `logger.warn` is called with the parse error (without token or secret fields) and the method still throws a `ProviderError` with the correct status code |
| T021 | Implement: mobbexProvider.handleErrorResponse justifying comment verification | implement | R012, R013, NF004 | In `handleErrorResponse` in `apps/services/src/modules/billing/providers/mobbexProvider.ts`, verify the existing `logger.warn` call is present and correct; ensure the inline comment explicitly states that a malformed error body from the provider should not prevent the correct HTTP status from being mapped, and that no token, secret, or PII is included in the logged fields |
