# SERVICES-011 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: `errorTrackingConfig` parsing and defaults | test | R007, R008 | In `apps/services/tests/unit/shared/configs/errorTrackingConfig.test.ts` write failing tests asserting: WHEN `ERROR_TRACKING_DSN` is set THEN `enabled` is `true` and `dsn` matches it; WHEN it is unset THEN `enabled` is `false` and `dsn` is `''`; WHEN `SERVICE_VERSION`/`ERROR_TRACKING_SAMPLE_RATE` are unset THEN `release` defaults to `'unknown'` and `sampleRate` defaults to `0.2`. |
| T002 | Implement `errorTrackingConfig` | implement | R007, R008 | In `apps/services/src/shared/configs/errorTrackingConfig.ts` export `errorTrackingConfig` reading `ERROR_TRACKING_DSN`, `SERVICE_VERSION`, `ERROR_TRACKING_SAMPLE_RATE`, and `NODE_ENV` per the design's shape, with no `process.env` reads outside this file. |
| T003 | Add `@sentry/node` runtime dependency | implement | R007 | In `apps/services/package.json` add `@sentry/node` (`^10.0.0`) to `dependencies` and run the workspace install so the lockfile is updated. |
| T004 | Test: `ErrorOccurrenceSampler` first-occurrence guarantee and repeat sampling | test | EC004 | In `apps/services/tests/unit/shared/providers/errorOccurrenceSampler.test.ts` write failing tests asserting: WHEN `shouldSend(fingerprint)` is called for a fingerprint never seen before THEN it returns `true` regardless of `sampleRate`; WHEN called again for the same fingerprint with `sampleRate` mocked deterministically THEN it applies the sample rate instead of always returning `true`. |
| T005 | Implement `ErrorOccurrenceSampler` | implement | EC004 | In `apps/services/src/shared/providers/errorOccurrenceSampler.ts` implement `ErrorOccurrenceSampler.shouldSend(fingerprint: string): boolean` per the design: always `true` on first sight of a fingerprint, `Math.random() < sampleRate` on repeats, backed by a `Map` capped at `MAX_TRACKED_FINGERPRINTS` with FIFO eviction. |
| T006 | Test: `NoopErrorReporter.report()` is a no-op | test | R008 | In `apps/services/tests/unit/shared/providers/noopErrorReporter.test.ts` write a failing test asserting: WHEN `report(error)` is called THEN it returns `undefined` and never throws, for any input including `undefined`/non-`Error` values. |
| T007 | Implement `NoopErrorReporter` | implement | R008 | In `apps/services/src/shared/providers/noopErrorReporter.ts` implement `NoopErrorReporter implements IErrorReporter` with a `report()` method that does nothing. |
| T008 | Test: `SentryErrorReporter.report()` requestId tagging | test | R003, EC005 | In `apps/services/tests/unit/shared/providers/sentryErrorReporter.test.ts` (mocking `@sentry/node`) write failing tests asserting: WHEN `report(error, { requestId: 'abc' })` is called THEN `Sentry.withScope`'s scope has `setTag('requestId', 'abc')` called before `Sentry.captureException(error)`; WHEN `context.requestId` is `undefined` THEN `setTag` is never called and `captureException` still runs. |
| T009 | Test: `SentryErrorReporter` constructor wires config into `Sentry.init` | test | R004, R006 | In the same test file write a failing test asserting: WHEN `new SentryErrorReporter(config)` is constructed THEN `Sentry.init` is called once with `dsn`, `environment`, `release` from `config` and `sendDefaultPii: false`. |
| T010 | Test: `SentryErrorReporter` `beforeSend` strips request/user data | test | R006, NF003, EC002 | In the same test file write a failing test asserting: WHEN the `beforeSend` callback passed to `Sentry.init` is invoked with an event containing `request`/`user` fields THEN the returned event has both fields removed. |
| T011 | Test: `SentryErrorReporter` `beforeSend` enforces occurrence sampling | test | EC004, R005 | In the same test file write failing tests asserting: WHEN `beforeSend` is invoked for a fingerprint the injected `ErrorOccurrenceSampler` reports as first-seen THEN the event is returned unchanged; WHEN the sampler reports the fingerprint should be dropped THEN `beforeSend` returns `null`. |
| T012 | Test: `SentryErrorReporter.report()` isolates internal SDK failures | test | NF002 | In the same test file write a failing test asserting: WHEN `Sentry.captureException` throws synchronously THEN `report()` does not propagate the throw, and the static `logger.error` is called instead. |
| T013 | Implement `SentryErrorReporter` | implement | R003, R004, R005, R006, NF002, NF003, EC002, EC004, EC005 | In `apps/services/src/shared/providers/sentryErrorReporter.ts` implement `SentryErrorReporter implements IErrorReporter` per the design: constructor builds an `ErrorOccurrenceSampler` and calls `Sentry.init({ dsn, environment, release, sendDefaultPii: false, beforeSend })`; `report()` wraps `Sentry.withScope` + `captureException` in `try/catch` logging failures via the static `logger`; `beforeSend` deletes `event.request`/`event.user` and consults the sampler via a fingerprint derived from `event.exception`. |
| T014 | Refactor: extract `fingerprintFor` as a pure exported function | refactor | R005 | In `apps/services/src/shared/providers/sentryErrorReporter.ts` extract the fingerprint derivation used by `beforeSend` into a standalone exported `fingerprintFor(event: Sentry.ErrorEvent): string` function so it is independently testable and `beforeSend` stays a thin orchestrator. |
| T015 | Test: `createErrorReporter` factory selection | test | R007, R008, EC006 | In `apps/services/tests/unit/shared/providers/errorReporter.test.ts` write failing tests asserting: WHEN `errorTrackingConfig.enabled` is `true` THEN `createErrorReporter()` returns a `SentryErrorReporter` instance; WHEN it is `false` THEN it returns a `NoopErrorReporter` instance and the static `logger.info` (never `logger.error`) is called. |
| T016 | Implement `errorReporter` port, factory, and singleton | implement | R007, R008, EC006 | In `apps/services/src/shared/providers/errorReporter.ts` export the `IErrorReporter` interface, `ErrorReportContext` type, `createErrorReporter()` function per the design's branching, and the module-scope singleton `export const errorReporter: IErrorReporter = createErrorReporter();`. |
| T017 | Test: `errorHandler` reports reportable errors | test | R001, R003 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts` write failing tests asserting: WHEN a non-`DomainError` reaches the error handler THEN `errorReporter.report` is called with that error and the current `requestId`; WHEN a `DomainError` with `statusCode >= 500` reaches it THEN `errorReporter.report` is called the same way. |
| T018 | Test: `errorHandler` skips reporting for expected 4xx errors | test | EC003 | In the same test file write a failing test asserting: WHEN a `DomainError` with `statusCode < 500` (e.g. `ValidationError`, `NotFoundError`) reaches the error handler THEN `errorReporter.report` is never called. |
| T019 | Test: `errorHandler` omits requestId outside request context | test | EC005 | In the same test file write a failing test asserting: WHEN `requestContext.getStore()` returns `null`/`undefined` at the time of the error THEN `errorReporter.report` is still called, with `context.requestId` left `undefined`. |
| T020 | Test: `errorHandler` reply is independent of the reporter | test | NF001, NF002 | In the same test file write a failing test asserting: WHEN `errorReporter.report` is mocked to throw synchronously THEN the HTTP response is still sent with the correct status/body, i.e. `errorHandler` never awaits or lets `report()` affect the reply. |
| T021 | Implement `errorHandler.ts` reporting integration | implement | R001, R003, NF001, NF002, EC003, EC005 | In `apps/services/src/shared/plugins/errorHandler.ts`'s `logError`, in the `DomainError` (`statusCode >= 500`) and non-`DomainError` branches only, add a call to `errorReporter.report(error, { requestId: requestContext.getStore()?.requestId })` immediately after the existing `logger.error(...)` call; import `errorReporter` and `requestContext`. |
| T022 | Test: `resendEmailNotifier.send()` reports caught dispatch failures | test | R002, EC001 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts` write a failing test asserting: WHEN `dispatch()` rejects (e.g. the Resend client throws) THEN `send()`'s catch calls `errorReporter.report` with that error and the current `requestId`, and the rejection never propagates out of `send()`. |
| T023 | Implement `resendEmailNotifier.ts` reporting integration | implement | R002, EC001 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`'s `send()`, replace the empty `.catch(() => {})` with `.catch((err: unknown) => { errorReporter.report(err, { requestId: requestContext.getStore()?.requestId }); })`; import `errorReporter` and `requestContext`. |
| T024 | Wire error tracking env vars into the DigitalOcean app spec | implement | R004, R007, R008 | In `.do/app.yaml`'s `services[0].envs`, add `ERROR_TRACKING_DSN` (`type: secret`, `value: ${ERROR_TRACKING_DSN}`), `ERROR_TRACKING_SAMPLE_RATE` (`type: general`), and `SERVICE_VERSION` (`type: general`) entries grouped under an `# --- errorTrackingConfig ---` comment, matching the file's existing per-config-file grouping convention. |
