# SERVICES-003 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: serverConfig defaults and env overrides | test | R001, R002, R003, R007, EC001 | In `apps/services/tests/unit/shared/configs/serverConfig.test.ts`, write failing tests asserting: WHEN env vars are absent THEN `serverConfig` exposes the documented defaults (`nodeEnv: 'development'`, `logLevel: 'info'`, `host: '0.0.0.0'`, `port: 3000`, `corsOrigin: '*'`); AND WHEN env vars are set THEN each property reflects the set value |
| T002 | Implement serverConfig | implement | R001, R002, R003, R007, EC001 | In `apps/services/src/shared/configs/serverConfig.ts` (CREATE), export `serverConfig` using `const env = process.env \|\| {}` with properties `nodeEnv`, `logLevel`, `host`, `port` (Number), `corsOrigin` and their documented defaults |
| T003 | Test: authConfig defaults and env overrides | test | R001, R004, R005, EC001, EC002 | In `apps/services/tests/unit/shared/configs/authConfig.test.ts`, write failing tests asserting: WHEN `CLERK_JWT_KEY` is absent THEN `authConfig.clerkJwtKey` is `undefined`; WHEN set THEN it reflects the value; WHEN `CLERK_WEBHOOK_SIGNING_SECRET` is absent THEN `authConfig.clerkWebhookSigningSecret` is `undefined`; WHEN set THEN it reflects the value |
| T004 | Implement authConfig | implement | R001, R004, R005, EC001, EC002 | In `apps/services/src/shared/configs/authConfig.ts` (CREATE), export `authConfig` using `const env = process.env \|\| {}` with properties `clerkJwtKey` (`env.CLERK_JWT_KEY`) and `clerkWebhookSigningSecret` (`env.CLERK_WEBHOOK_SIGNING_SECRET`) — no defaults (both are optional or fail-fast in consumers) |
| T005 | Test: mobbexConfig expanded shape | test | R001, R006, EC001, EC002, EC003 | In `apps/services/tests/unit/shared/configs/mobbexConfig.test.ts`, write failing tests asserting: WHEN `MOBBEX_*` vars are absent THEN `mobbexConfig` exposes defaults (`billingProvider: 'mobbex'`, `apiKey: ''`, `accessToken: ''`, `testMode: false`, `timeoutMs: 10000`, `webhookSecret: ''`); WHEN `MOBBEX_TEST_MODE` is `'true'` OR `'1'` THEN `testMode` is `true`; WHEN `MOBBEX_TEST_MODE` is any other value THEN `testMode` is `false` |
| T006 | Implement expanded mobbexConfig | implement | R001, R006, EC001, EC002, EC003 | In `apps/services/src/shared/configs/mobbexConfig.ts` (MODIFY), replace the current single-property shape with the full set: `billingProvider` (`env.BILLING_PROVIDER ?? 'mobbex'`), `apiKey` (`env.MOBBEX_API_KEY ?? ''`), `accessToken` (`env.MOBBEX_ACCESS_TOKEN ?? ''`), `testMode` (`env.MOBBEX_TEST_MODE === 'true' \|\| env.MOBBEX_TEST_MODE === '1'`), `timeoutMs` (`parseInt(env.MOBBEX_TIMEOUT_MS ?? '10000', 10)`), `webhookSecret` (`env.MOBBEX_WEBHOOK_SECRET ?? ''`) |
| T007 | Implement app.ts migration | implement | R002, R007, R008, NF002 | In `apps/services/src/app.ts`, import `serverConfig`; replace `process.env.LOG_LEVEL ?? 'info'` with `serverConfig.logLevel` and `process.env.NODE_ENV !== 'production'` with `serverConfig.nodeEnv !== 'production'` in the Fastify logger options |
| T008 | Implement server.ts migration | implement | R002, R008, NF002 | In `apps/services/src/server.ts`, import `serverConfig`; replace `process.env.HOST ?? '0.0.0.0'` with `serverConfig.host` and `Number(process.env.PORT ?? 3000)` with `serverConfig.port` |
| T009 | Implement cors.ts migration | implement | R003, R008, NF002 | In `apps/services/src/shared/plugins/cors.ts`, import `serverConfig`; replace `process.env.CORS_ORIGIN ?? '*'` with `serverConfig.corsOrigin` |
| T010 | Implement logger.ts migration | implement | R007, R008, NF002 | In `apps/services/src/shared/infrastructure/logger.ts`, import `serverConfig`; replace `process.env.LOG_LEVEL ?? 'info'` with `serverConfig.logLevel` and `process.env.NODE_ENV !== 'production'` with `serverConfig.nodeEnv !== 'production'` |
| T011 | Implement clerk-auth.plugin.ts migration | implement | R004, R008, NF001, NF002, EC002 | In `apps/services/src/shared/plugins/clerk-auth.plugin.ts`, import `authConfig`; replace `const jwtKey = process.env.CLERK_JWT_KEY` with `const jwtKey = authConfig.clerkJwtKey`; leave `const secretKey = process.env.CLERK_SECRET_KEY` and its fail-fast guard untouched (documented exception) |
| T012 | Implement clerk webhook routes migration | implement | R005, R008, NF001, NF002, EC002 | In `apps/services/src/modules/webhooks/clerk/routes.ts`, import `authConfig`; replace `const signingSecret = process.env.CLERK_WEBHOOK_SIGNING_SECRET` with `const signingSecret = authConfig.clerkWebhookSigningSecret`; preserve the fail-fast throw when `signingSecret` is falsy |
| T013 | Implement resolveProvider.ts migration | implement | R006, R008, NF001, NF002, EC001, EC002, EC003 | In `apps/services/src/modules/billing/providers/resolveProvider.ts`, import `mobbexConfig`; replace all `process.env['BILLING_PROVIDER']`, `process.env['MOBBEX_API_KEY']`, `process.env['MOBBEX_ACCESS_TOKEN']`, `process.env['MOBBEX_TEST_MODE']`, `process.env['MOBBEX_TIMEOUT_MS']`, `process.env['MOBBEX_WEBHOOK_SECRET']` reads with the corresponding `mobbexConfig` properties; preserve the fail-fast throw when `apiKey` or `accessToken` are absent and the unknown-provider throw |
