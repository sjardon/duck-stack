# SERVICES-007 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: DomainError stores originalError | test | R001, R003 | In `apps/services/tests/unit/shared/errors.test.ts`, write failing tests asserting: WHEN `DomainError` is constructed with a fourth argument THEN `instance.originalError` equals that argument; and WHEN constructed without a fourth argument THEN `instance.originalError` is `undefined`. |
| T002 | Implement: extend DomainError constructor with originalError | implement | R001, R002, R003 | In `DomainError` constructor in `apps/services/src/shared/errors.ts`, add `public readonly originalError?: unknown` as the fourth parameter and pass it to the stored property; leave all existing subclass `super(...)` calls unchanged. |
| T003 | Test: existing subclasses construct without originalError | test | R002 | In `apps/services/tests/unit/shared/errors.test.ts`, write failing tests asserting: WHEN each of `NotFoundError`, `ValidationError`, `UnauthorizedError`, `ForbiddenError`, `ProviderError` is constructed with their current call signature THEN the instance is a `DomainError`, `originalError` is `undefined`, and `code`/`message`/`statusCode` are unchanged. |
| T004 | Test: errorHandler logs DomainError 4xx at warn level | test | R004, R005 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts`, write a failing test asserting: WHEN `errorHandler` intercepts a `DomainError` with `statusCode < 500` THEN `logger.warn` is called once with a payload containing `code`, `message`, `statusCode`, and `originalError` before the reply is sent. |
| T005 | Test: errorHandler logs DomainError 5xx at error level with stack | test | R004, R006 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts`, write a failing test asserting: WHEN `errorHandler` intercepts a `DomainError` with `statusCode >= 500` THEN `logger.error` is called once with a payload containing `code`, `message`, `statusCode`, `stack`, and `originalError`. |
| T006 | Test: errorHandler logs non-DomainError at error level | test | R004, R007, NF003 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts`, write a failing test asserting: WHEN `errorHandler` intercepts a plain `Error` (not a `DomainError`) THEN `logger.error` is called once with a payload containing `message`, `stack`, and `originalError` equal to the raw error instance. |
| T007 | Test: errorHandler replies with DomainError code/message/statusCode | test | R008 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts`, write a failing test asserting: WHEN `errorHandler` intercepts a `DomainError` THEN the HTTP reply status equals `error.statusCode` and the body is `{ code: error.code, message: error.message }` with no additional fields. |
| T008 | Test: errorHandler replies INTERNAL_ERROR/500 for non-DomainError | test | R009, R010 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts`, write a failing test asserting: WHEN `errorHandler` intercepts any non-`DomainError` THEN the HTTP reply status is 500 and the body is exactly `{ code: 'INTERNAL_ERROR', message: 'Internal server error' }`. |
| T009 | Test: errorHandler never leaks originalError or stack in the response body | test | R010, NF002 | In `apps/services/tests/unit/shared/plugins/errorHandler.test.ts`, write failing tests asserting: WHEN `errorHandler` replies for both `DomainError` (with `originalError` set) and non-`DomainError` THEN the serialized response body contains only `code` and `message` fields. |
| T010 | Implement: add logError helper to errorHandler | implement | R004, R005, R006, R007, NF001, NF003 | In `apps/services/src/shared/plugins/errorHandler.ts`, import `logger` from `../infrastructure/logger.js` and add a private `logError(error: unknown): void` function with three branches: (1) `DomainError && statusCode < 500` → `logger.warn({ code, message, statusCode, originalError }, '...')`, (2) `DomainError && statusCode >= 500` → `logger.error({ code, message, statusCode, stack, originalError }, '...')`, (3) non-`DomainError` → `logger.error({ message, stack, originalError: error }, '...')`. |
| T011 | Implement: rewrite setErrorHandler callback to use logError and fix non-DomainError response | implement | R004, R008, R009, R010, NF002, NF003 | In the `setErrorHandler` callback in `apps/services/src/shared/plugins/errorHandler.ts`, call `logError(error)` first, then branch: `DomainError` → `reply.status(error.statusCode).send({ code: error.code, message: error.message })`; non-`DomainError` → `reply.status(500).send({ code: 'INTERNAL_ERROR', message: 'Internal server error' })`. |
