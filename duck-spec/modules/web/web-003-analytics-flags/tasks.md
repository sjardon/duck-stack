# WEB-003 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: init calls `posthog.init` when key present | test | R007 | In `apps/web/tests/analytics/analytics.test.ts` assert: WHEN `VITE_POSTHOG_KEY` is set THEN `initAnalytics()` calls the mocked `posthog.init` with that key and a resolved `api_host` (`VITE_POSTHOG_HOST` or the documented default). |
| T002 | Implement `readAnalyticsConfig` and `initAnalytics` init branch | implement | R007 | In `apps/web/src/lib/analytics.ts`, add `readAnalyticsConfig()` reading `import.meta.env.VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST` and `initAnalytics()` calling `posthog.init(key, { api_host, ... })` when config is present. |
| T003 | Test: no-op when key absent | test | R008 | In `apps/web/tests/analytics/analytics.test.ts` assert: WHEN `VITE_POSTHOG_KEY` is absent THEN `initAnalytics()` returns without calling `posthog.init` and without throwing. |
| T004 | Implement early-return guard for absent config | implement | R008 | In `apps/web/src/lib/analytics.ts`'s `initAnalytics()`, return immediately when `readAnalyticsConfig()` is `null`, before any `posthog.init` call. |
| T005 | Test: init failure isolation | test | NF004 | In `apps/web/tests/analytics/analytics.test.ts` assert: WHEN the mocked `posthog.init` throws THEN `initAnalytics()` does not throw and logs the error instead. |
| T006 | Implement try/catch around `posthog.init` | implement | NF004 | In `apps/web/src/lib/analytics.ts`'s `initAnalytics()`, wrap the `posthog.init` call in `try/catch`, logging via `console.error` on failure and never re-throwing. |
| T007 | Test: automatic screen-view capture configured | test | R001 | In `apps/web/tests/analytics/analytics.test.ts` assert: `initAnalytics()` calls `posthog.init` with `capture_pageview: "history_change"`. |
| T008 | Implement `capture_pageview` option | implement | R001 | In `apps/web/src/lib/analytics.ts`'s `initAnalytics()`, add `capture_pageview: "history_change"` to the `posthog.init` options object. |
| T009 | Test: `captureEvent` records a named event without blocking | test | R002, NF001 | In `apps/web/tests/analytics/analytics.test.ts` assert: `captureEvent(name, properties)` calls the mocked `posthog.capture(name, properties)` synchronously (no `await`/Promise chain in the call itself) and is a documented no-op (returns without throwing) when `posthog` was never initialized. |
| T010 | Implement `captureEvent` | implement | R002, NF001, R008 | In `apps/web/src/lib/analytics.ts`, add `captureEvent(name, properties?)` that calls `posthog.capture(name, properties)` and nothing else. |
| T011 | Test: no automatic high-frequency/DOM capture | test | EC005, NF002 | In `apps/web/tests/analytics/analytics.test.ts` assert: `initAnalytics()` calls `posthog.init` with `autocapture: false` and `capture_heatmaps: false`. |
| T012 | Implement autocapture/heatmap opt-out | implement | EC005, NF002 | In `apps/web/src/lib/analytics.ts`'s `initAnalytics()`, add `autocapture: false` and `capture_heatmaps: false` to the `posthog.init` options object. |
| T013 | Test: session replay masking configured, opt-out per element | test | R004, NF003, EC003 | In `apps/web/tests/analytics/analytics.test.ts` assert: `initAnalytics()` calls `posthog.init` with `session_recording.maskAllInputs: true` and a `maskTextSelector`/`maskTextFn` pair such that `maskTextFn("secret", element)` returns the masked string unless `element.dataset.phAllow === "true"`, in which case it returns the original text unchanged. |
| T014 | Implement session replay masking config | implement | R004, NF003, EC003 | In `apps/web/src/lib/analytics.ts`, add a `maskTextFn` checking `element?.dataset.phAllow === "true"` and pass `session_recording: { maskAllInputs: true, maskTextSelector: "*", maskTextFn }` to `posthog.init`. |
| T015 | Refactor `analytics.ts` for clarity | refactor | R001, R002, R004, R007, R008, NF001, NF002, NF003, NF004, EC003, EC005 | In `apps/web/src/lib/analytics.ts`, extract the `posthog.init` options object construction into a small named function (e.g. `resolveAnalyticsOptions()`), keeping `initAnalytics()` a short orchestration function; no behavior change. |
| T016 | Test: flag resolves to enabled/disabled once loaded | test | R005 | In `apps/web/tests/analytics/useFeatureFlag.test.ts` assert: WHEN the mocked `useFeatureFlagEnabled` returns `true`/`false` and analytics is configured THEN `useFeatureFlag(key)` returns `{ enabled: true, isResolved: true }` / `{ enabled: false, isResolved: true }` respectively. |
| T017 | Implement resolved-value mapping in `useFeatureFlag` | implement | R005 | Create `apps/web/src/hooks/useFeatureFlag.ts` calling `useFeatureFlagEnabled(key)` and, when configured and the value is not `undefined`, returning `{ enabled: rawValue, isResolved: true }`. |
| T018 | Test: not-configured returns deterministic safe default | test | R008, NF004 | In `apps/web/tests/analytics/useFeatureFlag.test.ts` assert: WHEN `readAnalyticsConfig()` returns `null` THEN `useFeatureFlag(key)` returns `{ enabled: false, isResolved: true }` regardless of the mocked `useFeatureFlagEnabled` return value, and `useFeatureFlagEnabled` is still called unconditionally (Rules of Hooks). |
| T019 | Implement not-configured short-circuit in `useFeatureFlag` | implement | R008, NF004 | In `apps/web/src/hooks/useFeatureFlag.ts`, after unconditionally calling `useFeatureFlagEnabled(key)`, return `{ enabled: false, isResolved: true }` immediately when `readAnalyticsConfig()` is `null`. |
| T020 | Test: unresolved (loading or unreachable provider) returns safe default, never resolves to a crash | test | EC001, EC002, NF004 | In `apps/web/tests/analytics/useFeatureFlag.test.ts` assert: WHEN analytics is configured and the mocked `useFeatureFlagEnabled` returns `undefined` (covers both the initial load window and a permanently unreachable/ad-blocked provider) THEN `useFeatureFlag(key)` returns `{ enabled: false, isResolved: false }` and does not throw. |
| T021 | Implement unresolved-value mapping in `useFeatureFlag` | implement | EC001, EC002, NF004 | In `apps/web/src/hooks/useFeatureFlag.ts`, when configured and `rawValue === undefined`, return `{ enabled: false, isResolved: false }`. |
| T022 | Test: flag re-resolves after identification | test | EC006 | In `apps/web/tests/analytics/useFeatureFlag.test.ts` assert: WHEN the mocked `useFeatureFlagEnabled` return value changes across a re-render (simulating the `featureflags` event fired by `posthog.identify()`'s `reloadFeatureFlags()`) THEN `useFeatureFlag(key)` reflects the new value on that re-render rather than the previously resolved one. |
| T023 | Test: gate never renders enabled before resolved | test | EC001 | In `apps/web/tests/analytics/FeatureFlagGate.test.tsx` assert: WHEN `useFeatureFlag` reports `isResolved: false` THEN `FeatureFlagGate` renders the `loading` prop (or `fallback` when `loading` is omitted), never `children`; WHEN `isResolved: true, enabled: true` THEN it renders `children`; WHEN `isResolved: true, enabled: false` THEN it renders `fallback`. |
| T024 | Implement `FeatureFlagGate` | implement | R005, EC001 | Create `apps/web/src/components/flags/FeatureFlagGate.tsx` rendering `loading ?? fallback` when `!isResolved`, `children` when `enabled`, and `fallback` otherwise. |
| T025 | Refactor flag-resolution hook and gate | refactor | R005, R008, NF004, EC001, EC002, EC006 | Review `useFeatureFlag.ts`/`FeatureFlagGate.tsx` for readability (state-mapping branch order, prop typing); no behavior change. |
| T026 | Test: identify called with id only on sign-in | test | R003 | In `apps/web/tests/analytics/use-sync-analytics-user.test.ts` assert: WHEN `useCurrentUser()` returns a signed-in user THEN `useSyncAnalyticsUser()` calls the mocked `posthog.identify` with `user.id` and no other argument carrying name/email/profile data. |
| T027 | Implement identify call in `useSyncAnalyticsUser` | implement | R003, EC004 | Create `apps/web/src/hooks/use-sync-analytics-user.ts` calling `useCurrentUser()` and, inside a `useEffect`, calling `posthog.identify(user.id)` when a user is present. |
| T028 | Test: reset called only when a previously-identified user signs out | test | EC004 | In `apps/web/tests/analytics/use-sync-analytics-user.test.ts` assert: WHEN a user was previously identified and `useCurrentUser()` then returns `null` THEN `useSyncAnalyticsUser()` calls the mocked `posthog.reset`; WHEN the hook mounts with no user and never identified anyone THEN `posthog.reset` is NOT called. |
| T029 | Implement sign-out reset tracking | implement | EC004 | In `apps/web/src/hooks/use-sync-analytics-user.ts`, track the last identified user id in a `useRef` and call `posthog.reset()` only when transitioning from a tracked id to no user. |
| T030 | Refactor `use-sync-analytics-user.ts` effect dependency | refactor | R003, EC004 | Ensure the `useEffect` dependency array reacts only to `user?.id` changes (not the whole `user` object reference); no behavior change. |
| T031 | Test: bootstrap initializes analytics and wraps the tree | test | R001, R002, R004, R007, R008 | In `apps/web/tests/analytics/main.test.tsx` assert: importing `main.tsx` (with a `#root` element present) calls the mocked `initAnalytics()` before `createRoot(...).render(...)`, and that the rendered tree includes `PostHogProvider` (with `client` set to the exported `posthog` singleton) wrapping `App`. |
| T032 | Implement `main.tsx` wiring | implement | R001, R002, R004, R007, R008 | Modify `apps/web/src/main.tsx` to call `initAnalytics()` before `createRoot(...).render(...)` and wrap `<AppErrorBoundary><App/></AppErrorBoundary>` in `<PostHogProvider client={posthog}>`. |
| T033 | Test: App syncs analytics user | test | R003 | In `apps/web/tests/analytics/App.test.tsx` assert: rendering `App` invokes the mocked `useSyncAnalyticsUser()` hook. |
| T034 | Implement `App.tsx` wiring | implement | R003 | Modify `apps/web/src/App.tsx` to call `useSyncAnalyticsUser()` alongside the existing `useSyncErrorTrackingUser()` call. |
| T035 | Refactor bootstrap wiring | refactor | R001, R002, R003, R004, R007, R008 | Review `main.tsx`/`App.tsx` for readability (ordering of the new calls relative to existing error-tracking wiring); no behavior change. |
| T036 | Add PostHog dependencies | implement | R001, R002, R003, R004, R005, R006, R007, R008 | Modify `apps/web/package.json` to add `posthog-js` and `@posthog/react` as runtime dependencies. |
| T037 | Document new deploy variables | implement | R007, R008 | Modify `.cloudflare/.env.deploy.web.example` to document `VITE_POSTHOG_KEY` and `VITE_POSTHOG_HOST` as optional public values, noting the application runs normally with analytics and flag resolution disabled when `VITE_POSTHOG_KEY` is unset. |
| T038 | Document analytics/flags behavior in README | implement | R001, R005, R006, R008 | Modify `.cloudflare/README.md` to describe the new opt-in analytics/feature-flag variables and the no-op-when-absent behavior. |
