# WEB-002 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: config gate + init when DSN present | test | R001, R002, R007 | In `apps/web/tests/error-tracking/errorTracking.test.ts` assert: WHEN `VITE_ERROR_TRACKING_DSN` is set THEN `initErrorTracking()` calls the mocked `Sentry.init` with that `dsn` and no `integrations`/`defaultIntegrations` override that would disable the SDK's default global error/unhandled-rejection handlers. |
| T002 | Implement `readErrorTrackingConfig` and `initErrorTracking` init branch | implement | R001, R002, R007 | In `apps/web/src/lib/errorTracking.ts`, add `readErrorTrackingConfig()` reading `import.meta.env.VITE_ERROR_TRACKING_DSN` and `initErrorTracking()` calling `Sentry.init({ dsn, ... })` when config is present. |
| T003 | Test: no-op when DSN absent | test | R008 | In `apps/web/tests/error-tracking/errorTracking.test.ts` assert: WHEN `VITE_ERROR_TRACKING_DSN` is absent THEN `initErrorTracking()` returns without calling `Sentry.init` and without throwing. |
| T004 | Implement early-return guard for absent config | implement | R008 | In `apps/web/src/lib/errorTracking.ts`'s `initErrorTracking()`, return immediately when `readErrorTrackingConfig()` is `null`, before any `Sentry.init` call. |
| T005 | Test: environment and release stamped on init | test | R005 | In `apps/web/tests/error-tracking/errorTracking.test.ts` assert: `initErrorTracking()` calls `Sentry.init` with `environment` read from `VITE_ENVIRONMENT` (defaulting to `"production"` when unset) and `release` read from `VITE_RELEASE` (defaulting to `"unknown"` when unset). |
| T006 | Implement environment/release fields in config | implement | R005 | In `apps/web/src/lib/errorTracking.ts`'s `readErrorTrackingConfig()`, add `environment`/`release` fields sourced from `import.meta.env.VITE_ENVIRONMENT`/`import.meta.env.VITE_RELEASE` with the documented defaults, passed through to `Sentry.init`. |
| T007 | Test: init failure isolation | test | NF004 | In `apps/web/tests/error-tracking/errorTracking.test.ts` assert: WHEN the mocked `Sentry.init` throws THEN `initErrorTracking()` does not throw and logs the error instead. |
| T008 | Implement try/catch around `Sentry.init` | implement | NF004 | In `apps/web/src/lib/errorTracking.ts`'s `initErrorTracking()`, wrap the `Sentry.init` call in `try/catch`, logging via `console.error` on failure and never re-throwing. |
| T009 | Test: third-party/extension errors discarded | test | EC003 | In `apps/web/tests/error-tracking/errorTracking.test.ts` assert: `isThirdPartyError()` returns `true` for an event whose only stack frame's `filename` starts with `chrome-extension:`/`moz-extension:`/`safari-extension:`/`safari-web-extension:`, and `false` for an event with an application-origin frame; and that `beforeSend` passed to `Sentry.init` returns `null` for such an event. |
| T010 | Implement `isThirdPartyError` and wire into `beforeSend` | implement | EC003 | In `apps/web/src/lib/errorTracking.ts`, add `isThirdPartyError(event)` checking stack frames against `EXTENSION_URL_SCHEMES`, and call it from the `beforeSend` option passed to `Sentry.init`, returning `null` when it matches. |
| T011 | Test: user PII scrubbed from every report | test | R006, NF003 | In `apps/web/tests/error-tracking/errorTracking.test.ts` assert: WHEN an event's `user` object carries extra fields (e.g. `email`, `username`) THEN the `beforeSend` passed to `Sentry.init` returns an event whose `user` is `{ id }` only; and that `Sentry.init` is called with `sendDefaultPii: false`. |
| T012 | Implement user-field scrubbing in `beforeSend` | implement | R006, NF003 | In `apps/web/src/lib/errorTracking.ts`'s `beforeSend`, reassign `event.user` to `{ id: event.user.id }` when present, and pass `sendDefaultPii: false` to `Sentry.init`. |
| T013 | Refactor `errorTracking.ts` for clarity | refactor | R001, R002, R005, R006, R007, R008, NF003, NF004, EC003 | In `apps/web/src/lib/errorTracking.ts`, extract the `beforeSend` body into a small named function (e.g. `scrubEvent`) and the extension-scheme list into a documented constant, keeping `initErrorTracking()` a short orchestration function; no behavior change. |
| T014 | Test: dependency-free fallback renders | test | EC004 | In `apps/web/tests/error-tracking/ErrorFallback.test.tsx` assert: `ErrorFallback` renders static heading/copy and a reload button with no wrapping provider/context, and that clicking the button calls `window.location.reload()`. |
| T015 | Implement `ErrorFallback` | implement | EC004 | Create `apps/web/src/components/error/ErrorFallback.tsx` rendering only static markup and a `window.location.reload()` button, with no hooks, no `@repo/types` import, and no provider-dependent state. |
| T016 | Test: render error caught, reported, and fallback shown | test | R004 | In `apps/web/tests/error-tracking/AppErrorBoundary.test.tsx` assert: WHEN a child component throws during render THEN `AppErrorBoundary` renders `ErrorFallback` instead of unmounting, and the mocked `Sentry.captureException`/`Sentry.ErrorBoundary` reporting path is exercised. |
| T017 | Implement `AppErrorBoundary` | implement | R004 | Create `apps/web/src/components/error/AppErrorBoundary.tsx` wrapping `Sentry.ErrorBoundary` with `fallback={ErrorFallback}` and `showDialog={false}`. |
| T018 | Refactor error-screen components | refactor | R004, EC004 | Review `ErrorFallback.tsx`/`AppErrorBoundary.tsx` prop typing (`ReactNode` children, no unused props) and ensure `ErrorFallback` has no accidental dependency on props Sentry's `ErrorBoundary` passes to its fallback; no behavior change. |
| T019 | Test: authenticated user attribution | test | R006 | In `apps/web/tests/error-tracking/use-sync-error-tracking-user.test.ts` assert: WHEN `useCurrentUser()` returns a signed-in user THEN `useSyncErrorTrackingUser()` calls the mocked `Sentry.setUser` with `{ id: user.id }` only. |
| T020 | Test: unauthenticated attribution is cleared, not skipped | test | EC005 | In `apps/web/tests/error-tracking/use-sync-error-tracking-user.test.ts` assert: WHEN `useCurrentUser()` returns `null` THEN `useSyncErrorTrackingUser()` calls the mocked `Sentry.setUser(null)` rather than not calling it at all. |
| T021 | Implement `useSyncErrorTrackingUser` | implement | R006, EC005 | Create `apps/web/src/hooks/use-sync-error-tracking-user.ts` calling `useCurrentUser()` and, inside a `useEffect` keyed on the user id, calling `Sentry.setUser({ id })` when present or `Sentry.setUser(null)` when absent. |
| T022 | Refactor `useSyncErrorTrackingUser` effect dependency | refactor | R006, EC005 | Ensure the `useEffect` dependency array reacts only to `user?.id` changes (not the whole `user` object reference) to avoid redundant `setUser` calls; no behavior change. |
| T023 | Test: bootstrap initializes tracking and wraps the tree | test | R001, R002, R004, R007 | In `apps/web/tests/error-tracking/main.test.tsx` assert: importing `main.tsx` (with a `#root` element present) calls the mocked `initErrorTracking()` before `createRoot(...).render(...)`, and that the rendered tree includes `AppErrorBoundary` wrapping `App`. |
| T024 | Implement `main.tsx` wiring | implement | R001, R002, R004, R007 | Modify `apps/web/src/main.tsx` to call `initErrorTracking()` before `createRoot(...).render(...)` and to wrap `<App />` in `<AppErrorBoundary>` inside `QueryClientProvider`. |
| T025 | Test: App syncs error-tracking user | test | R006 | In `apps/web/tests/error-tracking/App.test.tsx` assert: rendering `App` invokes the mocked `useSyncErrorTrackingUser()` hook. |
| T026 | Implement `App.tsx` wiring | implement | R006 | Modify `apps/web/src/App.tsx` to call `useSyncErrorTrackingUser()` before returning `<RouterProvider router={router} />`. |
| T027 | Refactor bootstrap wiring | refactor | R001, R002, R004, R006, R007 | Review `main.tsx`/`App.tsx` for readability (ordering of the new calls relative to existing `publishableKey` guard), no behavior change. |
| T028 | Test: sourcemaps and plugin gated on `SENTRY_AUTH_TOKEN` | test | R003, NF001, EC001 | In `apps/web/tests/error-tracking/viteConfig.test.ts` assert: WHEN `process.env.SENTRY_AUTH_TOKEN` is set THEN the resolved config has `build.sourcemap === true` and includes a `sentry-vite-plugin` entry configured with `sourcemaps.filesToDeleteAfterUpload` and no `errorHandler` override; WHEN unset THEN `build.sourcemap === false` and no such plugin is present. |
| T029 | Implement conditional `sentryVitePlugin` wiring | implement | R003, NF001, EC001 | Modify `apps/web/vite.config.ts` to compute `sourceMapsEnabled = Boolean(process.env.SENTRY_AUTH_TOKEN)`, set `build.sourcemap = sourceMapsEnabled`, and conditionally include `sentryVitePlugin({ org, project, authToken, url, release: { name: process.env.VITE_RELEASE }, sourcemaps: { filesToDeleteAfterUpload: ["**/*.map"] } })` without a custom `errorHandler`. |
| T030 | Test: release identifier matches between build and runtime | test | R005, EC002 | In `apps/web/tests/error-tracking/viteConfig.test.ts` assert: the `release.name` passed to `sentryVitePlugin` reads from the same `VITE_RELEASE` variable name that `apps/web/src/lib/errorTracking.ts`'s `readErrorTrackingConfig()` reads for the runtime `release` field (single source of truth, no divergence). |
| T031 | Implement shared `VITE_RELEASE` usage | implement | R005, EC002 | Confirm/adjust `apps/web/vite.config.ts` and `apps/web/src/lib/errorTracking.ts` both read the literal `VITE_RELEASE` key so the build-time and runtime release identifiers cannot diverge. |
| T032 | Refactor `vite.config.ts` plugin options | refactor | R003, R005, NF001, EC001, EC002 | Extract the `sentryVitePlugin` options object construction into a small named function (e.g. `resolveSentryVitePluginOptions()`) for readability and testability; no behavior change. |
| T033 | Add Sentry dependencies | implement | R001, R002, R003, R004, R006, R007, R008 | Modify `apps/web/package.json` to add `@sentry/react` as a runtime dependency and `@sentry/vite-plugin` as a devDependency. |
| T034 | Document new deploy variables | implement | R007, R008, EC006 | Modify `.cloudflare/.env.deploy.web.example` to document `VITE_ERROR_TRACKING_DSN`, `VITE_RELEASE`, `VITE_ENVIRONMENT` as optional public values, and add comments noting `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_URL` belong only in the per-environment, git-ignored values file. |
| T035 | Document source-map publish behavior in README | implement | R003, EC001, EC002, EC006 | Modify `.cloudflare/README.md` to describe the new opt-in error-tracking variables, that source maps are uploaded and removed from `dist` before deploy when `SENTRY_AUTH_TOKEN` is set, and that a failed upload aborts the deploy. |
