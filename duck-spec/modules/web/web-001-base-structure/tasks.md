# WEB-001 — Web App Base Structure — Tasks

## Tasks

| ID | Title | Description | Covers |
|----|-------|-------------|--------|
| T001 | Add runtime dependencies to `apps/web/package.json` | In `apps/web/package.json`, add `"@tanstack/react-query": "^5.0.0"` and `"zustand": "^4.0.0"` to the `dependencies` field so both libraries are available for import in subsequent tasks. | R003, R004, R005, R009 |
| T002 | Create `.env.example` with `VITE_API_URL` | In `apps/web/.env.example`, add `VITE_API_URL=http://localhost:3000` as the sole entry so the required environment variable is documented and discoverable. | R002 |
| T003 | Create `api/client.ts` — base HTTP client | In `apps/web/src/api/client.ts`, implement the `apiFetch<T>` function that reads `import.meta.env.VITE_API_URL` as the base URL, accepts an optional `token` string on the options object, attaches `Authorization: Bearer <token>` only when the token is present, and throws a typed `ApiError` (with `message` and `status` fields) on any non-2xx HTTP response. | R002, EC002 |
| T004 | Create `api/health.ts` — health endpoint call | In `apps/web/src/api/health.ts`, define the `HealthResponse` interface (`status: string; timestamp: string`) and implement `fetchHealth(): Promise<HealthResponse>` that calls `apiFetch<HealthResponse>('/health')`. | R008 |
| T005 | Create `store/session.store.ts` — session Zustand store | In `apps/web/src/store/session.store.ts`, define an empty `SessionState` interface and create `useSessionStore` using Zustand's `create<SessionState>()` so the store is extensible but has no required fields. | R004 |
| T006 | Create `store/ui.store.ts` — UI Zustand store | In `apps/web/src/store/ui.store.ts`, define an empty `UiState` interface and create `useUiStore` using Zustand's `create<UiState>()` so the store is extensible but has no required fields. | R005 |
| T007 | Create `lib/formatters.ts` — date and currency stubs | In `apps/web/src/lib/formatters.ts`, export `formatDate(date: Date | string): string` (returns `new Date(date).toISOString()`) and `formatCurrency(amount: number, currency?: string): string` (returns `amount.toLocaleString('en-US', { style: 'currency', currency: currency ?? 'USD' })`). No React imports. | R006 |
| T008 | Create `lib/utils.ts` — generic helpers | In `apps/web/src/lib/utils.ts`, export a `noop(): void` placeholder function with no React imports, establishing the module as a non-React utility container. | R007 |
| T009 | Create `components/ui/.gitkeep` and `components/domain/.gitkeep` | Create empty `.gitkeep` files at `apps/web/src/components/ui/.gitkeep` and `apps/web/src/components/domain/.gitkeep` so Git tracks the directories and the layer boundaries are visible in the repository before any component is added. | R001, NF002, NF003 |
| T010 | Create `hooks/useHealth.ts` — React Query wrapper | In `apps/web/src/hooks/useHealth.ts`, implement `useHealth()` that calls `useQuery({ queryKey: ['health'], queryFn: fetchHealth })` from `@tanstack/react-query` and returns the full `UseQueryResult<HealthResponse, ApiError>` so consuming pages receive `data`, `isLoading`, `isError`, and `error`. | R009, EC001 |
| T011 | Create `pages/health/HealthPage.tsx` — reference page | In `apps/web/src/pages/health/HealthPage.tsx`, implement `HealthPage` as a default-exported React component that calls `useHealth()`, renders a loading message when `isLoading` is true, renders a non-crashing error paragraph (showing `error.message`) when `isError` is true, and renders the health `status` and formatted `timestamp` (via `formatDate`) on success. The component must not call any `api/` function directly. | R010, NF001, NF003, NF004, EC001 |
| T012 | Modify `main.tsx` — wire `QueryClientProvider` | In `apps/web/src/main.tsx`, instantiate `const queryClient = new QueryClient()` from `@tanstack/react-query` and wrap the existing `<App />` render with `<QueryClientProvider client={queryClient}>` inside the existing `<StrictMode>` element. | R003 |
| T013 | Modify `App.tsx` — render `HealthPage` | In `apps/web/src/App.tsx`, import `HealthPage` from `./pages/health/HealthPage` and return it as the sole child so the end-to-end health-check example is exercised on boot. | R001, R010, NF004 |
