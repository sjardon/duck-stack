# SUBS-007 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: getMyQuotas API function | test | R011 | In `apps/web/tests/billing/use-quota.test.ts`, write a failing test asserting: WHEN `getMyQuotas(token)` is called THEN `apiFetch` is invoked with `'/billing/quotas/me'` and `{ token }`, and the returned `QuotaUsage[]` matches `response.quotas`. |
| T002 | Implement getMyQuotas | implement | R011 | In `apps/web/src/api/billing.ts`, add `export async function getMyQuotas(token: string): Promise<QuotaUsage[]>` that calls `apiFetch<QuotasResponse>('/billing/quotas/me', { token })` and returns `response.quotas`; import `QuotaUsage` and `QuotasResponse` from `@repo/types`. |
| T003 | Test: useQuota returns entry fields and isLoading | test | R001, R003 | In `apps/web/tests/billing/use-quota.test.ts`, write a failing test asserting: WHEN `useQuota('api_calls')` mounts and the fetch resolves with a matching entry THEN the hook returns `{ count, soft_limit, hard_limit, state, period_end, isLoading: false }` matching the entry. |
| T004 | Test: useQuota returns normal defaults when name absent | test | R002 | In `apps/web/tests/billing/use-quota.test.ts`, write a failing test asserting: WHEN the `GET /billing/quotas/me` response contains no entry for the requested `name` THEN `useQuota` returns `state = 'normal'` and `hard_limit = Infinity`. |
| T005 | Test: useQuota returns normal while loading | test | R001, EC001 | In `apps/web/tests/billing/use-quota.test.ts`, write a failing test asserting: WHEN `useQuota` is rendered before the initial fetch resolves THEN `isLoading = true` and `state = 'normal'`. |
| T006 | Test: useQuota shares a single cache entry across instances | test | NF001 | In `apps/web/tests/billing/use-quota.test.ts`, write a failing test asserting: WHEN two instances of `useQuota` (different names) are mounted simultaneously THEN `apiFetch` is called exactly once for `'/billing/quotas/me'`. |
| T007 | Implement useQuota hook | implement | R001, R002, R003, NF001, NF002, EC001, EC002, EC004 | In `apps/web/src/hooks/useQuota.ts`, implement `export function useQuota(name: QuotaName)` using `useQuery` with key `['billing', 'quotas', 'me']`, `staleTime: 60_000`, `refetchOnWindowFocus: true`; query function calls `getToken()` then `getMyQuotas(token)`; filter the result array for the matching entry; return the typed shape with safe defaults (`state = 'normal'`, `hard_limit = Infinity`) when entry is absent or while loading. |
| T008 | Test: useInvalidateQuotas invalidates the quota cache key | test | R010, EC005 | In `apps/web/tests/billing/use-quota.test.ts`, write a failing test asserting: WHEN the function returned by `useInvalidateQuotas()` is called THEN `queryClient.invalidateQueries` is invoked with `{ queryKey: ['billing', 'quotas', 'me'] }`. |
| T009 | Implement useInvalidateQuotas | implement | R010 | In `apps/web/src/hooks/useQuota.ts`, add `export function useInvalidateQuotas()` that uses `useQueryClient()` and returns a function that calls `queryClient.invalidateQueries({ queryKey: ['billing', 'quotas', 'me'] })`. |
| T010 | Test: QuotaGate renders children when state is normal | test | R006, EC001 | In `apps/web/tests/billing/QuotaGate.test.tsx`, write a failing test asserting: WHEN `useQuota` returns `state = 'normal'` (including while loading) THEN `<QuotaGate>` renders `children` without any warning or blocked decoration. |
| T011 | Test: QuotaGate renders fallbackBlocked when hard_exceeded | test | R004, R007 | In `apps/web/tests/billing/QuotaGate.test.tsx`, write a failing test asserting: WHEN `useQuota` returns `state = 'hard_exceeded'` THEN `<QuotaGate>` renders `fallbackBlocked` prop when provided, and the default blocked message "You have reached the limit of your plan" when not provided. |
| T012 | Test: QuotaGate renders children plus warning when soft_exceeded | test | R005, R007 | In `apps/web/tests/billing/QuotaGate.test.tsx`, write a failing test asserting: WHEN `useQuota` returns `state = 'soft_exceeded'` THEN `<QuotaGate>` renders `children` together with `fallbackWarning` prop when provided, or a default warning element when not provided; children must be visible alongside the warning. |
| T013 | Test: QuotaGate hard_exceeded takes precedence over soft_exceeded | test | R007 | In `apps/web/tests/billing/QuotaGate.test.tsx`, write a failing test asserting: WHEN a `QuotaUsage` entry has `state = 'hard_exceeded'` THEN `<QuotaGate>` renders the blocked branch and not the soft-exceeded or normal branch. |
| T014 | Test: QuotaGate renders upgrade CTA with next-plan link when not on top plan | test | R008 | In `apps/web/tests/billing/QuotaGate.test.tsx`, write a failing test asserting: WHEN the current plan is not the highest-priced plan and `state` is `hard_exceeded` or `soft_exceeded` THEN the default fallback contains a link to `/billing/subscribe?plan=<nextPlan.code>`. |
| T015 | Test: QuotaGate renders top-plan message when on highest plan | test | R009, EC003 | In `apps/web/tests/billing/QuotaGate.test.tsx`, write a failing test asserting: WHEN the current plan is the highest-priced plan in the catalog (or when no plan with a higher price exists after a plan is removed) and `state` is `soft_exceeded` or `hard_exceeded` THEN the default fallback renders "You are on our highest plan — contact us for custom limits" and no upgrade link. |
| T016 | Implement QuotaGate component | implement | R004, R005, R006, R007, R008, R009, EC001, EC002, EC003, EC004 | In `apps/web/src/components/domain/billing/QuotaGate.tsx`, implement `export function QuotaGate({ name, children, fallbackBlocked, fallbackWarning }: QuotaGateProps)`: call `useQuota(name)`, `usePlans()`, and `useMySubscription()`; resolve `nextPlan` by filtering plans with `price > currentPlan.price` sorted ascending and taking the first; render blocked branch when `hard_exceeded`, children-plus-warning branch when `soft_exceeded`, and children-only when `normal`; default fallbacks include an `<UpgradeCTA>` sub-component that renders the upgrade link or the top-plan informational message based on whether `nextPlan` exists. |
