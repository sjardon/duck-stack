# SUBS-001 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Add SubscriptionPlan type to @repo/types | implement | R001, R004 | In `packages/types/src/index.ts` export a new `SubscriptionPlan` interface with all columns from the schema: `id`, `code`, `name`, `description`, `price`, `currency`, `interval` (`'month' \| 'year'`), `features` (`string[]`), `is_active`, `provider_plan_id` (`string \| null`), `created_at`, `updated_at` |
| T002 | Create subscription_plans migration | implement | R001, R003, R004 | In `apps/services/supabase/migrations/20260623300000_subscription_plans.sql` write a `CREATE TABLE subscription_plans` DDL with all required columns, `CHECK (interval IN ('month','year'))`, `CHECK (price >= 0)`, and an idempotent `INSERT … ON CONFLICT (code) DO NOTHING` seeding `free` (price=0), `pro`, and `business` rows |
| T003 | Create subscriptionPlan.entity.ts | implement | R001 | In `apps/services/src/modules/subscriptions/entities/subscriptionPlan.entity.ts` declare the `SubscriptionPlanEntity` interface mirroring the DB row; do not import from `@repo/types` — this is an internal backend type |
| T004 | Create ISubscriptionPlanRepository interface | implement | R002 | In `apps/services/src/modules/subscriptions/repositories/interfaces/iSubscriptionPlanRepository.ts` declare `ISubscriptionPlanRepository` with a single method `listActive(): Promise<SubscriptionPlanEntity[]>` |
| T005 | Test: SubscriptionPlanDBRepository.listActive | test | R001, R002, R003, R004, EC001, EC002 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/subscriptionPlanDBRepository.test.ts` asserting: WHEN `listActive()` is called THEN it issues a SELECT with `is_active = true` and `ORDER BY price ASC`; WHEN a plan has `price = 0` THEN it is included; WHEN a plan has `is_active = false` THEN it is excluded |
| T006 | Implement SubscriptionPlanDBRepository | implement | R001, R002, R003, R004, EC001, EC002 | In `apps/services/src/modules/subscriptions/repositories/subscriptionPlanDBRepository.ts` implement `SubscriptionPlanDBRepository` with a `listActive()` method that executes a parameterized `SELECT id, code, name, description, price, currency, interval, features, is_active, provider_plan_id, created_at, updated_at FROM subscription_plans WHERE is_active = true ORDER BY price ASC` via the `postgres.js` singleton and logs query latency |
| T007 | Test: ListPlansUseCase | test | R002, EC001, EC002 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/listPlansUseCase.test.ts` asserting: WHEN `execute()` is called THEN it returns all items from `repo.listActive()`; WHEN the repository returns an empty array THEN the use case returns an empty array; WHEN the repository returns a plan with `price = 0` THEN it is included in the result |
| T008 | Implement ListPlansUseCase | implement | R002, EC001, EC002 | In `apps/services/src/modules/subscriptions/useCases/listPlansUseCase.ts` implement `ListPlansUseCase` that accepts `ISubscriptionPlanRepository` via constructor and whose `execute()` method calls `repo.listActive()` and returns the result |
| T009 | Test: listPlansHandler | test | R002, NF001 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/listPlansHandler.test.ts` asserting: WHEN `GET /billing/plans` is called without an Authorization header THEN the response is HTTP 200 with body `{ data: SubscriptionPlan[] }`; WHEN the route is invoked THEN no `preHandler` auth check is triggered |
| T010 | Implement listPlansHandler | implement | R002, NF001 | In `apps/services/src/modules/subscriptions/handlers/listPlansHandler.ts` implement `listPlansHandler` that instantiates `SubscriptionPlanDBRepository` and `ListPlansUseCase` at module scope, calls `useCase.execute()`, and replies with `{ data: result }` |
| T011 | Create subscriptions routes.ts | implement | R002 | In `apps/services/src/modules/subscriptions/routes.ts` create a `fastify-plugin` that registers `fastify.get('/billing/plans', listPlansHandler)` with no `preHandler` |
| T012 | Register subscriptionsRoutes in app.ts | implement | R002 | In `apps/services/src/app.ts` import the new `subscriptionsRoutes` plugin and register it with `await fastify.register(subscriptionsRoutes)` after `billingRoutes` |
