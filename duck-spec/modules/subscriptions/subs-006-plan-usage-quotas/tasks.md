# SUBS-006 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Add quota types to @repo/types | implement | R001, R008, R009 | In `packages/types/src/index.ts` export `QuotaName`, `QuotaThresholds`, `QuotaState`, `QuotaUsage`, and `QuotasResponse` interfaces/types |
| T002 | Add PLAN_QUOTAS mapping to entitlements | implement | R001 | In `apps/services/src/modules/subscriptions/entitlements.ts` add and export `PLAN_QUOTAS: Record<string, Record<string, QuotaThresholds>>` with thresholds for free, pro, and business plans |
| T003 | Test: PLAN_QUOTAS structure | test | R001 | In `apps/services/tests/unit/modules/subscriptions/entitlements.test.ts` add assertions: WHEN `PLAN_QUOTAS` is imported THEN each plan key maps to an object with `api_requests` containing numeric `soft_limit` and `hard_limit` where `soft_limit < hard_limit` |
| T004 | Create usage_counters migration | implement | R002 | Create `apps/services/supabase/migrations/20260626000000_usage_counters.sql` with the `usage_counters` table, scope check constraint, unique constraint on `(user_id, org_id, quota_name, period_start)`, and partial indexes on `(user_id, quota_name, period_start)` and `(org_id, quota_name, period_start)` |
| T005 | Create UsageCounterEntity | implement | R002 | Create `apps/services/src/modules/subscriptions/entities/usageCounterEntity.ts` exporting `UsageCounterEntity` interface mirroring the `usage_counters` table columns |
| T006 | Create IUsageCounterRepository interface | implement | R002, R003, NF001 | Create `apps/services/src/modules/subscriptions/repositories/interfaces/iUsageCounterRepository.ts` exporting `IUsageCounterRepository` with `incrementAndReturn(userId, orgId, quotaName, periodStart): Promise<number>` and `findCount(userId, orgId, quotaName, periodStart): Promise<number>` |
| T007 | Test: UsageCounterDBRepository.incrementAndReturn | test | R002, R003, NF001 | In `apps/services/tests/unit/modules/subscriptions/repositories/usageCounterDBRepository.test.ts` write tests asserting: WHEN `incrementAndReturn` is called THEN it issues INSERT … ON CONFLICT DO UPDATE with count + 1 and RETURNING count; WHEN called on a new row THEN returns 1; WHEN called on an existing row THEN returns previous count + 1 |
| T008 | Implement UsageCounterDBRepository | implement | R002, R003, NF001 | Create `apps/services/src/modules/subscriptions/repositories/usageCounterDBRepository.ts` implementing `IUsageCounterRepository`: `incrementAndReturn` executes the atomic upsert SQL; `findCount` executes SELECT returning 0 if no row exists; both follow the established logging pattern with `logger.info` on success and `logger.error` + re-throw on failure |
| T009 | Add QuotaExceededError to shared errors | implement | R004 | In `apps/services/src/shared/errors.ts` add `QuotaExceededError extends DomainError` with constructor `(quotaName: string, count: number, soft_limit: number, hard_limit: number, period_end: string)`, `statusCode = 429`, `code = 'QUOTA_EXCEEDED'`, and public fields for the extra quota properties |
| T010 | Test: QuotaExceededError structure | test | R004 | In `apps/services/tests/unit/shared/errors.test.ts` add test cases: WHEN `QuotaExceededError` is constructed THEN `statusCode` is 429, `code` is `QUOTA_EXCEEDED`, and the extra fields (`quotaName`, `count`, `soft_limit`, `hard_limit`, `period_end`) are accessible as public properties |
| T011 | Extend errorHandler for QuotaExceededError | implement | R004 | In `apps/services/src/shared/plugins/errorHandler.ts` add a check: WHEN the caught error is `instanceof QuotaExceededError` THEN reply with HTTP 429 and body `{ code, message, quota: err.quotaName, count: err.count, soft_limit: err.soft_limit, hard_limit: err.hard_limit, period_end: err.period_end }` before falling through to the generic `DomainError` branch |
| T012 | Test: ensureActiveSubscription — existing subscription | test | R007, EC006, EC007 | In `apps/services/tests/unit/modules/subscriptions/helpers/ensureActiveSubscription.test.ts` write tests: WHEN `findActiveOrWithinPeriodByScope` returns a subscription THEN helper returns it without calling `create`; WHEN both user and org concurrent inserts race THEN unique-constraint error triggers a retry and returns the existing row |
| T013 | Implement ensureActiveSubscription helper | implement | R007, EC006, EC007 | Create `apps/services/src/modules/subscriptions/helpers/ensureActiveSubscription.ts` exporting `ensureActiveSubscription(repo: ISubscriptionRepository, userId: string, orgId: string | null): Promise<SubscriptionWithPlanEntity>`; lazily creates a synthetic free subscription with `provider = 'internal'`, `current_period_start = date_trunc('month', now())`, `current_period_end = current_period_start + interval '1 month'`; catches unique-constraint violation (postgres error code `23505`) and retries the find once |
| T014 | Test: RequireQuotaUseCase — unlimited quota | test | R006, EC004 | In `apps/services/tests/unit/modules/subscriptions/useCases/requireQuotaUseCase.test.ts` write test: WHEN the plan does not define the quota name THEN `execute` resolves without calling `incrementAndReturn` |
| T015 | Test: RequireQuotaUseCase — within limit | test | R003, R005, EC001, EC002, EC005 | In same test file add: WHEN `incrementAndReturn` returns a count <= `hard_limit` THEN `execute` resolves; WHEN `orgId != null` THEN counter is incremented against `org_id` (userId=null); WHEN subscription is `past_due` THEN plan thresholds are evaluated; WHEN subscription is `canceled` with future period end THEN plan thresholds are evaluated |
| T016 | Test: RequireQuotaUseCase — hard limit exceeded | test | R004, EC003 | In same test file add: WHEN `incrementAndReturn` returns a count > `hard_limit` THEN `execute` throws `QuotaExceededError` with correct `quotaName`, `count`, `soft_limit`, `hard_limit`, and `period_end` |
| T017 | Test: RequireQuotaUseCase — lazy free subscription | test | R007 | In same test file add: WHEN `findActiveOrWithinPeriodByScope` returns null THEN `ensureActiveSubscription` is invoked and the resulting subscription's plan thresholds are used |
| T018 | Implement RequireQuotaUseCase | implement | R003, R004, R005, R006, R007, EC001, EC002, EC003, EC004, EC005, NF001 | Create `apps/services/src/modules/subscriptions/useCases/requireQuotaUseCase.ts` exporting `RequireQuotaUseCase` with constructor `(subscriptionRepo: ISubscriptionRepository, counterRepo: IUsageCounterRepository)` and `execute(userId: string, orgId: string | null, quotaName: string): Promise<void>`; calls `ensureActiveSubscription`, looks up `PLAN_QUOTAS`, determines effective scope per EC005, calls `counterRepo.incrementAndReturn`, throws `QuotaExceededError` when count exceeds `hard_limit` |
| T019 | Test: requireQuota preHandler — allow path | test | R003, R005, R006 | In `apps/services/tests/unit/modules/subscriptions/plugins/requireQuota.test.ts` write tests: WHEN use case resolves THEN preHandler resolves; WHEN quota is undefined for plan THEN preHandler resolves without throwing |
| T020 | Test: requireQuota preHandler — deny path | test | R004, NF001 | In same test file add: WHEN use case throws `QuotaExceededError` THEN preHandler propagates it; WHEN called twice on same request THEN `execute` is called twice (no per-request caching for quota enforcement — each request must always count) |
| T021 | Implement requireQuota preHandler factory | implement | R003, R004, R005, R006, R007, EC005, NF002 | Create `apps/services/src/modules/subscriptions/plugins/requireQuota.ts` exporting `requireQuota(name: string): (request: FastifyRequest) => Promise<void>`; create module-scope `SubscriptionDBRepository` and `UsageCounterDBRepository` singletons; instantiate `RequireQuotaUseCase` at module scope; resolve scope (EC005: orgId-first), call `useCase.execute`; include `FastifyRequest` augmentation declaration for any request-level typing |
| T022 | Test: GetMyQuotasUseCase — normal state | test | R008, R009 | In `apps/services/tests/unit/modules/subscriptions/useCases/getMyQuotasUseCase.test.ts` write test: WHEN active subscription has a plan with defined quotas and `findCount` returns a count below `soft_limit` THEN `execute` returns an array with `state = 'normal'` and correct `name`, `count`, `soft_limit`, `hard_limit`, `period_start`, `period_end` |
| T023 | Test: GetMyQuotasUseCase — soft and hard exceeded states | test | R009 | In same test file add: WHEN count > soft_limit and <= hard_limit THEN `state = 'soft_exceeded'`; WHEN count > hard_limit THEN `state = 'hard_exceeded'` |
| T024 | Test: GetMyQuotasUseCase — no subscription | test | R008, EC001 | In same test file add: WHEN `findActiveOrWithinPeriodByScope` returns null THEN `execute` returns quotas based on the free plan thresholds with a synthetic period derived from the current month |
| T025 | Test: GetMyQuotasUseCase — org scope | test | R008, EC005 | In same test file add: WHEN `orgId != null` THEN `findCount` is called with `userId=null` and the provided `orgId` |
| T026 | Implement GetMyQuotasUseCase | implement | R008, R009, EC001, EC002, EC005, NF003 | Create `apps/services/src/modules/subscriptions/useCases/getMyQuotasUseCase.ts` exporting `GetMyQuotasUseCase` with constructor `(subscriptionRepo: ISubscriptionRepository, counterRepo: IUsageCounterRepository)` and `execute(userId: string, orgId: string | null): Promise<QuotaUsage[]>`; resolves active subscription (falls back to free plan with a synthetic period if null); applies EC005 scope rule; calls `counterRepo.findCount` for each quota name; derives `state` per R009 |
| T027 | Test: getMyQuotasHandler — authenticated success | test | R008, R010 | In `apps/services/tests/unit/modules/subscriptions/handlers/getMyQuotasHandler.test.ts` write test: WHEN `userId` is set and use case returns quotas THEN handler replies with `{ quotas: [...] }` at HTTP 200 |
| T028 | Test: getMyQuotasHandler — unauthenticated | test | R010 | In same test file add: WHEN `userId` is undefined THEN `requireAuth` (mocked) throws `UnauthorizedError` and response is HTTP 401 |
| T029 | Implement getMyQuotasHandler | implement | R008, R009, R010, NF003 | Create `apps/services/src/modules/subscriptions/handlers/getMyQuotasHandler.ts` exporting `getMyQuotasHandler(request, reply)`; instantiates `SubscriptionDBRepository` and `UsageCounterDBRepository` using the `db` singleton; constructs `GetMyQuotasUseCase`; calls `execute(request.userId!, request.orgId ?? null)`; replies with `{ quotas }` |
| T030 | Register GET /billing/quotas/me in routes | implement | R008, R010 | In `apps/services/src/modules/subscriptions/routes.ts` add `fastify.get('/billing/quotas/me', { preHandler: requireAuth }, getMyQuotasHandler)` |
| T031 | Test: R011 natural period rollover | test | R011 | In `apps/services/tests/unit/modules/subscriptions/repositories/usageCounterDBRepository.test.ts` add test: WHEN `incrementAndReturn` is called with a different `periodStart` than an existing row for the same scope and quota THEN the SQL inserts a new row (mocked sql returns count = 1) rather than updating the existing one |
