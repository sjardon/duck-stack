# SUBS-002 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Implement Subscription and related types in @repo/types | implement | R012 | In `packages/types/src/index.ts` export `SubscriptionStatusValue`, `Subscription`, `CreateSubscriptionInput`, and `CancelSubscriptionInput` interfaces |
| T002 | Create subscriptions migration | implement | R001, R002 | In `apps/services/supabase/migrations/20260624000000_subscriptions.sql` write `CREATE TABLE subscriptions` with all columns, CHECK constraint on `status`, and two partial unique indexes (`subscriptions_active_per_user`, `subscriptions_active_per_org`) WHERE `status NOT IN ('canceled','expired')` |
| T003 | Create SubscriptionEntity | implement | R001 | In `apps/services/src/modules/subscriptions/entities/subscriptionEntity.ts` define the `SubscriptionEntity` interface mirroring all `subscriptions` table columns |
| T004 | Create ISubscriptionRepository interface | implement | R001, R002, R006, R007, R009, R010, R011 | In `apps/services/src/modules/subscriptions/repositories/interfaces/iSubscriptionRepository.ts` declare `ISubscriptionRepository` with methods: `findActiveByScopeStatus`, `findByIdAndScope`, `findPlanByCode`, `create`, `setCancelAtPeriodEnd`, and `cancelImmediately` |
| T005 | Test: SubscriptionDBRepository | test | R001, R002, R006, R007, R009, R010, R011 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/subscriptionDBRepository.test.ts` asserting: WHEN `findActiveByScopeStatus` is called THEN it returns only rows WHERE `status NOT IN ('canceled','expired')`; WHEN `findPlanByCode` is called with an inactive code THEN it returns null; WHEN `create` is called THEN it returns the inserted row; WHEN `setCancelAtPeriodEnd` is called THEN it sets `cancel_at_period_end = true`; WHEN `cancelImmediately` is called THEN it sets `status = 'canceled'` and `canceled_at` |
| T006 | Implement SubscriptionDBRepository | implement | R001, R002, R006, R007, R009, R010, R011 | In `apps/services/src/modules/subscriptions/repositories/subscriptionDBRepository.ts` implement `SubscriptionDBRepository` using the `postgres.js` singleton: `findActiveByScopeStatus` queries by scope WHERE `status NOT IN ('canceled','expired')`; `findByIdAndScope` queries by `id` and scope; `findPlanByCode` queries `subscription_plans WHERE code = $1 AND is_active = true`; `create` inserts a new row RETURNING all columns; `setCancelAtPeriodEnd` sets `cancel_at_period_end = true` and `updated_at = now()`; `cancelImmediately` sets `status = 'canceled'`, `canceled_at = now()`, and `updated_at = now()`; each method logs query latency |
| T007 | Create CreateSubscriptionBodySchema DTO | implement | R003, NF001 | In `apps/services/src/modules/subscriptions/dtos/createSubscriptionDto.ts` define and export `CreateSubscriptionBodySchema` as a Zod object with `planCode: z.string().min(1)` and export `CreateSubscriptionBodyType` |
| T008 | Create CancelSubscriptionBodySchema DTO | implement | R008, NF001 | In `apps/services/src/modules/subscriptions/dtos/cancelSubscriptionDto.ts` define and export `CancelSubscriptionBodySchema` as a Zod object with `atPeriodEnd: z.boolean().default(true)` and export `CancelSubscriptionBodyType` |
| T009 | Test: CreateSubscriptionUseCase | test | R003, R004, R005, R006, R007, EC002, EC003 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/createSubscriptionUseCase.test.ts` asserting: WHEN `planCode` does not match an active plan THEN throws `ValidationError` 400 (R007); WHEN scope already has a non-terminal subscription THEN throws `ValidationError` 409 (R006); WHEN previous subscription is canceled or expired THEN creates successfully (EC003); WHEN plan is free THEN creates with `status = 'active'` and no provider call (R004, EC002); WHEN plan is paid THEN calls provider, creates with `status = 'pending'`, returns `checkoutUrl` (R005) |
| T010 | Implement CreateSubscriptionUseCase | implement | R003, R004, R005, R006, R007, EC002, EC003, NF002 | In `apps/services/src/modules/subscriptions/useCases/createSubscriptionUseCase.ts` implement `CreateSubscriptionUseCase` with constructor receiving `ISubscriptionRepository` and `PaymentProvider`; `execute(userId, orgId, input)` resolves plan by code (400 if absent), checks for existing active subscription (409 if present), branches on free vs. paid plan — free creates `status = 'active'` with null `provider_subscription_id`; paid generates a UUID, calls `provider.createSubscription(plan.provider_plan_id, uuid)`, creates row with `status = 'pending'` and returned `provider_subscription_id`, returns `{ checkoutUrl, subscriptionId }` |
| T011 | Test: CancelSubscriptionUseCase | test | R008, R009, R010, EC001 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/cancelSubscriptionUseCase.test.ts` asserting: WHEN subscription not found or belongs to different scope THEN throws `NotFoundError` 404; WHEN `atPeriodEnd = true` THEN sets `cancel_at_period_end = true` and calls provider (R009); WHEN `atPeriodEnd = false` THEN sets `status = 'canceled'` and calls provider (R010); WHEN subscription is `pending` and provider returns 404 THEN treats as success (EC001) |
| T012 | Implement CancelSubscriptionUseCase | implement | R008, R009, R010, EC001, NF002 | In `apps/services/src/modules/subscriptions/useCases/cancelSubscriptionUseCase.ts` implement `CancelSubscriptionUseCase` with constructor receiving `ISubscriptionRepository` and `PaymentProvider`; `execute(userId, orgId, subscriptionId, input)` calls `findByIdAndScope` (throws `NotFoundError` if absent), branches on `atPeriodEnd`: true calls `setCancelAtPeriodEnd` then `provider.cancelSubscription`; false calls `cancelImmediately` then `provider.cancelSubscription`; catches `ProviderError` where the underlying status was HTTP 404 and swallows it (EC001), returning the updated subscription |
| T013 | Test: GetMySubscriptionUseCase | test | R011 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/getMySubscriptionUseCase.test.ts` asserting: WHEN a non-terminal subscription exists for scope THEN returns the entity; WHEN no non-terminal subscription exists THEN returns null |
| T014 | Implement GetMySubscriptionUseCase | implement | R011, NF002 | In `apps/services/src/modules/subscriptions/useCases/getMySubscriptionUseCase.ts` implement `GetMySubscriptionUseCase` with constructor receiving `ISubscriptionRepository`; `execute(userId, orgId)` calls `findActiveByScopeStatus` and returns the entity or `null` |
| T015 | Test: createSubscriptionHandler | test | R003, NF001 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/createSubscriptionHandler.test.ts` asserting: WHEN body is missing `planCode` THEN replies 400 `VALIDATION_ERROR` without calling the use case (NF001); WHEN body is valid THEN calls use case and returns the result |
| T016 | Implement createSubscriptionHandler | implement | R003, NF001 | In `apps/services/src/modules/subscriptions/handlers/createSubscriptionHandler.ts` implement a thin Fastify handler: parse body with `CreateSubscriptionBodySchema.safeParse`; on failure reply 400 with `VALIDATION_ERROR`; instantiate `SubscriptionDBRepository(db)` and `CreateSubscriptionUseCase(repo, resolveProvider())`; call `execute(request.userId!, request.orgId ?? null, parsed.data)`; reply with the result |
| T017 | Test: cancelSubscriptionHandler | test | R008, NF001 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/cancelSubscriptionHandler.test.ts` asserting: WHEN body fails Zod validation THEN replies 400 `VALIDATION_ERROR` without calling the use case (NF001); WHEN body is valid THEN calls use case with `atPeriodEnd` and replies with the updated subscription |
| T018 | Implement cancelSubscriptionHandler | implement | R008, NF001 | In `apps/services/src/modules/subscriptions/handlers/cancelSubscriptionHandler.ts` implement a thin Fastify handler: read `:id` param, parse body with `CancelSubscriptionBodySchema.safeParse`; on failure reply 400 with `VALIDATION_ERROR`; instantiate repo and `CancelSubscriptionUseCase(repo, resolveProvider())`; call `execute(request.userId!, request.orgId ?? null, id, parsed.data)`; reply with `{ subscription }` |
| T019 | Test: getMySubscriptionHandler | test | R011 | Write failing unit tests in `apps/services/tests/unit/modules/subscriptions/getMySubscriptionHandler.test.ts` asserting: WHEN use case returns a subscription THEN replies with `{ subscription }`; WHEN use case returns null THEN replies with `{ subscription: null }` |
| T020 | Implement getMySubscriptionHandler | implement | R011 | In `apps/services/src/modules/subscriptions/handlers/getMySubscriptionHandler.ts` implement a thin Fastify handler: instantiate repo and `GetMySubscriptionUseCase(repo)`; call `execute(request.userId!, request.orgId ?? null)`; reply with `{ subscription }` (where `subscription` is the entity or `null`) |
| T021 | Register new routes in subscriptions routes.ts | implement | R003, R008, R011 | In `apps/services/src/modules/subscriptions/routes.ts` import `requireAuth`, `createSubscriptionHandler`, `cancelSubscriptionHandler`, and `getMySubscriptionHandler`; register `POST /billing/subscriptions` with `preHandler: requireAuth`, `POST /billing/subscriptions/:id/cancel` with `preHandler: requireAuth`, and `GET /billing/subscriptions/me` with `preHandler: requireAuth` |
