# SUBS-008 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: migration adds trialing status and trial_ends_at | test | R003 | In `tests/unit/modules/subscriptions/repositories/subscriptionDBRepository.test.ts` add a placeholder assertion documenting that the migration file `20260701000000_subscriptions_trial.sql` must contain the altered CHECK constraint and the `trial_ends_at` column — WHEN the migration is applied THEN `status = 'trialing'` and a non-null `trial_ends_at` are accepted by the DB |
| T002 | Implement: subscriptions_trial migration | implement | R003 | Create `apps/services/supabase/migrations/20260701000000_subscriptions_trial.sql` with `ALTER TABLE subscriptions DROP CONSTRAINT subscriptions_status_check, ADD CONSTRAINT subscriptions_status_check CHECK (status IN ('pending', 'active', 'past_due', 'canceled', 'expired', 'trialing'))` and `ADD COLUMN trial_ends_at timestamptz` |
| T003 | Test: SubscriptionStatusValue includes trialing | test | NF005 | In `packages/types/src/index.ts` related type tests (or a snapshot), assert WHEN `status = 'trialing'` is assigned to `SubscriptionStatusValue` THEN TypeScript does not produce a type error; assert `Subscription` has `trial_ends_at: string \| null` and optional `days_remaining?: number` |
| T004 | Implement: extend @repo/types with trialing | implement | NF005 | In `packages/types/src/index.ts` add `'trialing'` to the `SubscriptionStatusValue` union; add `trial_ends_at: string \| null` and `days_remaining?: number` to `Subscription` |
| T005 | Test: subscriptionsConfig exposes SIGNUP_MODE and FREE_TRIAL_DAYS | test | R001, R002 | In `tests/unit/shared/configs/subscriptionsConfig.test.ts` assert WHEN `SIGNUP_MODE` is absent THEN `signupMode === 'freemium'`; WHEN `SIGNUP_MODE = 'free_trial'` THEN `signupMode === 'free_trial'`; WHEN `FREE_TRIAL_DAYS` is absent THEN `freeTrialDays === 14`; WHEN `FREE_TRIAL_DAYS = '7'` THEN `freeTrialDays === 7` |
| T006 | Implement: extend subscriptionsConfig with signupMode and freeTrialDays | implement | R001, R002 | In `apps/services/src/shared/configs/subscriptionsConfig.ts` add `signupMode: (env.SIGNUP_MODE ?? 'freemium') as 'freemium' \| 'free_trial'` and `freeTrialDays: parseInt(env.FREE_TRIAL_DAYS ?? '14', 10)` to the exported config object |
| T007 | Test: TrialExpiredError shape | test | R007 | In `tests/unit/shared/errors.test.ts` assert WHEN `new TrialExpiredError('2026-08-01T00:00:00Z')` is constructed THEN `error.statusCode === 403`, `error.code === 'TRIAL_EXPIRED'`, and `error.trialEndedAt === '2026-08-01T00:00:00Z'` |
| T008 | Implement: TrialExpiredError in shared/errors.ts | implement | R007 | In `apps/services/src/shared/errors.ts` add `export class TrialExpiredError extends DomainError` with constructor `(trialEndedAt: string)` calling `super('TRIAL_EXPIRED', '...', 403)` and assigning `this.trialEndedAt = trialEndedAt` |
| T009 | Implement: errorHandler serializes TrialExpiredError | implement | R007 | In `apps/services/src/shared/plugins/errorHandler.ts` detect `TrialExpiredError` instances and include `trialEndedAt` alongside `code` and `message` in the 403 response body, following the `QuotaExceededError` serialization pattern |
| T010 | Implement: extend SubscriptionEntity with trial_ends_at | implement | R003, NF005 | In `apps/services/src/modules/subscriptions/entities/subscriptionEntity.ts` add `trial_ends_at: string \| null` to the `SubscriptionEntity` interface |
| T011 | Test: ISubscriptionRepository new methods and widened types | test | R004, R006, NF002 | In `tests/unit/modules/subscriptions/repositories/subscriptionDBRepository.test.ts` add tests asserting WHEN `findMostExpensiveActivePlan` is called THEN it returns the plan with the highest price among active plans (or null if none); WHEN `transitionExpiredTrials` is called THEN it issues an UPDATE and returns the transitioned entity or null |
| T012 | Implement: extend ISubscriptionRepository interface | implement | R004, R006, NF002 | In `apps/services/src/modules/subscriptions/repositories/interfaces/iSubscriptionRepository.ts` add `findMostExpensiveActivePlan(): Promise<SubscriptionPlanEntity \| null>` and `transitionExpiredTrials(userId: string, orgId: string \| null): Promise<SubscriptionEntity \| null>`; widen `CreateSubscriptionData.status` to include `'trialing'`; add optional `trial_ends_at?: string \| null` to `CreateSubscriptionData` |
| T013 | Implement: findMostExpensiveActivePlan in SubscriptionDBRepository | implement | R004, NF002 | In `apps/services/src/modules/subscriptions/repositories/subscriptionDBRepository.ts` implement `findMostExpensiveActivePlan` with `SELECT ... FROM subscription_plans WHERE is_active = true ORDER BY price DESC LIMIT 1` |
| T014 | Implement: transitionExpiredTrials in SubscriptionDBRepository | implement | R006, NF004 | In `apps/services/src/modules/subscriptions/repositories/subscriptionDBRepository.ts` implement `transitionExpiredTrials(userId, orgId)` with `UPDATE subscriptions SET status = 'expired', updated_at = now() WHERE status = 'trialing' AND trial_ends_at < now() AND (scope predicate) RETURNING ...`; return the updated row or null if no row matched |
| T015 | Implement: update SubscriptionDBRepository.create to pass trial_ends_at | implement | R004 | In `apps/services/src/modules/subscriptions/repositories/subscriptionDBRepository.ts` update the `create` INSERT statement to include `trial_ends_at` from `input.trial_ends_at` and return it in the RETURNING clause |
| T016 | Test: CreateTrialSubscriptionUseCase — happy path | test | R004, NF001, NF002 | In `tests/unit/modules/subscriptions/useCases/createTrialSubscriptionUseCase.test.ts` assert WHEN `execute(userId)` is called and `findMostExpensiveActivePlan` returns a plan THEN `create` is called with `status = 'trialing'`, `plan_id` matching the most expensive plan, and `trial_ends_at = now() + freeTrialDays` |
| T017 | Test: CreateTrialSubscriptionUseCase — no active plan | test | NF003 | In `tests/unit/modules/subscriptions/useCases/createTrialSubscriptionUseCase.test.ts` assert WHEN `findMostExpensiveActivePlan` returns null THEN `create` is never called and no error is thrown (silent fail) |
| T018 | Test: CreateTrialSubscriptionUseCase — duplicate webhook idempotency | test | NF001, EC008 | In `tests/unit/modules/subscriptions/useCases/createTrialSubscriptionUseCase.test.ts` assert WHEN `create` throws a PG 23505 error THEN the use case catches it and returns without re-throwing |
| T019 | Implement: CreateTrialSubscriptionUseCase | implement | R004, NF001, NF002, NF003 | Create `apps/services/src/modules/subscriptions/useCases/createTrialSubscriptionUseCase.ts` with `execute(userId: string): Promise<void>` implementing: (1) call `findMostExpensiveActivePlan`, log error and return if null; (2) compute `trialEndsAt = now() + config.freeTrialDays`; (3) call `create` with `status: 'trialing'`, `trial_ends_at`, `current_period_start: now()`, `current_period_end: trialEndsAt`; (4) catch PG 23505 and return silently |
| T020 | Test: ClerkSyncRepository.upsertUser returns internal UUID | test | R004 | In `tests/unit/modules/webhooks/repositories/clerkSyncRepository.test.ts` assert WHEN `upsertUser` is called THEN it returns `{ id: string }` containing the internal UUID of the upserted user |
| T021 | Implement: ClerkSyncRepository.upsertUser returns { id } | implement | R004 | In `apps/services/src/modules/webhooks/repositories/clerkSyncRepository.ts` update the `upsertUser` INSERT to use `INSERT ... ON CONFLICT ... DO UPDATE ... RETURNING id` and return `{ id: string }` instead of void |
| T022 | Test: dispatchClerkEvent invokes trial creation on user.created in free_trial mode | test | R004, R005, EC008 | In `tests/unit/modules/webhooks/clerk/clerkEventHandlers.test.ts` assert WHEN `signupMode = 'free_trial'` and event is `user.created` THEN `CreateTrialSubscriptionUseCase.execute` is called with the userId; WHEN `signupMode = 'freemium'` THEN `CreateTrialSubscriptionUseCase.execute` is NOT called |
| T023 | Implement: extend dispatchClerkEvent to invoke CreateTrialSubscriptionUseCase | implement | R004, R005 | In `apps/services/src/modules/webhooks/clerk/clerkEventHandlers.ts` add `subscriptionRepo?: ISubscriptionRepository` parameter to `dispatchClerkEvent`; in `user.created` branch, after `handleUserUpsert` resolves to `{ id }`, when `signupMode === 'free_trial'` instantiate and call `CreateTrialSubscriptionUseCase.execute(id)` |
| T024 | Implement: clerk webhook routes passes subscriptionRepo | implement | R004 | In `apps/services/src/modules/webhooks/clerk/routes.ts` instantiate `SubscriptionDBRepository(db)` at module scope and pass it as the third argument to `dispatchClerkEvent` |
| T025 | Test: ensureActiveSubscription skips free-plan creation in free_trial mode | test | R009 | In `tests/unit/modules/subscriptions/helpers/ensureActiveSubscription.test.ts` assert WHEN `signupMode = 'free_trial'` and `findActiveOrWithinPeriodByScope` returns null THEN `create` is NOT called and the helper returns null |
| T026 | Implement: ensureActiveSubscription mode-aware null return | implement | R009 | In `apps/services/src/modules/subscriptions/helpers/ensureActiveSubscription.ts` at the top of the function, if `subscriptionsConfig.signupMode === 'free_trial'` return the existing subscription or null immediately without creating a free plan |
| T027 | Implement: requireQuotaUseCase handles null from ensureActiveSubscription | implement | R009 | In `apps/services/src/modules/subscriptions/useCases/requireQuotaUseCase.ts` handle the case where `ensureActiveSubscription` returns null (free_trial mode, no subscription) by treating it as an unlimited / no-quota scope and returning without enforcing limits |
| T028 | Implement: getMyQuotasUseCase handles null from ensureActiveSubscription | implement | R009 | In `apps/services/src/modules/subscriptions/useCases/getMyQuotasUseCase.ts` handle the case where `ensureActiveSubscription` returns null by returning an empty quotas array |
| T029 | Test: GetMySubscriptionUseCase includes trial fields and transitions expired trials | test | R006, R010 | In `tests/unit/modules/subscriptions/useCases/getMySubscriptionUseCase.test.ts` assert WHEN subscription has `status = 'trialing'` THEN the use case calls `transitionExpiredTrials` first and returns `trial_ends_at` and `days_remaining >= 0`; WHEN `trial_ends_at` is in the past THEN after transition `status` is `expired` |
| T030 | Implement: GetMySubscriptionUseCase calls transitionExpiredTrials and computes trial fields | implement | R006, R010 | In `apps/services/src/modules/subscriptions/useCases/getMySubscriptionUseCase.ts` call `repo.transitionExpiredTrials(userId, orgId)` before `findActiveByScopeStatus`; when the entity has `status === 'trialing'` compute `daysRemaining = Math.max(0, Math.ceil((new Date(trial_ends_at).getTime() - Date.now()) / 86400000))` and include `trial_ends_at` and `days_remaining` in the returned value |
| T031 | Implement: getMySubscriptionHandler forwards trial fields | implement | R010 | In `apps/services/src/modules/subscriptions/handlers/getMySubscriptionHandler.ts` pass through `trial_ends_at` and `days_remaining` from the use case result in the response body |
| T032 | Test: requireActiveSubscription blocks expired-trial scopes | test | R007, EC001 | In `tests/unit/modules/subscriptions/plugins/requireActiveSubscription.test.ts` assert WHEN `signupMode = 'free_trial'` and no active/trialing subscription exists THEN `TrialExpiredError` is thrown with `trialEndedAt` set; WHEN `signupMode = 'freemium'` THEN the preHandler resolves without any DB call |
| T033 | Test: requireActiveSubscription allows active and trialing scopes | test | R007, R008 | In `tests/unit/modules/subscriptions/plugins/requireActiveSubscription.test.ts` assert WHEN subscription has `status = 'active'` or `status = 'trialing'` THEN the preHandler resolves without throwing |
| T034 | Implement: requireActiveSubscription plugin | implement | R007, R008, EC001, EC005 | Create `apps/services/src/modules/subscriptions/plugins/requireActiveSubscription.ts` with a plain `preHandler` function: no-op if `signupMode !== 'free_trial'` or `!request.userId`; call `transitionExpiredTrials`, then `findActiveByScopeStatus`; throw `TrialExpiredError(subscription.trial_ends_at ?? now.toISOString())` when no non-expired subscription found |
| T035 | Implement: register requireActiveSubscription globally in app.ts | implement | R008, EC005 | In `apps/services/src/app.ts` add an `onRequest` hook after `clerkAuthPlugin` that skips URLs starting with `/billing/`, `/webhooks/`, `/health`, and any unauthenticated request, then calls `requireActiveSubscription(request, reply)` |
| T036 | Test: subscriptionsConfig defaults | test | R001, R002 | In `tests/unit/shared/configs/subscriptionsConfig.test.ts` verify existing tests still pass (no regression) after adding the new config keys |
