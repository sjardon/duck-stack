# SUBS-003 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Implement migration: subscription_id FK + event_id column | implement | R008, R010 | In `20260625000000_billing_webhook_events_subscription.sql` add FK constraint on `billing_webhook_events.subscription_id → subscriptions(id) ON DELETE SET NULL`, add `event_id TEXT` nullable column, and create partial index `ON billing_webhook_events (provider, event_id) WHERE event_id IS NOT NULL` |
| T002 | Implement interface extensions | implement | R008, R009, R010 | In `iMobbexBillingSyncRepository.ts` extend `RecordEventInput` with optional `subscriptionId?: string \| null` and `eventId?: string \| null`; add `SubscriptionSyncOutcome = 'applied' \| 'noop' \| 'orphan'`; add `UpdateSubscriptionStatusInput` interface with fields `providerSubscriptionId`, `eventType`, `currentPeriodStart?`, `currentPeriodEnd?`; add `UpdateSubscriptionStatusResult` interface with fields `outcome`, `subscriptionId`, `resolvedStatus`; declare methods `checkDuplicateEventId(eventId: string, provider: string): Promise<boolean>` and `updateSubscriptionStatus(input: UpdateSubscriptionStatusInput): Promise<UpdateSubscriptionStatusResult>` on `IMobbexBillingSyncRepository` |
| T003 | Test: checkDuplicateEventId — returns true on existing event_id, false otherwise | test | R010, EC005 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `checkDuplicateEventId('evt-001', 'mobbex')` is called and the DB returns a row with that provider+event_id THEN returns `true`; WHEN no matching row exists THEN returns `false`; mock `this.sql` tagged-template call |
| T004 | Implement: checkDuplicateEventId in MobbexBillingSyncRepository | implement | R010, EC005 | In `mobbexBillingSyncRepository.ts` add `checkDuplicateEventId(eventId, provider)`: execute `SELECT 1 FROM billing_webhook_events WHERE provider = $provider AND event_id = $eventId LIMIT 1`, return `rows.length > 0`; wrap in try/catch per repository error-handling rules; log duration |
| T005 | Test: recordEvent persists subscriptionId and eventId | test | R008 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `recordEvent({ eventType, payload, transactionId: null, subscriptionId: 'sub-001', eventId: 'evt-001' })` is called THEN the mocked `sql` tagged-template receives an INSERT that includes `subscription_id = 'sub-001'` and `event_id = 'evt-001'`; WHEN `subscriptionId` and `eventId` are omitted THEN both columns default to `null` |
| T006 | Implement: recordEvent extended with subscriptionId and eventId | implement | R008 | In `mobbexBillingSyncRepository.ts` update the `recordEvent` INSERT to include `subscription_id` (from `input.subscriptionId ?? null`) and `event_id` (from `input.eventId ?? null`) in the column list and VALUES; update `RecordEventInput` usage to include both fields without breaking the existing `transactionId` contract |
| T007 | Test: updateSubscriptionStatus — activated event sets active and periods | test | R002, R009 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `updateSubscriptionStatus({ providerSubscriptionId: 'psub-1', eventType: 'subscription.activated', currentPeriodStart: '2026-06-01T00:00:00Z', currentPeriodEnd: '2026-07-01T00:00:00Z' })` is called and the DB SELECT returns a `pending` subscription THEN `sql.begin` transaction UPDATEs `status='active'`, `current_period_start`, `current_period_end` and returns `{ outcome: 'applied', subscriptionId: 'sub-001' }`; WHEN SELECT returns an `active` subscription with matching periods THEN returns `{ outcome: 'noop' }` (R009) without UPDATE |
| T008 | Test: updateSubscriptionStatus — renewed event updates period, recovers past_due, handles EC001 | test | R003, R004, EC001, R009 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `eventType='subscription.renewed'` and current status is `active` with matching `current_period_end` THEN returns `'noop'` (R009); WHEN current status is `past_due` THEN UPDATE sets `status='active'` AND `current_period_end` (R004) and returns `'applied'`; WHEN current status is `pending` THEN UPDATE sets `status='active'`, `current_period_start`, AND `current_period_end` (EC001) and returns `'applied'`; WHEN current status is `active` with different `current_period_end` THEN UPDATE sets only `current_period_end` (R003) and returns `'applied'` |
| T009 | Test: updateSubscriptionStatus — payment_failed sets past_due, skips terminal/already-past-due | test | R005, EC002, R009 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `eventType='subscription.payment_failed'` and current status is `active` THEN UPDATE sets `status='past_due'` and returns `'applied'`; WHEN current status is `past_due` THEN returns `'noop'` without UPDATE (R009); WHEN current status is `canceled` THEN returns `'noop'` without UPDATE (EC002); WHEN current status is `expired` THEN returns `'noop'` without UPDATE (EC002) |
| T010 | Test: updateSubscriptionStatus — canceled event sets canceled and canceled_at | test | R006, R009 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `eventType='subscription.canceled'` and current status is `active` THEN UPDATE sets `status='canceled'` and `canceled_at=now()` and returns `'applied'`; WHEN current status is already `canceled` THEN returns `'noop'` without UPDATE (R009) |
| T011 | Test: updateSubscriptionStatus — expired event sets expired | test | R007, R009 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write failing tests asserting: WHEN `eventType='subscription.expired'` and current status is `active` THEN UPDATE sets `status='expired'` and returns `'applied'`; WHEN current status is already `expired` THEN returns `'noop'` without UPDATE (R009) |
| T012 | Test: updateSubscriptionStatus — orphan when subscription not found | test | EC003 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` write a failing test asserting: WHEN `updateSubscriptionStatus` is called with any eventType and the SELECT returns no rows THEN returns `{ outcome: 'orphan', subscriptionId: null, resolvedStatus: null }` |
| T013 | Implement: updateSubscriptionStatus in MobbexBillingSyncRepository | implement | R002, R003, R004, R005, R006, R007, R009, EC001, EC002, EC003 | In `mobbexBillingSyncRepository.ts` add `updateSubscriptionStatus(input)`: wrap in `sql.begin`; `SELECT id, status, current_period_start, current_period_end FROM subscriptions WHERE provider_subscription_id = $1 LIMIT 1 FOR UPDATE`; if no row return orphan; switch on `input.eventType` to apply the state machine defined in design.md; include R009 guard before each UPDATE; return `{ outcome, subscriptionId, resolvedStatus }`; wrap each sub-query in try/catch per repository rules; log duration for SELECT and UPDATE steps |
| T014 | Test: dispatchMobbexSubscriptionEvent — duplicate event_id returns early without recording | test | R010, EC005 | In `tests/unit/modules/webhooks/mobbex/mobbexSubscriptionEventHandlers.test.ts` write a failing test asserting: WHEN payload contains `data.event_id = 'evt-dup'` and `repo.checkDuplicateEventId` returns `true` THEN `dispatchMobbexSubscriptionEvent` returns `'duplicate'` AND `repo.recordEvent` is NOT called AND `repo.updateSubscriptionStatus` is NOT called |
| T015 | Test: dispatchMobbexSubscriptionEvent — unknown event type records and returns unknown | test | EC004 | In `tests/unit/modules/webhooks/mobbex/mobbexSubscriptionEventHandlers.test.ts` write a failing test asserting: WHEN payload `type` is not in `SUBSCRIPTION_EVENT_TYPES` (e.g. `'subscription.upgraded'`) THEN `dispatchMobbexSubscriptionEvent` returns `'unknown'` AND `repo.recordEvent` is called once with `subscriptionId: null` AND `repo.updateSubscriptionStatus` is NOT called |
| T016 | Test: dispatchMobbexSubscriptionEvent — orphan subscription records event with null subscriptionId | test | EC003, R008 | In `tests/unit/modules/webhooks/mobbex/mobbexSubscriptionEventHandlers.test.ts` write a failing test asserting: WHEN payload `type` is `'subscription.activated'` and `repo.updateSubscriptionStatus` returns `{ outcome: 'orphan', subscriptionId: null }` THEN `dispatchMobbexSubscriptionEvent` returns `'orphan'` AND `repo.recordEvent` is called with `subscriptionId: null` |
| T017 | Test: dispatchMobbexSubscriptionEvent — successful dispatch calls recordEvent and emits NF002 log | test | R008, R011, NF002 | In `tests/unit/modules/webhooks/mobbex/mobbexSubscriptionEventHandlers.test.ts` write failing tests asserting: WHEN a known subscription event is dispatched and `repo.updateSubscriptionStatus` returns `{ outcome: 'applied', subscriptionId: 'sub-001' }` THEN `repo.recordEvent` is called with `{ subscriptionId: 'sub-001', eventId: <extracted value> }`; spy on the static logger and assert a log line is emitted containing `event_type`, `provider_subscription_id`, `subscription_id: 'sub-001'`, and `outcome: 'applied'` |
| T018 | Implement: dispatchMobbexSubscriptionEvent and SUBSCRIPTION_EVENT_TYPES | implement | R001, R002, R003, R004, R005, R006, R007, R008, R009, R010, R011, NF002, EC001, EC002, EC003, EC004, EC005 | In `mobbexSubscriptionEventHandlers.ts` declare and export `SUBSCRIPTION_EVENT_TYPES = new Set([...five event types...])` and `dispatchMobbexSubscriptionEvent(payload, repo)` implementing the dispatch flow from design.md: extract fields, check duplicate event_id, guard unknown type, call `updateSubscriptionStatus`, call `recordEvent`, emit NF002 structured log via static `logger`, return outcome |
| T019 | Test: routes.ts routes subscription events to subscription dispatcher | test | R001, R011 | In `tests/unit/modules/webhooks/mobbex/routes.test.ts` write failing tests asserting: WHEN a POST to `/webhooks/billing/mobbex` carries a verified payload with `type = 'subscription.activated'` THEN the response status is 200 AND the mocked `dispatchMobbexSubscriptionEvent` is called once AND the mocked `dispatchMobbexEvent` is NOT called; WHEN payload `type = 'payment.success'` THEN `dispatchMobbexEvent` is called and `dispatchMobbexSubscriptionEvent` is NOT called |
| T020 | Implement: routes.ts subscription dispatcher branching | implement | R001, R011 | In `routes.ts` import `SUBSCRIPTION_EVENT_TYPES` and `dispatchMobbexSubscriptionEvent` from `./mobbexSubscriptionEventHandlers.js`; after extracting `eventType` from the parsed payload, replace the single `dispatchMobbexEvent(payload, repository)` call with a conditional: `if (SUBSCRIPTION_EVENT_TYPES.has(eventType)) dispatchMobbexSubscriptionEvent(payload, repository) else dispatchMobbexEvent(payload, repository)`; assign outcome to the existing `outcome` binding used in the downstream log |
