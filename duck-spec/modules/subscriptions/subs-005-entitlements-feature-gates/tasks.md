# SUBS-005 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: entitlements mapping | test | R001 | Write failing tests in `apps/services/tests/unit/modules/subscriptions/entitlements.test.ts` asserting: WHEN `PLAN_ENTITLEMENTS` is imported THEN it has entries for `free`, `pro`, and `business`; WHEN a plan code is looked up THEN it returns an array of `EntitlementName` values |
| T002 | Implement EntitlementName type | implement | R001 | In `packages/types/src/index.ts` export `EntitlementName` as a string-literal union of `'advanced_analytics' \| 'priority_support' \| 'api_access' \| 'team_collaboration' \| 'white_label'` |
| T003 | Implement PLAN_ENTITLEMENTS mapping | implement | R001 | Create `apps/services/src/modules/subscriptions/entitlements.ts` exporting `PLAN_ENTITLEMENTS: Record<string, EntitlementName[]>` with entries for `free` (empty), `pro`, and `business` |
| T004 | Test: EntitlementRequiredError | test | R004 | In `apps/services/tests/unit/shared/errors.test.ts` add tests asserting: WHEN `new EntitlementRequiredError('advanced_analytics')` is constructed THEN `error.code` is `'ENTITLEMENT_REQUIRED'`, `error.statusCode` is `403`, and `error.message` contains the entitlement name |
| T005 | Implement EntitlementRequiredError | implement | R004 | In `apps/services/src/shared/errors.ts` add class `EntitlementRequiredError extends DomainError` with code `'ENTITLEMENT_REQUIRED'`, status `403`, and message `Entitlement required: ${entitlement}` |
| T006 | Test: subscriptionsConfig | test | R002 | Create `apps/services/tests/unit/shared/configs/subscriptionsConfig.test.ts` asserting: WHEN `STRICT_ENTITLEMENTS_ON_PAST_DUE` is `'true'` THEN `subscriptionsConfig.strictEntitlementsOnPastDue` is `true`; WHEN the var is absent or any other value THEN it is `false` |
| T007 | Implement subscriptionsConfig | implement | R002 | Create `apps/services/src/shared/configs/subscriptionsConfig.ts` exporting `subscriptionsConfig` with `strictEntitlementsOnPastDue: env.STRICT_ENTITLEMENTS_ON_PAST_DUE === 'true'` |
| T008 | Implement SubscriptionWithPlanEntity | implement | R002 | Create `apps/services/src/modules/subscriptions/entities/subscriptionWithPlanEntity.ts` exporting interface `SubscriptionWithPlanEntity extends SubscriptionEntity` with field `plan_code: string` |
| T009 | Implement ISubscriptionRepository extension | implement | R002 | In `apps/services/src/modules/subscriptions/repositories/interfaces/iSubscriptionRepository.ts` add method `findActiveOrWithinPeriodByScope(userId: string, orgId: string | null): Promise<SubscriptionWithPlanEntity | null>` |
| T010 | Test: SubscriptionDBRepository.findActiveOrWithinPeriodByScope | test | R002 | In `apps/services/tests/unit/modules/subscriptions/subscriptionDBRepository.test.ts` add tests asserting: WHEN a non-terminal subscription exists THEN it is returned with `plan_code`; WHEN only a canceled-but-within-period subscription exists THEN it is returned; WHEN only expired/past-period subscriptions exist THEN returns `null` |
| T011 | Implement SubscriptionDBRepository.findActiveOrWithinPeriodByScope | implement | R002 | In `apps/services/src/modules/subscriptions/repositories/subscriptionDBRepository.ts` implement `findActiveOrWithinPeriodByScope` with a JOIN on `subscription_plans`, filtering `status NOT IN ('canceled','expired') OR (status='canceled' AND current_period_end > NOW())`, ordered by status priority then `created_at DESC`, `LIMIT 1` |
| T012 | Test: GetEntitlementsUseCase | test | R002, R005 | Create `apps/services/tests/unit/modules/subscriptions/useCases/getEntitlementsUseCase.test.ts` asserting: WHEN repo returns `null` (EC001) THEN returns free plan entitlements; WHEN status is `past_due` and strict mode is `true` (EC003) THEN returns free plan entitlements; WHEN status is `past_due` and strict mode is `false` (EC003) THEN returns plan's entitlements; WHEN status is `canceled` with future period end (EC004) THEN returns plan's entitlements; WHEN status is `active` THEN returns plan's entitlements |
| T013 | Implement GetEntitlementsUseCase | implement | R002, R005 | Create `apps/services/src/modules/subscriptions/useCases/getEntitlementsUseCase.ts` with `execute(userId, orgId): Promise<EntitlementName[]>` applying EC001/EC003 logic using `subscriptionsConfig` and returning `PLAN_ENTITLEMENTS[plan_code] ?? []` |
| T014 | Test: getMyEntitlementsHandler | test | R005, R006 | Create `apps/services/tests/unit/modules/subscriptions/handlers/getMyEntitlementsHandler.test.ts` asserting: WHEN use case resolves THEN handler replies `{ entitlements: EntitlementName[] }`; WHEN use case is called THEN handler passes `userId` and `orgId` from request |
| T015 | Implement getMyEntitlementsHandler | implement | R005 | Create `apps/services/src/modules/subscriptions/handlers/getMyEntitlementsHandler.ts` as a thin Fastify handler that creates module-scope `SubscriptionDBRepository` + `GetEntitlementsUseCase` instances and replies `{ entitlements }` |
| T016 | Test: requireEntitlement preHandler | test | R002, R003, R004, NF001 | Create `apps/services/tests/unit/modules/subscriptions/plugins/requireEntitlement.test.ts` asserting: WHEN entitlement is present THEN preHandler resolves without throwing (R003); WHEN entitlement is absent THEN throws `EntitlementRequiredError` (R004); WHEN called twice on the same request THEN use case execute is called only once (NF001); WHEN `request.entitlements` is already set THEN does not call the use case again (NF001) |
| T017 | Implement requireEntitlement | implement | R002, R003, R004, NF001 | Create `apps/services/src/modules/subscriptions/plugins/requireEntitlement.ts` with `declare module 'fastify' { interface FastifyRequest { entitlements?: EntitlementName[] } }`, module-scope `GetEntitlementsUseCase` instance, and exported `requireEntitlement(name)` factory function |
| T018 | Implement routes.ts — entitlements endpoint | implement | R005, R006 | In `apps/services/src/modules/subscriptions/routes.ts` register `fastify.get('/billing/entitlements/me', { preHandler: requireAuth }, getMyEntitlementsHandler)` |
| T019 | Test: getMyEntitlements API function | test | R007 | In `apps/web/tests/billing/use-entitlement.test.ts` assert: WHEN `getMyEntitlements(token)` is called THEN `apiFetch` is invoked with `'/billing/entitlements/me'` and the bearer token; WHEN the response returns an array THEN the array is returned |
| T020 | Implement getMyEntitlements | implement | R007 | In `apps/web/src/api/billing.ts` add `getMyEntitlements(token: string): Promise<EntitlementName[]>` that calls `apiFetch<{ entitlements: EntitlementName[] }>('/billing/entitlements/me', { token })` and returns `response.entitlements` |
| T021 | Test: useEntitlement hook | test | R007, NF002 | In `apps/web/tests/billing/use-entitlement.test.ts` assert: WHEN `useEntitlement('advanced_analytics')` is called and response includes the name THEN returns `true`; WHEN response does not include the name THEN returns `false`; WHEN the API returns 401 (EC005) THEN returns `false` without throwing; verify `staleTime` is set to at least 5 minutes (NF002) |
| T022 | Implement useEntitlement hook | implement | R007, NF002 | Create `apps/web/src/hooks/use-entitlement.ts` exporting `useEntitlement(name: EntitlementName): boolean` using `useQuery` with key `['billing', 'entitlements', 'me']`, `staleTime: 5 * 60 * 1000`, query function that catches `ApiError` 401 and returns `[]` (EC005) |
| T023 | Test: EntitlementGate component | test | R008, R009 | Create `apps/web/tests/billing/EntitlementGate.test.tsx` asserting: WHEN `useEntitlement` returns `true` THEN renders `children`; WHEN `useEntitlement` returns `false` THEN renders default upgrade CTA fallback; WHEN `useEntitlement` returns `false` and `fallback` prop is provided THEN renders the custom fallback instead of the default CTA |
| T024 | Implement EntitlementGate | implement | R008, R009 | Create `apps/web/src/components/domain/billing/EntitlementGate.tsx` exporting `EntitlementGate({ name, children, fallback? })` that calls `useEntitlement(name)` and renders `children` or `fallback ?? <UpgradeCTA />` |
