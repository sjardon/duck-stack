# SUBS-004 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: listPlans API in landing | test | R001 | Write failing test in `apps/landing/tests/Pricing.test.tsx` asserting: WHEN `listPlans()` is called THEN it fetches `GET /billing/plans` using `import.meta.env.VITE_API_URL` and returns an array of `LandingPlan` objects |
| T002 | Implement `listPlans` in `api/plans.ts` | implement | R001 | In `listPlans()` fetch `GET ${import.meta.env.VITE_API_URL}/billing/plans` with raw `fetch`, parse the response JSON, and return a `LandingPlan[]`; throw on non-2xx; declare the `LandingPlan` interface locally |
| T003 | Test: Pricing section renders plan cards | test | R001, EC006 | In `apps/landing/tests/Pricing.test.tsx` assert: WHEN `Pricing` mounts with plans available THEN each plan's `name`, formatted price+interval, features, and a CTA button are rendered; WHEN `listPlans()` returns `[]` THEN the "No plans available" empty state is rendered |
| T004 | Test: Pricing CTA navigates to web origin | test | R002 | In `apps/landing/tests/Pricing.test.tsx` assert: WHEN user clicks a plan's CTA button THEN `window.location.href` is set to `${VITE_WEB_URL}/billing/subscribe?plan=<code>` where `<code>` matches the plan |
| T005 | Implement `Pricing` section component | implement | R001, R002, EC006 | In `Pricing()` use `useState` and `useEffect` to call `listPlans()` on mount; render a loading state while fetching; render a non-blocking error with retry affordance on failure; render "No plans available right now" when `plans.length === 0`; render one card per plan showing `name`, formatted price, `interval`, and `features`; CTA `onClick` sets `window.location.href = \`${import.meta.env.VITE_WEB_URL}/billing/subscribe?plan=${plan.code}\`` |
| T006 | Implement `HomePage` with Pricing section | implement | R001, R002 | In `HomePage()` import and render `<Pricing />` as a section in the page composition |
| T007 | Test: billing API functions for subscribe, getMySubscription, cancelSubscription | test | R003, R005, R007 | In `apps/web/tests/billing/use-billing.test.ts` assert: `subscribe(token, body)` POSTs to `/billing/subscriptions` with bearer token and body; `getMySubscription(token)` GETs `/billing/subscriptions/me` and returns `null` on 404 rather than throwing; `cancelSubscription(token, id, body)` POSTs to `/billing/subscriptions/:id/cancel` |
| T008 | Implement billing API additions in `api/billing.ts` | implement | R003, R004, R005, R006, R007 | Add to existing `apps/web/src/api/billing.ts`: `listPlans()` calling `apiFetch` on `GET /billing/plans` with no token; `subscribe(token, body)` calling `apiFetch` with method POST on `/billing/subscriptions`; `getMySubscription(token)` calling `apiFetch` on `GET /billing/subscriptions/me` and catching `ApiError` with `status === 404` to return `null`; `cancelSubscription(token, id, body)` calling `apiFetch` with method POST on `/billing/subscriptions/${id}/cancel` |
| T009 | Implement `use-billing` hooks | implement | R001, R003, R004, R005, R006, R007, NF001 | In `apps/web/src/hooks/use-billing.ts` create: `usePlans()` as `useQuery` with key `['billing','plans']` calling `listPlans()`; `useMySubscription()` as `useQuery` with key `['billing','subscriptions','me']` obtaining token via `useAuth().getToken()` and calling `getMySubscription(token)`; `useSubscribe()` as `useMutation` obtaining token and calling `subscribe(token, body)`; `useCancelSubscription()` as `useMutation` obtaining token and calling `cancelSubscription(token, id, body)`, invalidating `['billing','subscriptions','me']` on success |
| T010 | Test: SubscribePage subscribe flow | test | R003, R004, EC004, EC005 | In `apps/web/tests/billing/SubscribePage.test.tsx` assert: WHEN page mounts with valid `?plan=<code>` THEN `subscribe` mutation is called with `{ planCode: code }`; WHEN response includes `checkoutUrl` THEN `window.location.href` is set to it; WHEN response has no `checkoutUrl` THEN `navigate('/billing')` is called; WHEN `?plan` is absent or backend returns 400 THEN error message and pricing CTA are rendered; WHEN backend returns 409 THEN "already subscribed" message and `/billing` CTA are rendered |
| T011 | Implement `SubscribePage` | implement | R003, R004, EC004, EC005 | In `SubscribePage()` read `plan` from `useSearchParams()`; call `usePlans()` to resolve plan details for display; fire `useSubscribe` mutation inside a `useEffect` (with empty dependency array, run once on mount) when `plan` param is present; on mutation success with `checkoutUrl` set `window.location.href = checkoutUrl`; on success without `checkoutUrl` call `navigate('/billing')`; on `ApiError.status === 400` or missing `plan` param render error state with CTA linking to the `apps/landing` pricing surface via `VITE_LANDING_URL`; on `ApiError.status === 409` render "already subscribed" state with CTA to `/billing`; render `PlanCard` in display-only mode while processing |
| T012 | Test: StatusBadge color-coded rendering | test | R008 | In `apps/web/tests/billing/BillingPage.test.tsx` assert: WHEN `StatusBadge` is rendered with `status='pending'` THEN a yellow badge is visible; WHEN `status='active'` THEN green; WHEN `status='past_due'` THEN red; WHEN `status='canceled'` THEN grey |
| T013 | Implement `StatusBadge` component | implement | R008 | In `StatusBadge({ status })` render a `<span>` with a distinct inline style or className per status value: yellow for `pending`, green for `active`, red for `past_due`, grey for `canceled`; display the status string as the badge label |
| T014 | Test: PlanCard renders plan details and disables CTA when loading | test | NF001 | In `apps/web/tests/billing/BillingPage.test.tsx` assert: WHEN `PlanCard` receives a plan THEN name, formatted price, interval, and features are shown; WHEN `loading=true` THEN the CTA button has the `disabled` attribute |
| T015 | Implement `PlanCard` component | implement | NF001 | In `PlanCard({ plan, onSelect, loading })` render plan `name`, `formatCurrency(plan.price, plan.currency)` + `plan.interval`, a `features` list, and a button that calls `onSelect(plan.code)` on click; set `disabled={loading}` on the button |
| T016 | Test: BillingPage subscription states | test | R005, R006, R007, R008, R009, NF001, NF002, EC001, EC002, EC003 | In `apps/web/tests/billing/BillingPage.test.tsx` assert: WHEN subscription is active THEN plan name, `StatusBadge`, and formatted `current_period_end` are shown; WHEN subscription is null THEN free-plan empty state and pricing CTA are shown; WHEN Cancel is clicked THEN confirmation dialog opens; WHEN dialog is dismissed THEN cancel mutation is not called; WHEN dialog is confirmed THEN cancel mutation fires with `{ atPeriodEnd: true }`; WHEN status is `past_due` THEN red badge and payment-failed message appear; WHEN status is `canceled` and `current_period_end` is future THEN end-date line appears and Cancel button is absent; WHEN plan is null THEN plan_id with "(legacy plan)" label is rendered; WHEN query errors THEN non-blocking error message shown |
| T017 | Implement `SubscriptionStatusCard` component | implement | R005, R006, R008, NF001, NF002, EC001, EC002, EC003 | In `SubscriptionStatusCard({ subscription, plan, onCancel, cancelLoading, onNavigateToPricing })`: WHEN `subscription === null` render free-plan empty state with a button calling `onNavigateToPricing`; WHEN `status === 'past_due'` render `<StatusBadge status='past_due' />` plus an inline message and an external `<a>` link to the provider portal; WHEN `status === 'canceled'` render `<StatusBadge status='canceled' />` with "access ends `<formatDate(current_period_end)>`" and hide the Cancel button; WHEN `plan === null` render `subscription.plan_id` text followed by "(legacy plan)"; otherwise render `plan.name`, `<StatusBadge status={subscription.status} />`, `formatDate(subscription.current_period_end)`, and a Cancel button calling `onCancel` with `disabled={cancelLoading}` |
| T018 | Implement `CancelDialog` component | implement | R007, R009, NF001 | In `CancelDialog({ open, onConfirm, onDismiss, loading })` render nothing when `open === false`; when open render a modal overlay with a confirmation message, a Confirm button calling `onConfirm` with `disabled={loading}`, and a Dismiss button calling `onDismiss` that never calls `onConfirm` |
| T019 | Implement `BillingPage` | implement | R005, R006, R007, R009, NF001, NF002 | In `BillingPage()` call `useMySubscription()`, `usePlans()`, and `useCancelSubscription()`; while queries are loading render a loading indicator; when either query errors render a non-blocking error message with a retry affordance; resolve plan via `plans?.find(p => p.id === subscription?.plan_id) ?? null`; manage `cancelDialogOpen` boolean with `useState`; render `<SubscriptionStatusCard>` with all resolved props and `onCancel={() => setCancelDialogOpen(true)}`; render `<CancelDialog>` with `onConfirm` calling `cancelSubscription({ id: subscription.id, body: { atPeriodEnd: true } })` and closing dialog on success |
| T020 | Implement billing routes in `router.tsx` | implement | R003, R004, R005 | In `router.tsx` add `{ path: 'billing', element: <BillingPage /> }` and `{ path: 'billing/subscribe', element: <SubscribePage /> }` as children of the existing `{ path: '/*', element: <AppLayout /> }` route entry, which is already nested under `<AuthGuard />` |
