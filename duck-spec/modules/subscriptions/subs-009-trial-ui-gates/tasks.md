# SUBS-009 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: useTrialStatus return shape | test | R001, R002, NF001, NF002, EC001 | In `apps/web/tests/billing/useTrialStatus.test.ts` write failing tests asserting: WHEN `useTrialStatus` mounts THEN it issues `useQuery` with `queryKey: ['billing', 'subscriptions', 'me']`, `staleTime: 60_000`, `refetchOnWindowFocus: true`; WHEN loading THEN returns `{ isTrialing: false, isExpired: false, daysRemaining: null, trialEndsAt: null, isLoading: true }`; WHEN two instances share a `QueryClient` THEN `apiFetch` is called exactly once |
| T002 | Implement useTrialStatus hook | implement | R001, R002, NF001, NF002, EC001 | In `apps/web/src/hooks/useTrialStatus.ts` create `useTrialStatus()`: call `useQuery` with `queryKey: ['billing', 'subscriptions', 'me']`, `staleTime: 60_000`, `refetchOnWindowFocus: true`, and `queryFn: () => getMySubscription(token)`; return safe defaults while loading |
| T003 | Test: useTrialStatus trialing branch | test | R003, R006, R008, EC007 | In `apps/web/tests/billing/useTrialStatus.test.ts` write failing tests asserting: WHEN `status === 'trialing'` THEN `isTrialing = true`, `daysRemaining = days_remaining`, `trialEndsAt = trial_ends_at`; WHEN `days_remaining === 0` THEN `daysRemaining === 0`; WHEN `days_remaining === 2` THEN `daysRemaining === 2` |
| T004 | Implement useTrialStatus trialing derivation | implement | R003 | In `useTrialStatus` in `apps/web/src/hooks/useTrialStatus.ts` add derivation branch: when `data?.status === 'trialing'` return `isTrialing: true`, `daysRemaining: data.days_remaining ?? null`, `trialEndsAt: data.trial_ends_at`, `isExpired: false` |
| T005 | Test: useTrialStatus expired branch | test | R004, EC006 | In `apps/web/tests/billing/useTrialStatus.test.ts` write failing tests asserting: WHEN `status === 'expired'` THEN `isExpired = true`, `isTrialing = false`; WHEN `status === 'active'` THEN `isExpired = false`, `isTrialing = false` |
| T006 | Implement useTrialStatus expired and default derivation | implement | R004, R005, EC006 | In `useTrialStatus` in `apps/web/src/hooks/useTrialStatus.ts` add: `status === 'expired'` branch returning `isExpired: true`, `isTrialing: false`; default branch (`active`, `pending`, `past_due`, `canceled`, `null`) returning `isTrialing: false`, `isExpired: false` |
| T007 | Test: useInvalidateMySubscription | test | R015, EC004 | In `apps/web/tests/billing/useTrialStatus.test.ts` write failing test asserting: WHEN `useInvalidateMySubscription()` is called THEN `queryClient.invalidateQueries` is invoked with `{ queryKey: ['billing', 'subscriptions', 'me'] }` |
| T008 | Implement useInvalidateMySubscription | implement | R015, EC004 | In `apps/web/src/hooks/useTrialStatus.ts` export `useInvalidateMySubscription()`: returns a function that calls `queryClient.invalidateQueries({ queryKey: ['billing', 'subscriptions', 'me'] })` |
| T009 | Test: apiFetch TRIAL_EXPIRED interceptor | test | R014, EC003 | In `apps/web/tests/client.trial.test.ts` write failing tests asserting: WHEN `fetch` returns a 403 response with body `{ code: 'TRIAL_EXPIRED' }` THEN `window.location.replace` is called with `/trial-expired` and an `ApiError` is thrown; WHEN `fetch` returns a 403 with a different body THEN `window.location.replace` is not called and `ApiError` is thrown normally |
| T010 | Implement apiFetch TRIAL_EXPIRED interceptor | implement | R014, EC003 | In `apps/web/src/api/client.ts` modify the non-2xx branch of `apiFetch`: when `response.status === 403`, attempt to parse the body as JSON and check for `code === 'TRIAL_EXPIRED'`; if matched call `window.location.replace('/trial-expired')` then throw `ApiError`; otherwise fall through to existing error path |
| T011 | Test: AuthGuard trial redirect logic | test | R012, R013, EC001, EC002 | In `apps/web/tests/AuthGuard.trial.test.tsx` write failing tests asserting: WHEN `isExpired === true` and path is `/dashboard` THEN renders a redirect to `/trial-expired`; WHEN `isExpired === true` and path is `/billing` THEN renders `<Outlet />`; WHEN `isLoading === true` THEN renders loading state without redirecting; WHEN `isExpired === false` THEN renders `<Outlet />` normally |
| T012 | Implement AuthGuard trial redirect condition | implement | R012, R013, EC001, EC002 | In `apps/web/src/components/auth/AuthGuard.tsx` add `useTrialStatus()` call after the existing onboarding checks; append third sequential condition: hold loading state while `trialStatus.isLoading`; redirect to `/trial-expired` when `trialStatus.isExpired && !TRIAL_EXPIRED_WHITELIST.includes(pathname)`; render `<Outlet />` when `isExpired` and path is whitelisted |
| T013 | Test: TrialBanner renders nothing outside window | test | R007 | In `apps/web/tests/billing/TrialBanner.test.tsx` write failing tests asserting: WHEN `isTrialing === false` THEN renders nothing; WHEN `daysRemaining > 3` THEN renders nothing; WHEN `daysRemaining === null` THEN renders nothing |
| T014 | Test: TrialBanner renders urgency text | test | R006, R008, NF003, EC007 | In `apps/web/tests/billing/TrialBanner.test.tsx` write failing tests asserting: WHEN `isTrialing === true && daysRemaining === 2` THEN renders "2 days left in your trial — upgrade now" with a link to `/pricing`; WHEN `daysRemaining === 0` THEN renders "Less than 1 day left in your trial — upgrade now"; WHEN banner renders THEN its root element has `position: fixed` or equivalent sticky class |
| T015 | Implement TrialBanner component | implement | R006, R007, R008, NF003, EC007 | In `apps/web/src/components/domain/billing/TrialBanner.tsx` create `TrialBanner()`: call `useTrialStatus()`, return `null` when `!isTrialing || daysRemaining === null || daysRemaining > 3`; otherwise render fixed-position top bar with urgency text (special-case `daysRemaining === 0` → "Less than 1 day left") and `/pricing` CTA link |
| T016 | Modify AppLayout to mount TrialBanner | implement | R006, NF003 | In `apps/web/src/components/layout/AppLayout.tsx` import `<TrialBanner />` and render it as the first child of the root wrapper element, above the `<header>`, so it occupies fixed space at the top of the viewport without causing body reflow |
| T017 | Test: TrialExpiredPage renders plan list | test | R009, R011, EC005 | In `apps/web/tests/billing/TrialExpired.test.tsx` write failing tests asserting: WHEN the page renders THEN the title "Your free trial has ended" is visible; WHEN plans contain no free plan THEN `<PlanCard />` is rendered for each paid plan and no "Continue with free" button is present |
| T018 | Test: TrialExpiredPage free plan button | test | R010, R015, EC004 | In `apps/web/tests/billing/TrialExpired.test.tsx` write failing tests asserting: WHEN plans contain a plan with `code === 'free'` THEN a "Continue with free" button is rendered; WHEN the button is clicked THEN `POST /billing/subscriptions` is dispatched with `{ planCode: 'free' }`; WHEN mutation succeeds THEN `invalidateQueries` is called with the `subscriptions/me` key and `navigate('/')` is called |
| T019 | Implement TrialExpiredPage | implement | R009, R010, R011, R015, EC004, EC005 | In `apps/web/src/pages/TrialExpired.tsx` create `TrialExpiredPage()`: call `usePlans()`, `useSubscribe()`, `useInvalidateMySubscription()`, `useNavigate()`; render title, map plans to `<PlanCard onSelect={...} />`; when `plans.some(p => p.code === 'free')` render "Continue with free" button that fires `subscribe({ planCode: 'free' })` with `onSuccess` calling `invalidate()` then `navigate('/')` |
| T020 | Register /trial-expired route in router | implement | R012, R013 | In `apps/web/src/router.tsx` import `TrialExpiredPage` and add `{ path: '/trial-expired', element: <TrialExpiredPage /> }` as a direct child of the `AuthGuard` layout route, outside the `AppLayout` `/*` subtree |
