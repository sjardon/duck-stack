# SUBS-011 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: confirm baseline TDZ failure | test | R001, R002, R003, R004, EC001 | In `apps/services/tests/unit/modules/subscriptions/getMySubscriptionHandler.test.ts`, run the existing `describe` suite (its title carries a stale `R011` label from a prior feature; the assertions inside are what apply here) against the current implementation and confirm it fails to run with `ReferenceError: Cannot access 'mockExecute' before initialization`, establishing the R001 baseline; the suite's three cases already assert the R002 (`{ subscription }`), R003 (`{ subscription: null }`), and R004/EC001 (`useCase.execute` called with `userId`, `orgId ?? null`) behaviors that must keep passing once fixed. |
| T002 | Implement per-invocation dependency resolution | implement | R001, R002, R003, R004, NF001, EC001 | In `apps/services/src/modules/subscriptions/handlers/getMySubscriptionHandler.ts`, move `const repo = new SubscriptionDBRepository(db)` and `const useCase = new GetMySubscriptionUseCase(repo)` from module scope into the top of the `getMySubscriptionHandler` function body, so they are constructed per request instead of at import time, leaving the `useCase.execute(request.userId!, request.orgId ?? null)` call and `reply.send({ subscription })` response unchanged. |
| T003 | Test: verify suite passes post-fix | test | R001, R002, R003, R004, EC001 | In `apps/services/tests/unit/modules/subscriptions/getMySubscriptionHandler.test.ts`, re-run the same suite and confirm all three cases pass with no TDZ `ReferenceError`, verifying R001 is resolved while R002, R003, R004, and EC001 remain satisfied. |
