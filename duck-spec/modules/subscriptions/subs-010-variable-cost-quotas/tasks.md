# SUBS-010 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Add `QuotaMode`, `QuotaUnit`, `QuotaStrategy` types and extend `QuotaUsage` | implement | R001, R002, R010 | In `packages/types/src/index.ts` add `export type QuotaMode = 'pre' \| 'post'`, `export type QuotaUnit = string`, `export interface QuotaStrategy { unit: QuotaUnit; mode: QuotaMode; compute: (req: unknown) => number }`, and add `unit: string` field to `QuotaUsage` |
| T002 | Test: `resolveStrategy` falls back to default for unknown quotas | test | R001, R002 | In `apps/services/tests/unit/modules/subscriptions/entitlements.test.ts` add a describe block asserting: WHEN `resolveStrategy('unknown_quota')` is called THEN it returns `DEFAULT_QUOTA_STRATEGY` with `unit = 'request'`, `mode = 'pre'`, and `compute()` returns `1`; WHEN `resolveStrategy('api_requests')` is called THEN it returns the registered strategy |
| T003 | Add `QUOTA_STRATEGIES`, `DEFAULT_QUOTA_STRATEGY`, and `resolveStrategy` to `entitlements.ts` | implement | R001, R002 | In `apps/services/src/modules/subscriptions/entitlements.ts` add `import type { QuotaStrategy } from '@repo/types'`; export `DEFAULT_QUOTA_STRATEGY`, `QUOTA_STRATEGIES` (keyed by `QuotaName` with `api_requests` registered as `{ unit: 'request', mode: 'pre', compute: () => 1 }`), and `resolveStrategy(name: string): QuotaStrategy` that returns `QUOTA_STRATEGIES[name] ?? DEFAULT_QUOTA_STRATEGY` |
| T004 | Test: `IUsageCounterRepository` additions — `incrementByAndReturn` and `adjustCount` | test | R003, R005, NF001, NF002, NF003 | In `apps/services/tests/unit/modules/subscriptions/repositories/usageCounterDBRepository.test.ts` write tests asserting: WHEN `incrementByAndReturn` is called with cost `5` THEN the SQL upsert adds `5` to `count` and returns the new value; WHEN `adjustCount` is called with delta `-3` THEN the SQL UPDATE applies `count + (-3)` without a prior SELECT; WHEN delta is `0` the method resolves without issuing a query |
| T005 | Add `incrementByAndReturn` to `IUsageCounterRepository` interface | implement | R003, NF002 | In `apps/services/src/modules/subscriptions/repositories/interfaces/iUsageCounterRepository.ts` add method signature `incrementByAndReturn(userId: string \| null, orgId: string \| null, quotaName: string, periodStart: string, cost: number): Promise<number>` with JSDoc noting the atomic upsert adding `cost` instead of `1` |
| T006 | Add `adjustCount` to `IUsageCounterRepository` interface | implement | R005, NF001, NF003 | In `apps/services/src/modules/subscriptions/repositories/interfaces/iUsageCounterRepository.ts` add method signature `adjustCount(userId: string \| null, orgId: string \| null, quotaName: string, periodStart: string, delta: number): Promise<void>` with JSDoc noting the atomic `UPDATE count = count + delta` with no prior SELECT; note that if no row exists the UPDATE is a no-op |
| T007 | Implement `incrementByAndReturn` in `UsageCounterDBRepository` | implement | R003, NF002 | In `apps/services/src/modules/subscriptions/repositories/usageCounterDBRepository.ts` add `async incrementByAndReturn(userId, orgId, quotaName, periodStart, cost)` that executes `INSERT INTO usage_counters … VALUES (…, $cost) ON CONFLICT DO UPDATE SET count = usage_counters.count + $cost RETURNING count`; log latency; wrap errors as `ProviderError` |
| T008 | Implement `adjustCount` in `UsageCounterDBRepository` | implement | R005, NF001, NF003 | In `apps/services/src/modules/subscriptions/repositories/usageCounterDBRepository.ts` add `async adjustCount(userId, orgId, quotaName, periodStart, delta)` that short-circuits when `delta === 0`; otherwise executes `UPDATE usage_counters SET count = count + $delta WHERE user_id IS NOT DISTINCT FROM $userId AND org_id IS NOT DISTINCT FROM $orgId AND quota_name = $quotaName AND period_start = $periodStart`; log latency; wrap errors as `ProviderError` |
| T009 | Add `ProgrammingError` to shared errors | implement | R006, EC006 | In `apps/services/src/shared/errors.ts` add `export class ProgrammingError extends DomainError { constructor(message: string) { super('PROGRAMMING_ERROR', message, 500); } }` |
| T010 | Test: `RequireQuotaUseCase` strategy-aware cost computation and post-mode decoration | test | R003, R004, EC001, EC002, EC003 | In `apps/services/tests/unit/modules/subscriptions/useCases/requireQuotaUseCase.test.ts` add describe blocks asserting: WHEN strategy `compute` returns `0` THEN `incrementByAndReturn` is not called and `quotaReservations` is not set (EC001); WHEN `compute` returns `-1` THEN `ValidationError` is thrown before DB call (EC002); WHEN `compute` returns a non-integer THEN `ValidationError` is thrown (EC002); WHEN `compute` returns a cost exceeding `hard_limit` THEN `QuotaExceededError` is thrown before DB call (EC003); WHEN mode is `post` THEN `request.quotaReservations[name]` is set with `reserved` and `charged` equal to cost (R004) |
| T011 | Extend `RequireQuotaUseCase.execute` for strategy-aware cost | implement | R003, R004, EC001, EC002, EC003 | In `apps/services/src/modules/subscriptions/useCases/requireQuotaUseCase.ts` change signature to `execute(userId, orgId, quotaName, request: unknown)`; call `resolveStrategy(quotaName)`; call `strategy.compute(request)` to get `cost`; throw `ValidationError` if `cost < 0 || !Number.isInteger(cost)` (EC002); return early if `cost === 0` (EC001); throw `QuotaExceededError` if `cost > thresholds.hard_limit` (EC003); replace `incrementAndReturn(…, 1)` with `incrementByAndReturn(…, cost)`; if `strategy.mode === 'post'` cast `request` to record and set `(request as Record<string, unknown>).quotaReservations[quotaName] = { reserved: cost, charged: cost, rowKey: { userId: counterUserId, orgId: counterOrgId, periodStart } }` |
| T012 | Test: `requireQuota` plugin passes request to use case and registers `decorateRequest` | test | R003, R004, EC001, EC002 | In `apps/services/tests/unit/modules/subscriptions/plugins/requireQuota.test.ts` add tests asserting: WHEN `requireQuota('api_requests')` preHandler is invoked THEN `useCase.execute` is called with four arguments (userId, orgId, quotaName, request); WHEN the use case decorates `request.quotaReservations` THEN the field is readable on the same request object |
| T013 | Update `requireQuota` plugin — pass request and register `decorateRequest` | implement | R003, R004 | In `apps/services/src/modules/subscriptions/plugins/requireQuota.ts` change `useCase.execute(userId, orgId, name)` to `useCase.execute(userId, orgId, name, request)`; add `fastify.decorateRequest('quotaReservations', null)` using `fp` (fastify-plugin) wrapping; add module augmentation for `FastifyRequest.quotaReservations: Record<string, { reserved: number; charged: number; rowKey: { userId: string \| null; orgId: string \| null; periodStart: string } }> \| null` |
| T014 | Test: `ChargeQuotaUseCase` — validation, delta computation, mode guard | test | R005, R006, R007, R009, EC004, EC006 | In `apps/services/tests/unit/modules/subscriptions/useCases/chargeQuotaUseCase.test.ts` write tests asserting: WHEN `actual < 0` THEN `ValidationError` is thrown and `adjustCount` is not called (EC004); WHEN strategy mode is `pre` THEN `ProgrammingError` is thrown (EC006); WHEN `actual > charged` THEN `adjustCount` is called with positive delta (R005); WHEN `actual < charged` THEN `adjustCount` is called with negative delta (R005); WHEN called twice with increasing actuals THEN delta is computed relative to the most recent `charged` (R007); WHEN delta is `0` THEN `adjustCount` is not called (NF001) |
| T015 | Create `ChargeQuotaUseCase` | implement | R005, R006, R007, R009, EC004, EC006 | Create `apps/services/src/modules/subscriptions/useCases/chargeQuotaUseCase.ts` with class `ChargeQuotaUseCase` that accepts `IUsageCounterRepository` in constructor; `async execute(reservation: { reserved: number; charged: number; rowKey: QuotaRowKey }, quotaName: string, actual: number): Promise<number>` — throws `ValidationError` if `actual < 0`; throws `ProgrammingError` if `resolveStrategy(quotaName).mode !== 'post'`; computes `delta = actual - reservation.charged`; logs warning (via shared logger) if `delta > 0` noting count may exceed hard limit (R009); calls `counterRepo.adjustCount(…, delta)` when `delta !== 0`; returns `actual` as the new `charged` value |
| T016 | Test: `chargeQuota` helper — missing reservation, incremental charging, no-call behavior | test | R005, R006, R007, R008 | In `apps/services/tests/unit/modules/subscriptions/helpers/chargeQuota.test.ts` write tests asserting: WHEN `request.quotaReservations` is null or missing key THEN `ProgrammingError` is thrown (R006); WHEN `chargeQuota` is called once with `actual = 50` THEN `ChargeQuotaUseCase.execute` is called with reservation and `actual = 50` and `request.quotaReservations[name].charged` is updated to `50` (R005, R007); WHEN `chargeQuota` is called twice sequentially THEN second call delta is relative to first call's charged value (R007); WHEN `chargeQuota` is never called THEN `quotaReservations[name].charged` remains equal to `reserved` (R008 — passive test that no cleanup call is needed) |
| T017 | Create `chargeQuota` helper | implement | R005, R006, R007, R008 | Create `apps/services/src/modules/subscriptions/helpers/chargeQuota.ts`; instantiate `ChargeQuotaUseCase` with `UsageCounterDBRepository(db)` at module scope; export `async function chargeQuota(request: FastifyRequest, name: string, actual: number): Promise<void>` that reads `request.quotaReservations?.[name]`, throws `ProgrammingError('chargeQuota called for quota with no active reservation')` if absent; calls `useCase.execute(reservation, name, actual)`; sets `request.quotaReservations[name].charged = actual` |
| T018 | Test: `GetMyQuotasUseCase` includes `unit` in each `QuotaUsage` | test | R010 | In `apps/services/tests/unit/modules/subscriptions/useCases/getMyQuotasUseCase.test.ts` write tests asserting: WHEN `execute` resolves THEN each item in the returned array has a `unit` field equal to the value from `resolveStrategy(quotaName).unit`; WHEN the quota is `api_requests` THEN `unit` is `'request'` |
| T019 | Extend `GetMyQuotasUseCase` to include `unit` field | implement | R010 | In `apps/services/src/modules/subscriptions/useCases/getMyQuotasUseCase.ts` import `resolveStrategy` from `../entitlements.js`; add `unit: resolveStrategy(quotaName).unit` to the object pushed into `results` |
