# BILLING-002 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Implement SQL migration for transactions table | implement | R001 | In `apps/services/supabase/migrations/20260623000000_transactions.sql` create the `transactions` table with all R001 columns (`id`, `user_id`, `org_id`, `provider`, `provider_transaction_id`, `amount`, `currency`, `status`, `description`, `reference`, `metadata`, `failure_reason`, `checkout_url`, `created_at`, `updated_at`), a CHECK constraint on `status IN ('pending','approved','failed','refunded')`, a UNIQUE constraint on `reference`, indexes on `(user_id, created_at DESC)` and `(org_id, created_at DESC)`, and the `set_updated_at` trigger |
| T002 | Implement shared types for billing | implement | R015 | In `packages/types/src/index.ts` add `TransactionStatusValue`, `Transaction`, `CreateCheckoutInput`, and `TransactionListResponse` type exports |
| T003 | Implement TransactionEntity | implement | R001 | In `apps/services/src/modules/billing/entities/transaction.entity.ts` define the `TransactionEntity` interface mirroring the DB row shape including `checkout_url` |
| T004 | Implement ITransactionRepository interface | implement | R002, R006, R009, R011, R012 | In `apps/services/src/modules/billing/repositories/interfaces/iTransactionRepository.ts` define `ITransactionRepository` with methods `create(input: CreateTransactionData): Promise<TransactionEntity>`, `findById(id: string): Promise<TransactionEntity \| null>`, `findByIdempotencyKey(key: string, userId: string, orgId: string \| null): Promise<TransactionEntity \| null>`, `updateFailureReason(id: string, reason: string): Promise<void>`, `updateProviderData(id: string, data: { providerTransactionId: string; checkoutUrl: string }): Promise<void>`, and `list(query: ListTransactionsQuery): Promise<{ rows: TransactionEntity[]; nextCursor: string \| null }>` |
| T005 | Implement CheckoutBodySchema and ListTransactionsQuerySchema DTOs | implement | NF001, NF002 | In `apps/services/src/modules/billing/dtos/checkout.dto.ts` define `CheckoutBodySchema` (amount positive int, currency enum ARS/USD, description min 1, optional items and metadata) and `ListTransactionsQuerySchema` (limit coerced int 1–100 default 20, optional cursor string) |
| T006 | Test: TransactionDBRepository SQL calls | test | R001, R002, R009, R011, R012 | In `apps/services/tests/unit/billing/transactionDBRepository.test.ts` write failing tests asserting: WHEN `create` is called THEN it executes an INSERT with the correct field values; WHEN `findById` is called THEN it queries by `id`; WHEN `findByIdempotencyKey` is called THEN it queries by `reference`, `user_id`, and `org_id`; WHEN `list` is called with a cursor THEN it applies the `(created_at, id) < (cursor_at, cursor_id)` predicate and returns `nextCursor` when more rows exist |
| T007 | Implement TransactionDBRepository | implement | R001, R002, R006, R009, R011, R012 | In `apps/services/src/modules/billing/repositories/transactionDBRepository.ts` implement `TransactionDBRepository` using `postgres.js` tagged-template SQL; `create` inserts a row and returns the created entity; `findById` selects by `id`; `findByIdempotencyKey` selects by `reference` scoped to `user_id`/`org_id`; `updateFailureReason` updates `failure_reason`; `updateProviderData` updates `provider_transaction_id` and `checkout_url`; `list` implements cursor-based pagination via `(created_at, id) < (cursor_at, cursor_id)` clause with `LIMIT limit+1` and encodes `nextCursor` as base64 |
| T008 | Test: CheckoutUseCase | test | R002, R003, R004, R005, R011, R012, R013, NF004, EC001, EC003, EC004 | In `apps/services/tests/unit/billing/checkoutUseCase.test.ts` write failing tests asserting: WHEN idempotency key matches existing transaction THEN returns existing `{ checkoutUrl, transactionId }` without calling provider; WHEN no idempotency key THEN inserts pending row with `reference = id` then calls provider; WHEN provider succeeds THEN calls `updateProviderData` and returns `{ checkoutUrl, transactionId }`; WHEN provider throws `ProviderError` THEN calls `updateFailureReason` with error message and re-throws; WHEN `orgId` is non-null THEN `org_id` is set on the created row |
| T009 | Implement CheckoutUseCase | implement | R002, R003, R004, R005, R011, R012, R013, NF004, EC001, EC003, EC004 | In `apps/services/src/modules/billing/useCases/checkoutUseCase.ts` implement `CheckoutUseCase` class with `execute(userId: string, orgId: string \| null, body: CheckoutBodyType, idempotencyKey?: string): Promise<{ checkoutUrl: string; transactionId: string }>`: (1) if `idempotencyKey` is present, call `repo.findByIdempotencyKey` and return cached result if found; (2) generate a UUID for `id`, call `repo.create` with `status='pending'` and `reference=id`; (3) call `resolveProvider().createCheckout` with `reference=id`; (4) on success call `repo.updateProviderData` and return result; (5) on `ProviderError` call `repo.updateFailureReason` and re-throw |
| T010 | Test: GetTransactionUseCase | test | R006, R007, R008, EC005 | In `apps/services/tests/unit/billing/getTransactionUseCase.test.ts` write failing tests asserting: WHEN transaction exists and `user_id` matches requester (no orgId) THEN returns the full record; WHEN transaction does not exist THEN throws `NotFoundError` (404); WHEN transaction `user_id` differs from requester THEN throws `ForbiddenError` (403); WHEN requester has `orgId` and transaction `org_id` matches THEN returns the record; WHEN requester has `orgId` but transaction `org_id` differs THEN throws `ForbiddenError` |
| T011 | Implement GetTransactionUseCase | implement | R006, R007, R008, EC005 | In `apps/services/src/modules/billing/useCases/getTransactionUseCase.ts` implement `GetTransactionUseCase` with `execute(id: string, userId: string, orgId: string \| null): Promise<TransactionEntity>`: call `repo.findById`; throw `NotFoundError` if null; check ownership (if `orgId` non-null match `org_id`, else match `user_id` and `org_id IS NULL`); throw `ForbiddenError` on mismatch; return the record |
| T012 | Test: ListTransactionsUseCase | test | R009, NF002, NF003, EC005, EC007 | In `apps/services/tests/unit/billing/listTransactionsUseCase.test.ts` write failing tests asserting: WHEN called without cursor THEN calls `repo.list` with correct `userId`/`orgId` and default limit 20; WHEN valid cursor provided THEN calls `repo.list` with decoded cursor; WHEN malformed cursor provided THEN throws `ValidationError` (400); WHEN `orgId` is non-null THEN passes `orgId` to `repo.list` for org-scoped filtering; WHEN `repo.list` returns `limit+1` rows THEN `nextCursor` is non-null |
| T013 | Implement ListTransactionsUseCase | implement | R009, NF002, NF003, EC005, EC007 | In `apps/services/src/modules/billing/useCases/listTransactionsUseCase.ts` implement `ListTransactionsUseCase` with `execute(userId: string, orgId: string \| null, query: { limit: number; cursor?: string }): Promise<TransactionListResponse>`: decode base64 cursor to `{ created_at, id }` (throw `ValidationError` on failure); call `repo.list`; return `{ data: rows, nextCursor }` |
| T014 | Test: checkoutHandler Zod validation and reply | test | R002, R010, NF001, EC006 | In `apps/services/tests/unit/billing/checkoutHandler.test.ts` write failing tests asserting: WHEN body has `amount <= 0` THEN replies 400 `VALIDATION_ERROR`; WHEN currency is unsupported THEN replies 400 `VALIDATION_ERROR`; WHEN description is empty THEN replies 400 `VALIDATION_ERROR`; WHEN body is valid THEN delegates to `CheckoutUseCase.execute` and replies with `{ checkoutUrl, transactionId }`; WHEN `Idempotency-Key` header is present THEN passes it to `CheckoutUseCase.execute` |
| T015 | Implement checkoutHandler | implement | R002, R003, R004, R005, R010, NF001, EC006 | In `apps/services/src/modules/billing/handlers/checkoutHandler.ts` implement `checkoutHandler(request, reply)`: parse body with `CheckoutBodySchema` and return 400 on `ZodError`; extract `Idempotency-Key` header; instantiate `TransactionDBRepository(db)` and `CheckoutUseCase(repo)`; call `execute`; reply with `{ checkoutUrl, transactionId }` |
| T016 | Implement getTransactionHandler | implement | R006, R007, R008, R010 | In `apps/services/src/modules/billing/handlers/getTransactionHandler.ts` implement `getTransactionHandler(request, reply)`: extract `id` from `request.params`; instantiate `TransactionDBRepository(db)` and `GetTransactionUseCase(repo)`; call `execute(id, request.userId!, request.orgId ?? null)`; reply with `{ data: transaction }` |
| T017 | Implement listTransactionsHandler | implement | R009, R010, NF002, NF003, EC007 | In `apps/services/src/modules/billing/handlers/listTransactionsHandler.ts` implement `listTransactionsHandler(request, reply)`: parse query params with `ListTransactionsQuerySchema` and return 400 on `ZodError`; instantiate `TransactionDBRepository(db)` and `ListTransactionsUseCase(repo)`; call `execute(request.userId!, request.orgId ?? null, { limit, cursor })`; reply with `{ data, nextCursor }` |
| T018 | Implement billing routes plugin | implement | R002, R006, R009, R010 | In `apps/services/src/modules/billing/routes.ts` create a `fastify-plugin` that registers `POST /billing/checkout` (preHandler: requireAuth, handler: checkoutHandler), `GET /billing/transactions/:id` (preHandler: requireAuth, handler: getTransactionHandler), and `GET /billing/transactions` (preHandler: requireAuth, handler: listTransactionsHandler) |
| T019 | Register billing routes in app.ts | implement | R002, R006, R009, R010 | In `apps/services/src/app.ts` import `billingRoutes` from `./modules/billing/routes.js` and register it with `fastify.register(billingRoutes)` after `usersRoutes` |
| T020 | Implement frontend billing API client | implement | R014, R015 | In `apps/web/src/api/billing.ts` implement `createCheckout(token: string, body: CreateCheckoutInput): Promise<{ checkoutUrl: string; transactionId: string }>`, `getTransaction(token: string, id: string): Promise<Transaction>`, and `listTransactions(token: string, params?: { limit?: number; cursor?: string }): Promise<TransactionListResponse>` using `apiFetch` with the bearer token |
