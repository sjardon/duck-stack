# BILLING-003 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: billing_webhook_events migration | test | R001 | Write test in `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` asserting: WHEN `recordEvent` is called THEN a row is inserted into `billing_webhook_events` with `provider = 'mobbex'`, `event_type`, `payload`, `received_at`, and a nullable `transaction_id` — stub the `sql` tagged-template to capture the query and verify the columns |
| T002 | Create billing_webhook_events migration | implement | R001 | In `apps/services/supabase/migrations/20260623100000_billing_webhook_events.sql` define the `billing_webhook_events` table with columns `id UUID PK`, `provider TEXT NOT NULL`, `event_type TEXT NOT NULL`, `payload JSONB NOT NULL`, `received_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `transaction_id UUID REFERENCES transactions(id) ON DELETE SET NULL`, `subscription_id UUID` (unconstrained) |
| T003 | Create mobbexConfig | implement | R014 | In `apps/services/src/shared/configs/mobbexConfig.ts` export `mobbexConfig` object reading `MOBBEX_WEBHOOK_SECRET` from `process.env`; this value is consumed by the route plugin at registration time |
| T004 | Create IMobbexBillingSyncRepository interface | implement | R013 | In `apps/services/src/modules/webhooks/repositories/interfaces/iMobbexBillingSyncRepository.ts` declare types `EventOutcome`, `RecordEventInput`, `UpdateTransactionStatusInput`, and interface `IMobbexBillingSyncRepository` exposing `recordEvent(input: RecordEventInput): Promise<void>` and `updateTransactionStatus(input: UpdateTransactionStatusInput): Promise<EventOutcome>` |
| T005 | Test: MobbexBillingSyncRepository.recordEvent | test | R007 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` assert: WHEN `recordEvent({ eventType, payload, transactionId: 'uuid' })` is called THEN the sql template receives an INSERT into `billing_webhook_events` with `provider = 'mobbex'`, correct `event_type`, `payload`, and `transaction_id`; AND WHEN `transactionId` is `null` THEN `transaction_id` is `NULL` in the insert |
| T006 | Implement MobbexBillingSyncRepository.recordEvent | implement | R007, R013 | In `apps/services/src/modules/webhooks/repositories/mobbexBillingSyncRepository.ts` implement class `MobbexBillingSyncRepository` with constructor `(private readonly sql: Sql)` and method `recordEvent` that executes an `INSERT INTO billing_webhook_events (provider, event_type, payload, received_at, transaction_id) VALUES ('mobbex', ...)` using `this.sql.json(payload)` for JSONB; log query duration |
| T007 | Test: MobbexBillingSyncRepository.updateTransactionStatus — approved path | test | R008, R011, EC002 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` assert: WHEN a transaction with status `pending` is found by `provider_transaction_id` THEN `updateTransactionStatus({ status: 'approved', ... })` issues an UPDATE and returns `'approved'`; AND WHEN the transaction already has status `approved` THEN no UPDATE is issued and `'noop'` is returned |
| T008 | Test: MobbexBillingSyncRepository.updateTransactionStatus — failed path | test | R009, R010, EC001, EC004 | In `tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` assert: WHEN a transaction is found THEN `updateTransactionStatus({ status: 'failed', failureReason: 'msg' })` updates `status` and `failure_reason` and returns `'failed'`; AND WHEN no transaction is found by either `provider_transaction_id` or `reference` THEN returns `'unresolved'` without issuing an UPDATE |
| T009 | Implement MobbexBillingSyncRepository.updateTransactionStatus | implement | R008, R009, R010, R011, R013, EC001, EC002, EC004 | In `apps/services/src/modules/webhooks/repositories/mobbexBillingSyncRepository.ts` implement `updateTransactionStatus` inside a `sql.begin` block: (1) SELECT transaction by `provider_transaction_id` if provided, else by `reference`; if not found return `'unresolved'`; (2) compare current `status` to target; if equal return `'noop'`; (3) UPDATE `status` and `failure_reason` (when provided); return `'approved'` or `'failed'`; log query duration at each step |
| T010 | Test: dispatchMobbexEvent — success event | test | R008, R012, EC005 | In `tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` assert: WHEN payload `{ type: 'payment.success', data: { id: 'ptx', reference: 'ref' } }` is dispatched THEN `repo.updateTransactionStatus` is called with `{ status: 'approved', providerTransactionId: 'ptx', reference: 'ref' }`; THEN `repo.recordEvent` is called with matching `eventType`; AND WHEN event type is unknown THEN `updateTransactionStatus` is not called but `recordEvent` is still called |
| T011 | Test: dispatchMobbexEvent — failure event | test | R009, EC004 | In `tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` assert: WHEN payload `{ type: 'payment.failure', data: { id: 'ptx', reference: 'ref', message: 'declined' } }` is dispatched THEN `repo.updateTransactionStatus` is called with `{ status: 'failed', failureReason: 'declined', ... }` AND `repo.recordEvent` is called; AND WHEN `data.id` and `data.reference` are both absent THEN both fields are `null` in the call |
| T012 | Implement dispatchMobbexEvent | implement | R008, R009, R010, R012, EC001, EC002, EC003, EC004, EC005 | In `apps/services/src/modules/webhooks/mobbex/mobbexEventHandlers.ts` implement `dispatchMobbexEvent(payload, repo)`: extract `type`/`event_type` and `data`; extract `data.id` as `providerTransactionId` and `data.reference` as `reference`; for success types (`payment.success`, `checkout.success`) call `repo.updateTransactionStatus({ status: 'approved', ... })`; for failure types (`payment.failure`, `checkout.failure`, `payment.rejected`) call `repo.updateTransactionStatus({ status: 'failed', failureReason: data.message ?? '' })`; for unhandled types set `outcome = 'unresolved'`; call `repo.recordEvent` in all paths; return `outcome` |
| T013 | Test: routes — secret verification | test | R002, R004, R005, R014 | In `tests/unit/modules/webhooks/mobbex/routes.test.ts` assert: WHEN the plugin is registered without `MOBBEX_WEBHOOK_SECRET` THEN registration throws `Error`; WHEN a request arrives with a wrong or missing `secret` query param THEN the response is HTTP 401 with `{ code: 'UNAUTHORIZED' }`; WHEN the secret is correct THEN the handler proceeds past verification |
| T014 | Test: routes — payload parsing | test | R003, R006 | In `tests/unit/modules/webhooks/mobbex/routes.test.ts` assert: WHEN the request body is not valid JSON THEN the response is HTTP 400 with `{ code: 'VALIDATION_ERROR' }`; WHEN the body is valid JSON THEN parsing succeeds and `dispatchMobbexEvent` is called with the parsed object |
| T015 | Test: routes — success response and logging | test | R007, R012, NF002, NF003 | In `tests/unit/modules/webhooks/mobbex/routes.test.ts` assert: WHEN a verified valid event is processed THEN the response is HTTP 200 `{ received: true }`; AND the log entry contains `event_type`, `provider_transaction_id`, and `outcome` but does NOT contain the secret value or full payload headers |
| T016 | Implement routes.ts webhook plugin | implement | R002, R003, R004, R005, R006, R012, R013, R014, NF001, NF002, NF003 | In `apps/services/src/modules/webhooks/mobbex/routes.ts` implement Fastify plugin using `fp`: (1) read `mobbexConfig.webhookSecret` and throw `Error` if absent; (2) register scoped `addContentTypeParser('application/json', { parseAs: 'buffer' })` returning the buffer; (3) instantiate `new MobbexBillingSyncRepository(db)`; (4) handle `POST /webhooks/billing/mobbex` — verify `request.query.secret` throwing `UnauthorizedError` on mismatch; parse Buffer with `JSON.parse` in try/catch throwing `ValidationError` on error; call `dispatchMobbexEvent`; log `{ event_type, provider_transaction_id, outcome }`; reply `{ received: true }` HTTP 200 |
| T017 | Test: app.ts registers mobbex webhook before clerkAuthPlugin | test | R002, R014 | In `tests/unit/modules/webhooks/mobbex/routes.test.ts` (or a dedicated app registration test) assert: WHEN `createApp()` is called with a valid `MOBBEX_WEBHOOK_SECRET` THEN the route `POST /webhooks/billing/mobbex` is available; WHEN `MOBBEX_WEBHOOK_SECRET` is absent THEN `createApp()` rejects |
| T018 | Register mobbexWebhookRoutes in app.ts | implement | R002, R014 | In `apps/services/src/app.ts` import `mobbexWebhookRoutes` from `./modules/webhooks/mobbex/routes.js` and call `await fastify.register(mobbexWebhookRoutes)` before the `clerkWebhookRoutes` registration line |
