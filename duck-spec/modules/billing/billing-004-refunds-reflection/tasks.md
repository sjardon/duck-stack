# BILLING-004 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Create refunds migration | implement | R001 | In `apps/services/supabase/migrations/20260623200000_refunds.sql` create the `refunds` table with columns `id` (uuid PK), `transaction_id` (uuid FK → `transactions.id` ON DELETE CASCADE), `amount` (numeric NOT NULL), `reason` (text nullable), `status` (text NOT NULL CHECK in `pending`, `approved`, `failed`), `provider_refund_id` (text NOT NULL UNIQUE), `created_at` and `updated_at` (timestamptz); add index on `transaction_id`; attach `set_updated_at()` trigger |
| T002 | Add Refund shared types | implement | R001, R013 | In `packages/types/src/index.ts` export `RefundStatusValue = 'pending' \| 'approved' \| 'failed'` and `Refund` interface matching all `refunds` table columns |
| T003 | Create RefundEntity | implement | R001, R013 | Create `apps/services/src/modules/billing/entities/refund.entity.ts` exporting `RefundEntity` interface (same shape as `Refund` from `@repo/types`, importing `RefundStatusValue`) |
| T004 | Extend ITransactionRepository with getRefundsByTransactionId | implement | R009, R013 | In `apps/services/src/modules/billing/repositories/interfaces/iTransactionRepository.ts` add `getRefundsByTransactionId(transactionId: string): Promise<RefundEntity[]>` to the `ITransactionRepository` interface |
| T005 | Test: getRefundsByTransactionId repository method | test | R009, R013 | Write failing test in `apps/services/tests/unit/billing/transactionDBRepository.test.ts` asserting: WHEN `getRefundsByTransactionId` is called THEN executes a SELECT on `refunds` WHERE `transaction_id = $id` ORDER BY `created_at ASC` |
| T006 | Implement getRefundsByTransactionId on TransactionDBRepository | implement | R009, R013 | In `apps/services/src/modules/billing/repositories/transactionDBRepository.ts` implement `getRefundsByTransactionId(transactionId)` selecting `id, transaction_id, amount, reason, status, provider_refund_id, created_at, updated_at` from `refunds` WHERE `transaction_id = $transactionId` ORDER BY `created_at ASC` using `postgres.js` tagged-template |
| T007 | Extend IMobbexBillingSyncRepository with refund types and method | implement | R002, R003, R006, R013 | In `apps/services/src/modules/webhooks/repositories/interfaces/iMobbexBillingSyncRepository.ts` add `UpsertRefundInput`, `RefundOutcome`, `UpsertRefundResult` types and declare `upsertRefundAndMaybeMarkTransactionRefunded(input: UpsertRefundInput): Promise<UpsertRefundResult>` on the interface |
| T008 | Test: upsertRefundAndMaybeMarkTransactionRefunded — approved refund (partial) | test | R002, R005, R006 | In `apps/services/tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add test: WHEN a refund with `refundStatus = 'approved'` is upserted and the cumulative approved sum is less than `transactions.amount` THEN the method returns `{ outcome: 'refund_approved', transactionId }` and `transactions.status` is not changed |
| T009 | Test: upsertRefundAndMaybeMarkTransactionRefunded — full refund triggers status transition | test | R004, R006, NF001 | In `apps/services/tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add test: WHEN the cumulative approved refund amount equals `transactions.amount` THEN the method updates `transactions.status` to `'refunded'` inside the same transaction and returns `{ outcome: 'transaction_refunded', transactionId }` |
| T010 | Test: upsertRefundAndMaybeMarkTransactionRefunded — failed refund | test | R003, EC004 | In `apps/services/tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add test: WHEN `refundStatus = 'failed'` THEN the refund row is upserted with `status = 'failed'`, `transactions.status` is not modified, and outcome is `'refund_failed'` |
| T011 | Test: upsertRefundAndMaybeMarkTransactionRefunded — transaction not found | test | R007, EC001 | In `apps/services/tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add test: WHEN `provider_transaction_id` does not match any transaction THEN no refund row is inserted and result is `{ outcome: 'unresolved', transactionId: null }` |
| T012 | Test: upsertRefundAndMaybeMarkTransactionRefunded — idempotent re-delivery | test | R008, EC002 | In `apps/services/tests/unit/modules/webhooks/repositories/mobbexBillingSyncRepository.test.ts` add test: WHEN the same `provider_refund_id` is received twice THEN the second upsert produces no duplicate row (ON CONFLICT DO UPDATE) and the cumulative sum is recomputed correctly |
| T013 | Implement upsertRefundAndMaybeMarkTransactionRefunded on MobbexBillingSyncRepository | implement | R002, R003, R004, R005, R006, R007, R008, NF001 | In `apps/services/src/modules/webhooks/repositories/mobbexBillingSyncRepository.ts` implement `upsertRefundAndMaybeMarkTransactionRefunded` inside `sql.begin`: (1) SELECT transaction by `provider_transaction_id`, return `unresolved` if missing; (2) log warning if `transaction.status === 'pending'` (EC005); (3) upsert `refunds` ON CONFLICT `provider_refund_id` DO UPDATE; (4) if `approved`: SUM approved amounts and UPDATE `transactions.status = 'refunded'` only when sum equals `transaction.amount`; (5) return `UpsertRefundResult` |
| T014 | Test: dispatchMobbexEvent — refund.success event | test | R002, NF002, NF003 | In `apps/services/tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` add test: WHEN `event_type` is `'refund.success'` with valid `provider_refund_id` and positive `amount` THEN `upsertRefundAndMaybeMarkTransactionRefunded` is called with `refundStatus = 'approved'` and `recordEvent` is called afterward |
| T015 | Test: dispatchMobbexEvent — refund.failure event | test | R003, EC004 | In `apps/services/tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` add test: WHEN `event_type` is `'refund.failure'` THEN `upsertRefundAndMaybeMarkTransactionRefunded` is called with `refundStatus = 'failed'` and `recordEvent` is called |
| T016 | Test: dispatchMobbexEvent — missing providerRefundId | test | EC006 | In `apps/services/tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` add test: WHEN `data.refund_id` is absent THEN `upsertRefundAndMaybeMarkTransactionRefunded` is NOT called, `recordEvent` IS called with `transactionId: null`, and outcome is `'unresolved'` |
| T017 | Test: dispatchMobbexEvent — missing or non-positive amount | test | EC006 | In `apps/services/tests/unit/modules/webhooks/mobbex/mobbexEventHandlers.test.ts` add test: WHEN `data.amount` is absent, null, zero, or negative THEN `upsertRefundAndMaybeMarkTransactionRefunded` is NOT called, `recordEvent` IS called with `transactionId: null`, and outcome is `'unresolved'` |
| T018 | Extend dispatchMobbexEvent with refund event handling | implement | R002, R003, R007, R008, EC001, EC002, EC003, EC004, EC005, EC006, NF002, NF003 | In `apps/services/src/modules/webhooks/mobbex/mobbexEventHandlers.ts` add `REFUND_SUCCESS_EVENT_TYPES` and `REFUND_FAILURE_EVENT_TYPES` sets; add a new branch after the existing failure branch that validates `providerRefundId` and `amount` (EC006), calls `repo.upsertRefundAndMaybeMarkTransactionRefunded`, and captures the returned `transactionId` for passing to `recordEvent` |
| T019 | Extend mobbex route handler logging for refund events | implement | NF002, NF003 | In `apps/services/src/modules/webhooks/mobbex/routes.ts` extend the structured log entry after `dispatchMobbexEvent` to include `provider_refund_id` and `amount` when the event type is a refund event, without logging secrets, headers, or PII |
| T020 | Test: GetRefundsUseCase — found and owned returns refund list | test | R009, EC007 | Write failing test in `apps/services/tests/unit/billing/getRefundsUseCase.test.ts` asserting: WHEN `findById` returns a transaction owned by the requester and `getRefundsByTransactionId` returns an array THEN `execute` returns that array (may be empty) |
| T021 | Test: GetRefundsUseCase — transaction not found | test | R010 | In `apps/services/tests/unit/billing/getRefundsUseCase.test.ts` add test: WHEN `findById` returns `null` THEN `execute` throws `NotFoundError` with `statusCode: 404` and code `'NOT_FOUND'` |
| T022 | Test: GetRefundsUseCase — wrong owner | test | R011 | In `apps/services/tests/unit/billing/getRefundsUseCase.test.ts` add test: WHEN the transaction's `user_id`/`org_id` does not match the authenticated requester THEN `execute` throws `ForbiddenError` with `statusCode: 403` and code `'FORBIDDEN'` |
| T023 | Implement GetRefundsUseCase | implement | R009, R010, R011, EC007 | Create `apps/services/src/modules/billing/useCases/getRefundsUseCase.ts` with `GetRefundsUseCase` class: constructor accepts `ITransactionRepository`; `execute(transactionId, userId, orgId)` calls `findById`, throws `NotFoundError` if null, applies same ownership check as `GetTransactionUseCase`, then calls and returns `getRefundsByTransactionId(transactionId)` |
| T024 | Test: getRefundsHandler — success reply shape | test | R009, R012 | Write failing test in `apps/services/tests/unit/billing/getRefundsHandler.test.ts` asserting: WHEN the use case returns a list of refunds THEN the handler replies `{ data: refunds }` with HTTP 200 |
| T025 | Implement getRefundsHandler | implement | R009, R012 | Create `apps/services/src/modules/billing/handlers/getRefundsHandler.ts` implementing `getRefundsHandler(request, reply)`: extracts `:id` from `request.params`, instantiates `TransactionDBRepository(db)` and `GetRefundsUseCase(repo)`, calls `useCase.execute(id, request.userId!, request.orgId ?? null)`, replies `{ data: refunds }` |
| T026 | Register GET /billing/transactions/:id/refunds route | implement | R009, R012 | In `apps/services/src/modules/billing/routes.ts` add `fastify.get('/billing/transactions/:id/refunds', { preHandler: requireAuth }, getRefundsHandler)` importing `getRefundsHandler` from `./handlers/getRefundsHandler.js` |
