# BILLING-001 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Add shared payment types to @repo/types | implement | R001 | In `packages/types/src/index.ts` add and export `Money`, `CheckoutInput`, `CheckoutSession`, `TransactionStatus`, `WebhookEvent`, and `PaymentProvider` interfaces as specified in design.md |
| T002 | Add ProviderError to shared errors | implement | NF002 | In `apps/services/src/shared/errors.ts` add `ProviderError extends DomainError` with constructor `(message: string, statusCode: 400 | 502 = 502)` and code `'PROVIDER_ERROR'` |
| T003 | Test: ProviderError construction and status codes | test | NF002 | Write failing tests in `apps/services/tests/unit/billing/ProviderError.test.ts` asserting: WHEN `new ProviderError('msg')` is constructed THEN `statusCode` is `502` and `code` is `'PROVIDER_ERROR'`; WHEN constructed with `400` THEN `statusCode` is `400` |
| T004 | Test: MobbexProvider auth headers on every request | test | R003, R004 | Write failing tests in `apps/services/tests/unit/billing/MobbexProvider.test.ts` asserting: WHEN `createCheckout` is called THEN outbound fetch includes `X-API-Key` and `X-Access-Token` headers from config |
| T005 | Test: MobbexProvider test mode | test | R005 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN `testMode: true` THEN requests carry the Mobbex sandbox indicator |
| T006 | Test: MobbexProvider createCheckout happy path | test | R003 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN Mobbex responds 200 THEN `createCheckout` returns a `CheckoutSession` with `sessionId`, `checkoutUrl`, and `expiresAt` |
| T007 | Test: MobbexProvider queryTransaction happy path | test | R003 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN Mobbex responds 200 THEN `queryTransaction` returns a `TransactionStatus` with correct `status` mapping |
| T008 | Test: MobbexProvider createSubscription and cancelSubscription | test | R003 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN Mobbex responds 200 THEN `createSubscription` returns `{ subscriptionId }` and `cancelSubscription` resolves without error |
| T009 | Test: MobbexProvider verifyWebhook happy path | test | R008, EC003 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN rawBody and matching secret are provided THEN `verifyWebhook` returns a `WebhookEvent` with `type` and `data`; WHEN secret does not match THEN throws `ProviderError` with statusCode 400 |
| T010 | Test: MobbexProvider timeout and network errors | test | NF003, EC001 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN fetch exceeds `timeoutMs` THEN throws `ProviderError` with `statusCode 502`; WHEN network error occurs THEN throws `ProviderError` with `statusCode 502` |
| T011 | Test: MobbexProvider 4xx and 5xx error mapping | test | NF002, EC001, EC002 | In `apps/services/tests/unit/billing/MobbexProvider.test.ts` assert: WHEN Mobbex returns 4xx THEN throws `ProviderError` with `statusCode 400`; WHEN returns 5xx or 401 THEN throws `ProviderError` with `statusCode 502` and upstream error code in message |
| T012 | Implement MobbexProvider adapter | implement | R003, R004, R005, R008, NF001, NF002, NF003, EC001, EC002, EC003 | In `apps/services/src/modules/billing/providers/MobbexProvider.ts` implement `MobbexProvider` class: define `MobbexConfig` interface, implement all five `PaymentProvider` methods, `fetchWithTimeout` private helper using `AbortController`, auth header injection, test mode flag, error mapping to `ProviderError`, and `verifyWebhook` secret comparison |
| T013 | Test: resolveProvider returns MobbexProvider for default env | test | R002 | Write failing tests in `apps/services/tests/unit/billing/resolveProvider.test.ts` asserting: WHEN `BILLING_PROVIDER` is unset or `'mobbex'` AND credentials are present THEN `resolveProvider()` returns an object satisfying the `PaymentProvider` interface |
| T014 | Test: resolveProvider fail-fast on unknown provider | test | R006 | In `apps/services/tests/unit/billing/resolveProvider.test.ts` assert: WHEN `BILLING_PROVIDER` is set to an unknown value THEN `resolveProvider()` throws `Error` with message containing the unknown value |
| T015 | Test: resolveProvider fail-fast on missing credentials | test | R007 | In `apps/services/tests/unit/billing/resolveProvider.test.ts` assert: WHEN `BILLING_PROVIDER` is `'mobbex'` AND `MOBBEX_API_KEY` or `MOBBEX_ACCESS_TOKEN` is missing THEN `resolveProvider()` throws a descriptive `Error` |
| T016 | Test: resolveProvider returns cached instance on repeated calls | test | EC004 | In `apps/services/tests/unit/billing/resolveProvider.test.ts` assert: WHEN `resolveProvider()` is called twice THEN the same object reference is returned both times |
| T017 | Implement resolveProvider factory | implement | R002, R006, R007, EC004 | In `apps/services/src/modules/billing/providers/resolveProvider.ts` implement `resolveProvider()`: read `BILLING_PROVIDER` env var (default `'mobbex'`), validate provider name and credentials with fail-fast `Error` throws, construct and cache `MobbexProvider` in a module-level variable, return the cached instance on subsequent calls |
| T018 | Integrate resolveProvider into createApp boot sequence | implement | R002, R006, R007 | In `apps/services/src/app.ts` call `resolveProvider()` at the top of `createApp()` before registering any plugin, so a misconfigured environment fails the process before the HTTP server starts |
