# NOTIFICATIONS-001 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: EmailTemplateMap type contract | test | R001, R002 | In `tests/unit/modules/notifications/ports/iEmailNotifier.test.ts` assert: WHEN `EmailSendRequest` is constructed with a known `templateId` THEN TypeScript accepts the correct variables shape and rejects a wrong shape (compile-time guard via `@ts-expect-error`) |
| T002 | Implement EmailTemplateMap, EmailTemplateId, EmailSendRequest in @repo/types | implement | R001, R002 | In `packages/types/src/index.ts` add `EmailTemplateMap` interface (`'example.welcome_demo': { recipientName: string }`), `EmailTemplateId` type alias, and generic `EmailSendRequest<T extends EmailTemplateId>` interface |
| T003 | Implement notificationsConfig | implement | NF004 | In `apps/services/src/shared/configs/notificationsConfig.ts` export `notificationsConfig` reading `SES_REGION`, `SES_FROM_ADDRESS`, `SQS_NOTIFICATIONS_QUEUE_URL`, `SQS_NOTIFICATIONS_DLQ_URL`, `SQS_POLLING_INTERVAL_MS`, `SQS_MAX_MESSAGES`, `SQS_VISIBILITY_TIMEOUT_SEC` from `process.env` with safe defaults |
| T004 | Implement IEmailNotifier port | implement | R001 | In `apps/services/src/modules/notifications/ports/iEmailNotifier.ts` declare `IEmailNotifier` interface with `send(params: { to: string; subject: string; html: string }): Promise<void>` |
| T005 | Implement ISqsEmailQueue port | implement | R003, R005, R006, NF002 | In `apps/services/src/modules/notifications/ports/iSqsEmailQueue.ts` declare `ISqsEmailQueue` interface with `enqueue(msg: EmailSendMessage): Promise<void>`, `receive(): Promise<SqsEnvelope[]>`, and `delete(receiptHandle: string): Promise<void>`; also export `SqsEnvelope` and `EmailSendMessage` types |
| T006 | Implement EmailSendMessageDto (Zod schema) | implement | R004, EC001 | In `apps/services/src/modules/notifications/dtos/emailSendMessageDto.ts` export `EmailSendMessageSchema` (zod object: `requestId` uuid, `userId` optional string, `templateId` string, `to` email, `variables` record, `enqueuedAt` datetime) and infer `EmailSendMessage` type |
| T007 | Test: SesEmailNotifier transient and permanent error handling | test | R004, R005, EC002, EC003 | In `tests/unit/modules/notifications/adapters/sesEmailNotifier.test.ts` assert: WHEN SES returns a 5xx/timeout THEN `send()` throws `ProviderError` with statusCode 502; WHEN SES returns an invalid-address error THEN throws `ProviderError` with statusCode 400 |
| T008 | Implement SesEmailNotifier adapter | implement | R004, NF004 | In `apps/services/src/modules/notifications/adapters/sesEmailNotifier.ts` implement `SesEmailNotifier implements IEmailNotifier`; constructor accepts `NotificationsConfig`; `send()` calls `@aws-sdk/client-sesv2` `SendEmailCommand`; maps SES permanent errors to `ProviderError(400)` and transient/network errors to `ProviderError(502)` |
| T009 | Test: SqsEmailQueue enqueue / receive / delete | test | R003, R006, NF002 | In `tests/unit/modules/notifications/adapters/sqsEmailQueue.test.ts` assert: WHEN `enqueue()` is called THEN `SendMessageCommand` is sent to the configured URL with JSON body; WHEN `receive()` is called THEN `ReceiveMessageCommand` uses correct params; WHEN `delete()` is called THEN `DeleteMessageCommand` is sent with the given receipt handle |
| T010 | Implement SqsEmailQueue adapter | implement | R003, R006, NF002, NF004 | In `apps/services/src/modules/notifications/adapters/sqsEmailQueue.ts` implement `SqsEmailQueue implements ISqsEmailQueue`; constructor accepts `NotificationsConfig`; wraps AWS SDK v3 `@aws-sdk/client-sqs` `SendMessageCommand`, `ReceiveMessageCommand`, `DeleteMessageCommand`; messages are only deleted on explicit `delete()` call, ensuring unacknowledged messages remain in-flight |
| T011 | Test: templateRegistry contains example template | test | R001, R009 | In `tests/unit/modules/notifications/templates/templateRegistry.test.ts` assert: WHEN `templateRegistry['example.welcome_demo'].render({ recipientName: 'Alice' })` is called THEN the result is a non-empty string containing HTML |
| T012 | Implement ExampleWelcomeDemoEmail React Email component | implement | R009 | In `apps/services/src/modules/notifications/templates/exampleWelcomeDemoEmail.tsx` implement a React Email component accepting `{ recipientName: string }` and rendering a simple HTML email body |
| T013 | Implement templateRegistry | implement | R001, R007, R009 | In `apps/services/src/modules/notifications/templates/templateRegistry.ts` export `templateRegistry` typed as `Record<EmailTemplateId, { subject: string; render: (vars: EmailTemplateMap[EmailTemplateId]) => string }>` mapping `'example.welcome_demo'` to `ExampleWelcomeDemoEmail` using `@react-email/render` |
| T014 | Test: SendEmailUseCase — unknown templateId throws ValidationError | test | R007, R008 | In `tests/unit/modules/notifications/useCases/sendEmailUseCase.test.ts` assert: WHEN `execute()` is called with an unregistered `templateId` THEN it throws `ValidationError` and no enqueue call is made |
| T015 | Test: SendEmailUseCase — known templateId enqueues and logs | test | R003, R007, R008, NF001 | In `tests/unit/modules/notifications/useCases/sendEmailUseCase.test.ts` assert: WHEN `execute()` is called with a valid `templateId` THEN `ISqsEmailQueue.enqueue()` is called with a message containing `requestId`, `userId`, `templateId`, `to`, `variables`, `enqueuedAt`; AND logger is called with `{ requestId, userId, templateId, outcome: 'enqueued' }` containing no rendered content |
| T016 | Implement SendEmailUseCase | implement | R001, R003, R007, R008, NF001 | In `apps/services/src/modules/notifications/useCases/sendEmailUseCase.ts` implement `SendEmailUseCase` with constructor `(queue: ISqsEmailQueue)`; `execute<T extends EmailTemplateId>(req: EmailSendRequest<T>)` checks `templateId` against `templateRegistry`, throws `ValidationError` if absent, builds `EmailSendMessage`, calls `queue.enqueue()`, logs `{ requestId, userId, templateId, outcome: 'enqueued', duration }` |
| T017 | Test: worker — success path deletes message and logs dispatched | test | R004, R008, NF001 | In `tests/unit/modules/notifications/worker.test.ts` assert: WHEN worker receives a valid message and `IEmailNotifier.send()` resolves THEN `ISqsEmailQueue.delete()` is called with the receipt handle AND logger records `{ requestId, templateId, outcome: 'dispatched', duration }` with no HTML content |
| T018 | Test: worker — transient ProviderError leaves message unacknowledged | test | R005, NF002, EC002 | In `tests/unit/modules/notifications/worker.test.ts` assert: WHEN `IEmailNotifier.send()` throws `ProviderError(502)` THEN `ISqsEmailQueue.delete()` is NOT called AND logger records `{ outcome: 'retry' }` |
| T019 | Test: worker — permanent ProviderError deletes message | test | EC003 | In `tests/unit/modules/notifications/worker.test.ts` assert: WHEN `IEmailNotifier.send()` throws `ProviderError(400)` THEN `ISqsEmailQueue.delete()` IS called AND logger records `{ outcome: 'permanent_failure' }` |
| T020 | Test: worker — poison message (parse failure) deletes without retry | test | EC001 | In `tests/unit/modules/notifications/worker.test.ts` assert: WHEN `receive()` returns a message with malformed JSON THEN the message is deleted, no template render is attempted, and logger records `{ messageId, outcome: 'parse_error' }` |
| T021 | Implement worker dispatch logic | implement | R004, R005, R006, R008, NF001, NF002, NF003, EC001, EC002, EC003, EC004 | In `apps/services/src/modules/notifications/worker.ts` implement the SQS poll loop: `receive()` → parse with `EmailSendMessageSchema` (delete on failure — EC001) → `templateRegistry[templateId].render(variables)` → `IEmailNotifier.send()` → on success delete + log dispatched; on `ProviderError(502)` log retry and do NOT delete (EC002, NF002); on `ProviderError(400)` delete + log permanent_failure (EC003); emit `{ requestId, userId, templateId, outcome, duration }` at each stage (R008, NF001) |
| T022 | Implement worker entrypoint and graceful shutdown | implement | NF003 | In `apps/services/src/modules/notifications/worker.ts` wire `SqsEmailQueue` and `SesEmailNotifier` from `notificationsConfig`, start the poll loop, register `SIGTERM`/`SIGINT` handlers that set a stop flag and await the current batch to finish before exiting |
