# NOTIFICATIONS-001 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Add notifications dependencies and worker script | implement | R004 | In `apps/services/package.json` add `@aws-sdk/client-sqs`, `@aws-sdk/client-ses`, `@react-email/render`, `react`, `react-dom` dependencies, `@types/react`/`@types/react-dom` devDependencies, and a `"worker": "tsx watch src/worker.ts"` script mirroring `dev`. |
| T002 | Enable JSX compilation for email templates | implement | R009 | In `apps/services/tsconfig.json` add `"jsx": "react-jsx"` to `compilerOptions` so `.tsx` template files compile. |
| T003 | Add notifications config | implement | R003 | In `apps/services/src/shared/configs/notificationsConfig.ts`, export `notificationsConfig` reading `AWS_REGION`, `NOTIFICATIONS_EMAIL_QUEUE_URL`, `NOTIFICATIONS_EMAIL_DLQ_URL`, `NOTIFICATIONS_SES_FROM_ADDRESS`, `NOTIFICATIONS_SQS_POLL_WAIT_SECONDS` with sane defaults. |
| T004 | Document notifications env vars | implement | R003 | In `apps/services/.env.example` add commented entries for `AWS_REGION`, `NOTIFICATIONS_EMAIL_QUEUE_URL`, `NOTIFICATIONS_EMAIL_DLQ_URL`, `NOTIFICATIONS_SES_FROM_ADDRESS`, `NOTIFICATIONS_SQS_POLL_WAIT_SECONDS`. |
| T005 | Test: example template renders end to end | test | R009 | Write failing acceptance test in `apps/services/tests/unit/modules/notifications/templates/emailTemplateRegistry.test.ts` asserting: WHEN `emailTemplateRegistry['example.ping'].render({ recipientName, sentAt })` and `.subject(...)` are called THEN the resolved HTML contains the given `recipientName` and the subject is a non-empty string. |
| T006 | Implement example ping email template | implement | R009 | In `apps/services/src/modules/notifications/templates/examplePingEmail.tsx`, export `ExamplePingEmail({ recipientName, sentAt }: { recipientName: string; sentAt: string })` as a minimal React Email component rendering both values. |
| T007 | Implement email template registry | implement | R009 | In `apps/services/src/modules/notifications/templates/emailTemplateRegistry.ts`, export `EMAIL_TEMPLATE_IDS`, `EmailTemplateId`, `EmailTemplateVariables`, `emailTemplateRegistry` (wiring `'example.ping'` to `ExamplePingEmail` via `@react-email/render`'s `render()`), and `isKnownEmailTemplate(id)`. |
| T008 | Test: IEmailNotifier enforces template variables at compile time | test | R001, R002 | Write failing acceptance test in `apps/services/tests/unit/modules/notifications/providers/iEmailNotifier.test.ts` asserting: a call to a typed `IEmailNotifier.send('example.ping', { recipientName, sentAt }, { to })` compiles and a stub resolves; `@ts-expect-error` calls with a missing variable, a wrong-typed variable, and an extra variable each fail to compile. |
| T009 | Implement email send message entity | implement | R001 | In `apps/services/src/modules/notifications/entities/emailSendMessage.ts`, export `EmailSendMessage<K extends EmailTemplateId = EmailTemplateId>` with `requestId`, `templateId`, `variables`, `to`, `userId?`. |
| T010 | Implement IEmailNotifier port | implement | R001, R002 | In `apps/services/src/modules/notifications/providers/interfaces/iEmailNotifier.ts`, export `IEmailNotifier` with `send<K extends EmailTemplateId>(templateId: K, variables: EmailTemplateVariables[K], recipient: { to: string; userId?: string }): Promise<void>`. |
| T011 | Test: send() enqueues and returns without waiting for delivery | test | R003 | Add a test case to `apps/services/tests/unit/modules/notifications/providers/sqsEmailNotifier.test.ts` asserting: WHEN `SqsEmailNotifier.send('example.ping', variables, { to })` is called with a mocked `SQSClient` THEN it issues one `SendMessageCommand` containing the envelope and resolves without invoking any SES/delivery call. |
| T012 | Test: send() rejects unknown template ids before enqueuing | test | R008 | Add a test case to `apps/services/tests/unit/modules/notifications/providers/sqsEmailNotifier.test.ts` asserting: WHEN `SqsEmailNotifier.send('not-a-real-template' as EmailTemplateId, {}, { to })` is called THEN it throws `ValidationError` and the mocked `SQSClient.send` is never called. |
| T013 | Implement SqsEmailNotifier | implement | R003, R008 | In `apps/services/src/modules/notifications/providers/sqsEmailNotifier.ts`, implement `SqsEmailNotifier.send()`: validate `templateId` via `isKnownEmailTemplate` (throw `ValidationError` if unknown), build the `EmailSendMessage` envelope (`requestId` from `requestContext` or a generated UUID), publish via `SendMessageCommand` to `notificationsConfig.emailQueueUrl`, log, and re-throw `ProviderError(502)` on an SQS client failure. |
| T014 | Implement resolveEmailNotifier factory | implement | R001, R003 | In `apps/services/src/modules/notifications/providers/resolveEmailNotifier.ts`, export `resolveEmailNotifier()`: throw a descriptive `Error` if `notificationsConfig.emailQueueUrl` is unset, otherwise lazily construct and cache a singleton `SqsEmailNotifier` backed by an `SQSClient`. |
| T015 | Implement IEmailSender port | implement | R004 | In `apps/services/src/modules/notifications/providers/interfaces/iEmailSender.ts`, export `EmailMessage` (`to`, `subject`, `html`) and `IEmailSender` with `send(message: EmailMessage): Promise<void>`. |
| T016 | Implement fake email sender test fixture | implement | R004 | In `apps/services/tests/mocks/fakeEmailSender.ts`, export `FakeEmailSender implements IEmailSender` recording calls and allowing configurable success/failure resolution for use in worker and use-case tests. |
| T017 | Test: DeliverEmailUseCase renders and delegates to IEmailSender | test | R004 | Write failing acceptance test in `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` asserting: WHEN `DeliverEmailUseCase.execute(message)` is called with a `FakeEmailSender` THEN the sender receives the rendered subject/HTML for `message.templateId`/`message.variables` and the recipient `message.to`. |
| T018 | Implement DeliverEmailUseCase | implement | R004 | In `apps/services/src/modules/notifications/useCases/deliverEmailUseCase.ts`, implement `DeliverEmailUseCase.execute(message)`: look up `emailTemplateRegistry[message.templateId]`, render subject/HTML, call `this.sender.send({ to, subject, html })`, logging and re-throwing on failure. |
| T019 | Implement SesEmailSender | implement | R004 | In `apps/services/src/modules/notifications/providers/sesEmailSender.ts`, implement `SesEmailSender.send()` calling `SendEmailCommand` via `@aws-sdk/client-ses`; on failure, log and throw `ProviderError(..., 400, error)` for known-permanent SES error names and `ProviderError(..., 502, error)` for anything else. |
| T020 | Implement email send message schema | implement | R004 | In `apps/services/src/modules/notifications/dtos/emailSendMessageSchema.ts`, export `EmailSendMessageSchema` (Zod) validating `requestId`, `templateId` (enum of `EMAIL_TEMPLATE_IDS`), `variables`, `to` (email), `userId?`. |
| T021 | Test: worker discards a malformed queue message without retry | test | R004 | Write failing acceptance test in `apps/services/tests/unit/modules/notifications/worker/emailWorker.test.ts` asserting: WHEN `processMessage(sqsClient, message)` is called with a body that fails `EmailSendMessageSchema` validation THEN the message is deleted from the queue, no `DeliverEmailUseCase`/sender call is made, and the error is logged. |
| T022 | Test: worker delivers successfully and logs the required fields | test | R004, R007, NF001 | Add a test case to `apps/services/tests/unit/modules/notifications/worker/emailWorker.test.ts` asserting: WHEN delivery succeeds THEN the worker logs `{ requestId, userId, templateId, result: 'sent', duration }` with no `variables`/`html`/`subject` fields, and deletes the message. |
| T023 | Test: worker leaves a transiently-failed message un-acked | test | R005, R007, NF002 | Add a test case to `apps/services/tests/unit/modules/notifications/worker/emailWorker.test.ts` asserting: WHEN delivery throws `ProviderError(..., 502)` THEN the worker logs a `warn`-level `{ ..., result: 'transient_failure', duration }` line and does not call `DeleteMessageCommand`. |
| T024 | Test: worker forwards a permanently-failed message to the DLQ | test | R006, R007, NF001, NF002 | Add a test case to `apps/services/tests/unit/modules/notifications/worker/emailWorker.test.ts` asserting: WHEN delivery throws `ProviderError(..., 400)` THEN the worker sends the message to `notificationsConfig.emailDeadLetterQueueUrl`, deletes it from the source queue, and logs `{ ..., result: 'permanent_failure', duration }`. |
| T025 | Implement envelope parsing | implement | R004 | In `apps/services/src/modules/notifications/worker/emailWorker.ts`, implement `parseEnvelope(body)`: `JSON.parse` plus `EmailSendMessageSchema.safeParse`, returning a discriminated success/failure result without throwing. |
| T026 | Implement message processing | implement | R004, R005, R006, R007, NF001, NF002 | In `apps/services/src/modules/notifications/worker/emailWorker.ts`, implement `processMessage(sqsClient, rawMessage)`: on parse failure, log and delete without retry (EC001); otherwise run inside `requestContext.run({ requestId }, ...)`, construct `SesEmailSender` + `DeliverEmailUseCase`, call `execute`, measure duration, and on success/transient/permanent outcomes apply the log line and ack/leave/DLQ-forward behavior described in design.md. |
| T027 | Implement worker poll loop | implement | R004 | In `apps/services/src/modules/notifications/worker/emailWorker.ts`, implement `startEmailWorker()`: long-poll via `ReceiveMessageCommand` using `notificationsConfig`, delegate each message to `processMessage`, and stop cleanly on `SIGINT`/`SIGTERM`. |
| T028 | Implement worker process entrypoint | implement | R004 | In `apps/services/src/worker.ts`, import `dotenv/config` and call `startEmailWorker()`, mirroring `server.ts`'s startup/shutdown handling. |
