# NOTIFICATIONS-001 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Implement email-notification port types | implement | R001, R002, R003, R004, EC004 | In `packages/types/src/index.ts` add `WelcomeEmailContext`, `EmailTemplateContextMap`, `EmailTemplateId`, `SendEmailInput<T>`, and the `EmailNotifier` port (`send<T extends EmailTemplateId>(input: SendEmailInput<T>): void`). |
| T002 | Add email-core dependencies | implement | R002, R003 | In `apps/services/package.json`, add `@aws-sdk/client-sesv2`, `react-email`, `@react-email/components`, `react`, `react-dom` to `dependencies`. |
| T003 | Enable JSX compilation for services | implement | R002 | In `apps/services/tsconfig.json`, add `"jsx": "react-jsx"` under `compilerOptions` so `.tsx` template files compile. |
| T004 | Enable Jest transform for `.tsx` template files | implement | R002 | In `apps/services/jest.config.ts`, broaden the `ts-jest` `transform` entry pattern from `^.+\\.ts$` to also match `.tsx` files. |
| T005 | Implement typed email config | implement | R003 | In `apps/services/src/shared/configs/emailConfig.ts`, export `emailConfig` with `sesRegion` (from `SES_REGION`, default `us-east-1`) and `senderEmail` (from `EMAIL_SENDER_ADDRESS`, default `''`), mirroring `mobbexConfig.ts`'s shape. |
| T006 | Test: `SesEmailNotifier.send` returns control without waiting for delivery | test | R001, NF001 | In `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`, assert: WHEN `send()` is called with a valid template/context THEN it returns `undefined` synchronously and the mocked `SESv2Client.send` has not yet resolved at that point (use a deferred/never-resolved provider mock to prove non-blocking behavior). |
| T007 | Implement non-blocking `SesEmailNotifier.send` | implement | R001, NF001 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, implement `SesEmailNotifier.send()`: call the private `dispatch()` without `await`, attach a `.catch()` fire-and-forget guard, and return `void` immediately. |
| T008 | Refactor `SesEmailNotifier` send/dispatch split | refactor | R001 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, extract the email-format regex to a module-level constant and ensure `send()` contains no business logic beyond detaching `dispatch()`. |
| T009 | Test: `renderEmailTemplate` produces HTML and plain text | test | R002 | In `apps/services/tests/unit/modules/notifications/templates/renderEmailTemplate.test.ts`, assert: WHEN `renderEmailTemplate('welcome', { recipientName })` is called THEN it resolves with `{ subject, html, text }` where `html` contains `recipientName` inside markup and `text` contains `recipientName` without HTML tags. |
| T010 | Implement `WelcomeEmail` template component | implement | R002, R006 | In `apps/services/src/modules/notifications/templates/welcomeEmail.tsx`, implement `WelcomeEmail(props: WelcomeEmailContext)` using `@react-email/components` primitives (`Html`, `Head`, `Body`, `Container`, `Heading`, `Text`) and export `welcomeEmailSubject`. |
| T011 | Implement email template registry | implement | R002, R004 | In `apps/services/src/modules/notifications/templates/emailTemplateRegistry.ts`, implement `emailTemplateRegistry: Record<EmailTemplateId, { subject: string; Component: (props) => JSX.Element }>` with the `welcome` entry wired to `WelcomeEmail`/`welcomeEmailSubject`. |
| T012 | Implement `renderEmailTemplate` | implement | R002, R004 | In `apps/services/src/modules/notifications/templates/renderEmailTemplate.ts`, implement `renderEmailTemplate<T>(templateId, context)`: look up `emailTemplateRegistry[templateId]`, return `undefined` if absent, otherwise `createElement(Component, context)` and call `render()`/`render(element, { plainText: true })` from `react-email` to produce `{ subject, html, text }`. |
| T013 | Refactor template rendering types | refactor | R002 | In `apps/services/src/modules/notifications/templates/`, extract a shared `RenderedEmail` interface (`{ subject, html, text }`) used by `renderEmailTemplate.ts`, and confirm `emailTemplateRegistry`'s type is exhaustive over `EmailTemplateId`. |
| T014 | Test: `SesEmailNotifier` delivers via SES | test | R003 | In `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`, assert: WHEN `send()` is called with a valid template/context/recipient THEN the mocked `SESv2Client.send` is called once with a `SendEmailCommand` whose input has `FromEmailAddress` = configured sender, `Destination.ToAddresses` = `[to]`, and `Content.Simple.Subject/Body.Html/Body.Text` matching the rendered output. |
| T015 | Implement SES delivery in `SesEmailNotifier.dispatch` | implement | R003 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, implement the delivery branch of `dispatch()`: build a `SendEmailCommand` from the rendered `{subject, html, text}` and configured sender, and call `this.client.send(...)`. |
| T016 | Test: `resolveNotifier` returns a cached singleton | test | R003 | In `apps/services/tests/unit/modules/notifications/providers/resolveNotifier.test.ts`, assert: WHEN `resolveNotifier()` is called twice THEN both calls return the same `SesEmailNotifier` instance, constructed from `emailConfig`'s `sesRegion`/`senderEmail`. |
| T017 | Implement `resolveNotifier` factory | implement | R003 | In `apps/services/src/modules/notifications/providers/resolveNotifier.ts`, implement `resolveNotifier()`: build and cache one `SesEmailNotifier` from `emailConfig` on first call, throwing a plain `Error` fail-fast if `emailConfig.senderEmail` is empty; return the cached instance on subsequent calls. |
| T018 | Refactor notifications providers | refactor | R003 | In `apps/services/src/modules/notifications/providers/`, align `resolveNotifier.ts`'s structure/naming with `resolveProvider.ts` (billing) for consistency (singleton cache variable, fail-fast error message format). |
| T019 | Test: unknown template id is logged and does not throw | test | R004, EC001, NF003 | In `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`, assert: WHEN `send()` is called with an unregistered `templateId` THEN `logger.warn` is called with a payload containing only the template id (no `to`/`context`), `SESv2Client.send` is never called, and `send()` does not throw. |
| T020 | Implement unknown-template guard in `dispatch` | implement | R004, EC001, NF003 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, in `dispatch()`, after `renderEmailTemplate()` returns `undefined`, `logger.warn({ templateId }, ...)` and return (commented silent-fail per BACKEND.md), logging only `templateId`. |
| T021 | Refactor `SesEmailNotifier` logging fields | refactor | R004 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, ensure every `logger.warn`/`logger.error` call site uses a consistent field shape (`{ templateId, err? }`) with no other identifiers. |
| T022 | Test: malformed/missing recipient is logged and not delivered | test | EC002 | In `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`, assert: WHEN `send()` is called with an empty or malformed `to` THEN `logger.warn` is called, `SESv2Client.send` is never called, and `send()` does not throw. |
| T023 | Implement recipient validation guard | implement | EC002 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, in `dispatch()`, validate `to` against the email regex before rendering/sending; on failure, `logger.warn` and return (commented silent-fail). |
| T024 | Test: rendering/delivery failure is isolated from the caller | test | R005, EC003, NF002, NF003 | In `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`, assert: WHEN the mocked `SESv2Client.send` rejects THEN `logger.error` is called with the original error (and only `templateId`/`err` fields — no PII), and the promise returned internally never rejects observably from `send()`'s perspective (no unhandled rejection, no throw). |
| T025 | Implement delivery failure isolation | implement | R005, EC003, NF002 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, wrap the `this.client.send(...)` call in `dispatch()` in `try/catch`: on error, `logger.error({ err, templateId }, ...)` then re-throw `ProviderError('Failed to send email via SES', 502, err)`, relying on `send()`'s fire-and-forget `.catch()` to swallow it. |
| T026 | Refactor `SesEmailNotifier` error handling | refactor | R005 | In `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts`, confirm the adapter-style `try/catch` (in `dispatch()`) and the fire-and-forget wrapper (in `send()`) each have a single, clearly commented responsibility, and remove any duplicate catch logic. |
| T027 | Test: template/context type mismatch fails compilation | test | EC004 | In `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`, add a `// @ts-expect-error` case calling `notifier.send({ templateId: 'welcome', to, context: {} })` (missing `recipientName`) to prove the incomplete context is rejected by `tsc`. |
| T028 | Test: `user.created` webhook dispatches the welcome email | test | R006, EC005 | In `apps/services/tests/unit/modules/webhooks/clerk/clerkEventHandlers.test.ts`, add a mock `EmailNotifier` (`send: jest.fn()`), update existing `dispatchClerkEvent(...)` calls to pass it, and assert: WHEN `dispatchClerkEvent` is called with a `user.created` event THEN `notifier.send` is called once with `{ templateId: 'welcome', to: <event email>, context: { recipientName } }`; WHEN called with a `user.updated` event THEN `notifier.send` is NOT called. |
| T029 | Implement welcome-email dispatch in `dispatchClerkEvent` | implement | R006, EC005 | In `apps/services/src/modules/webhooks/clerk/clerkEventHandlers.ts`, change `handleUserUpsert` to also return the already-derived `email`/`name`; add a `notifier: EmailNotifier` parameter to `dispatchClerkEvent`; inside `case 'user.created'` only, call `notifier.send({ templateId: 'welcome', to: email, context: { recipientName: name } })` without awaiting it. |
| T030 | Wire `resolveNotifier` into the Clerk webhook route | implement | R006 | In `apps/services/src/modules/webhooks/clerk/routes.ts`, instantiate `const notifier = resolveNotifier();` alongside the existing repository/provider instantiations and pass it as the new argument to every `dispatchClerkEvent(...)` call. |
| T031 | Refactor Clerk event handler wiring | refactor | R006 | In `apps/services/src/modules/webhooks/clerk/clerkEventHandlers.ts`, review `handleUserUpsert`/`dispatchClerkEvent` for duplicated email/name derivation and consolidate into a single computation reused by both the upsert call and the welcome-email dispatch. |
