# NOTIFICATIONS-003 — Tasks

| ID | Title | Type | File | Symbol | Covers | Description |
|---|---|---|---|---|---|---|
| T001 | Add email_suppressions migration | implement | `apps/services/supabase/migrations/20260723010000_email_suppressions.sql` | `email_suppressions` | R001, R004 | Create `email_suppressions` (`email TEXT PRIMARY KEY`, `reason TEXT NOT NULL CHECK (reason IN ('bounce','complaint'))`, `suppressed_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`) plus an `email_suppressions_set_updated_at` trigger reusing `set_updated_at()`; also `DROP`/re-`ADD CONSTRAINT` on `email_deliveries`'s `state` `CHECK` to include `'suppressed'`. |
| T002 | Implement email suppressions repository interface | implement | `apps/services/src/shared/repositories/interfaces/iEmailSuppressionsRepository.ts` | `IEmailSuppressionsRepository` | R001 | Export `SuppressionReason = 'bounce' \| 'complaint'` and `IEmailSuppressionsRepository` with `upsert(email: string, reason: SuppressionReason): Promise<void>` and `isSuppressed(email: string): Promise<boolean>`. |
| T003 | Extend email deliveries repository interface for suppression | implement | `apps/services/src/shared/repositories/interfaces/iEmailDeliveriesRepository.ts` | `EmailDeliveryState`, `IEmailDeliveriesRepository` | R004 | Add `'suppressed'` to the `EmailDeliveryState` union and add `markSuppressed(id: string): Promise<void>` to the interface. |
| T004 | Test: EmailSuppressionsDBRepository.upsert issues an idempotent INSERT ON CONFLICT | test | `apps/services/tests/unit/shared/repositories/emailSuppressionsDBRepository.test.ts` | `EmailSuppressionsDBRepository.upsert` | R001, R005, NF002 | Write failing acceptance test asserting: WHEN `upsert(email, reason)` is called against a mocked `Sql` THEN one query is issued containing `INSERT INTO email_suppressions`, `ON CONFLICT (email)`, `DO UPDATE`, and the given `email`/`reason` values. |
| T005 | Test: EmailSuppressionsDBRepository.isSuppressed queries by email | test | `apps/services/tests/unit/shared/repositories/emailSuppressionsDBRepository.test.ts` | `EmailSuppressionsDBRepository.isSuppressed` | R001, NF001 | Add test cases asserting: `isSuppressed(email)` issues a `SELECT … FROM email_suppressions WHERE email = …` query keyed on the given email; resolves `true` when the mocked query returns a row and `false` when it returns an empty array. |
| T006 | Implement EmailSuppressionsDBRepository | implement | `apps/services/src/shared/repositories/emailSuppressionsDBRepository.ts` | `EmailSuppressionsDBRepository` | R001, R005, NF001, NF002 | Implement `upsert` as `INSERT INTO email_suppressions (email, reason) VALUES (…) ON CONFLICT (email) DO UPDATE SET reason = EXCLUDED.reason, updated_at = now()` and `isSuppressed` as a `SELECT 1 … WHERE email = … LIMIT 1` returning `rows.length > 0`, both wrapped in the same `guarded()` try/catch/log/`ProviderError(502)` pattern used by `EmailDeliveriesDBRepository`. |
| T007 | Refactor: EmailSuppressionsDBRepository | refactor | `apps/services/src/shared/repositories/emailSuppressionsDBRepository.ts` | `EmailSuppressionsDBRepository` | R001 | Tidy method organization and duration logging for consistency with `EmailDeliveriesDBRepository`; no behavior change. |
| T008 | Implement fake email suppressions repository | implement | `apps/services/tests/mocks/fakeEmailSuppressionsRepository.ts` | `FakeEmailSuppressionsRepository` | R003, R004 | Implement `IEmailSuppressionsRepository` in-memory (a `Map<string, SuppressionReason>` or `Set<string>` plus reason tracking) so dependent-module tests (use case, worker, webhook handler) can substitute it without a real database. |
| T009 | Test: EmailDeliveriesDBRepository.markSuppressed transitions a queued record | test | `apps/services/tests/unit/shared/repositories/emailDeliveriesDBRepository.test.ts` | `EmailDeliveriesDBRepository.markSuppressed` | R004 | Add a test case asserting: WHEN `markSuppressed(id)` is called against a mocked `Sql` THEN an `UPDATE email_deliveries` is issued setting `state = 'suppressed'` guarded by `WHERE state = 'queued'` and the given `id`. |
| T010 | Implement EmailDeliveriesDBRepository.markSuppressed | implement | `apps/services/src/shared/repositories/emailDeliveriesDBRepository.ts` | `EmailDeliveriesDBRepository.markSuppressed` | R004 | Add `markSuppressed(id)`: `UPDATE email_deliveries SET state = 'suppressed' WHERE id = … AND state = 'queued'`, following the same `guarded()`/duration-logging pattern as `markSent`. |
| T011 | Implement fake markSuppressed | implement | `apps/services/tests/mocks/fakeEmailDeliveriesRepository.ts` | `FakeEmailDeliveriesRepository.markSuppressed` | R004 | Add `markSuppressed(id)` to the fake, guarded on `record.state === 'queued'` before setting `state = 'suppressed'`, mirroring the existing `markSent` guard. |
| T012 | Test: DeliverEmailUseCase short-circuits on a suppressed recipient | test | `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` | `DeliverEmailUseCase.execute` | R003, R004 | Write failing acceptance test asserting: WHEN `FakeEmailSuppressionsRepository.isSuppressed(message.to)` resolves `true` THEN `execute()` never calls `sender.send()` and calls `deliveries.markSuppressed(message.sendId)` instead. |
| T013 | Test: DeliverEmailUseCase dispatches normally for a non-suppressed recipient | test | `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` | `DeliverEmailUseCase.execute` | R003, EC002 | Add a test case asserting: WHEN `isSuppressed` resolves `false` THEN `sender.send()` is called and the existing `recordProviderMessageId` → `markSent` flow proceeds unchanged (this is the case that also holds for EC002: the check runs at dispatch/processing time, not at enqueue time, so a message enqueued before suppression is re-evaluated here). |
| T014 | Test: DeliverEmailUseCase does not re-check suppression on an already-dispatched retry | test | `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` | `DeliverEmailUseCase.execute` | R003, R005 | Add a test case asserting: WHEN `findById(sendId)` already has a non-null `providerMessageId` THEN `execute()` calls `markSent` directly without consulting `isSuppressed`, preserving the existing idempotent-retry path. |
| T015 | Implement DeliverEmailUseCase suppression check | implement | `apps/services/src/modules/notifications/useCases/deliverEmailUseCase.ts` | `DeliverEmailUseCase.dispatch` | R003, R004 | Inject `IEmailSuppressionsRepository` as a third constructor dependency; at the top of `dispatch()`, call `await this.suppressions.isSuppressed(message.to)` and, if `true`, call `await this.deliveries.markSuppressed(message.sendId)` and return before rendering the template or calling `sender.send()`. |
| T016 | Refactor: DeliverEmailUseCase | refactor | `apps/services/src/modules/notifications/useCases/deliverEmailUseCase.ts` | `DeliverEmailUseCase` | R003, R004 | Tidy the suppression short-circuit alongside the existing dispatch/idempotency branching for readability; no behavior change. |
| T017 | Test: emailWorker wires EmailSuppressionsDBRepository into DeliverEmailUseCase | test | `apps/services/tests/unit/modules/notifications/worker/emailWorker.test.ts` | `processMessage` | R003 | Add a test case asserting: WHEN `processMessage` runs THEN `DeliverEmailUseCase` is constructed with an `EmailSuppressionsDBRepository` instance as its third argument, alongside the existing sender/deliveries wiring assertions. |
| T018 | Wire EmailSuppressionsDBRepository into emailWorker | implement | `apps/services/src/modules/notifications/worker/emailWorker.ts` | `processMessage` | R003 | Construct `new EmailSuppressionsDBRepository(db)` alongside the existing `EmailDeliveriesDBRepository` and pass it as `DeliverEmailUseCase`'s third constructor argument. |
| T019 | Test: dispatchSesEvent suppresses on a permanent bounce | test | `apps/services/tests/unit/modules/webhooks/ses/sesEventHandlers.test.ts` | `dispatchSesEvent` | R002 | Write failing acceptance test asserting: WHEN `eventType` is `'Bounce'` and `bounce.bounceType` is `'Permanent'` THEN `suppressions.upsert` is called with reason `'bounce'` for every email in `bounce.bouncedRecipients`. |
| T020 | Test: dispatchSesEvent does not suppress on a transient bounce | test | `apps/services/tests/unit/modules/webhooks/ses/sesEventHandlers.test.ts` | `dispatchSesEvent` | EC001 | Add a test case asserting: WHEN `eventType` is `'Bounce'` and `bounce.bounceType` is `'Transient'` THEN `suppressions.upsert` is never called. |
| T021 | Test: dispatchSesEvent suppresses on complaint independent of delivery outcome | test | `apps/services/tests/unit/modules/webhooks/ses/sesEventHandlers.test.ts` | `dispatchSesEvent` | R002, EC003 | Add a test case asserting: WHEN `eventType` is `'Complaint'` THEN `suppressions.upsert` is called with reason `'complaint'` for every email in `complaint.complainedRecipients`, regardless of whether `applyDeliveryEventByProviderMessageId` resolves `'applied'` or `'already_terminal'`. |
| T022 | Extend SesEventSchema with bounce/complaint fields | implement | `apps/services/src/modules/webhooks/ses/dtos/sesEventSchema.ts` | `SesEventSchema` | R002 | Add optional `bounce: { bounceType?: string; bouncedRecipients?: { emailAddress: string }[] }` and `complaint: { complainedRecipients?: { emailAddress: string }[] }`, both `.passthrough()`, alongside the existing `eventType`/`mail` fields. |
| T023 | Implement dispatchSesEvent suppression upserts | implement | `apps/services/src/modules/webhooks/ses/sesEventHandlers.ts` | `dispatchSesEvent` | R002, EC001, EC003 | Accept a second `IEmailDeliveriesRepository`-sibling parameter, `suppressions: IEmailSuppressionsRepository`; after (and independent of) the existing delivery-state mapping, upsert `bounce.bouncedRecipients[].emailAddress` with reason `'bounce'` when `bounce.bounceType === 'Permanent'`, and upsert `complaint.complainedRecipients[].emailAddress` with reason `'complaint'` unconditionally on `Complaint` events. |
| T024 | Refactor: sesEventHandlers | refactor | `apps/services/src/modules/webhooks/ses/sesEventHandlers.ts` | `dispatchSesEvent` | R002 | Extract an `extractSuppressionTargets(event)` helper returning `{ email, reason }[]` so `dispatchSesEvent` stays orchestration-only; no behavior change. |
| T025 | Test: SES webhook route wires EmailSuppressionsDBRepository into dispatchSesEvent | test | `apps/services/tests/unit/modules/webhooks/ses/routes.test.ts` | `sesEventsWebhookRoutes` | R002 | Add a test case asserting: WHEN a `Notification` is processed THEN `dispatchSesEvent` is called with an `EmailSuppressionsDBRepository` instance as an additional argument, alongside the existing `IEmailDeliveriesRepository` argument. |
| T026 | Wire EmailSuppressionsDBRepository into sesEventsWebhookRoutes | implement | `apps/services/src/modules/webhooks/ses/routes.ts` | `sesEventsWebhookRoutes` | R002 | Construct `new EmailSuppressionsDBRepository(db)` alongside the existing `EmailDeliveriesDBRepository` and pass it to `dispatchSesEvent` in `processNotification`. |
| T027 | Test: concurrent sends to a newly-suppressed address all resolve to suppressed | test | `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` | `DeliverEmailUseCase.execute` | EC004 | Add a test case asserting: WHEN two messages with distinct `sendId`s but the same `to` address are executed concurrently against a `FakeEmailSuppressionsRepository` where that address is suppressed THEN both calls resolve with `deliveries.markSuppressed` called for their own respective `sendId`, and `sender.send()` is never called for either. |
