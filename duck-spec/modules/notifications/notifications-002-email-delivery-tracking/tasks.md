# NOTIFICATIONS-002 — Tasks

| ID | Title | Type | File | Symbol | Covers | Description |
|---|---|---|---|---|---|---|
| T001 | Add email_deliveries migration | implement | `apps/services/supabase/migrations/20260723000000_email_deliveries.sql` | `email_deliveries` | R001, R002, R003, NF003 | Create the `email_deliveries` table (`id UUID PRIMARY KEY` with no default, `template_id`, `recipient_email`, `user_id`, `state` with a `CHECK` over `queued/sent/delivered/bounced/complained/failed` defaulting to `queued`, `provider_message_id UNIQUE`, `created_at`, `updated_at`), the `email_deliveries_set_updated_at` trigger reusing `set_updated_at()`, and a partial unique index on `provider_message_id`. |
| T002 | Add notifications SES/SNS config | implement | `apps/services/src/shared/configs/notificationsConfig.ts` | `notificationsConfig` | R002, R003, R004 | Add `sesConfigurationSetName` (reads `NOTIFICATIONS_SES_CONFIGURATION_SET_NAME`) and `sesEventsTopicArn` (reads `NOTIFICATIONS_SES_EVENTS_TOPIC_ARN`), both defaulting to `''`. |
| T003 | Document new notifications env vars | implement | `apps/services/.env.example` | — | R002, R003, R004 | Add commented entries for `NOTIFICATIONS_SES_CONFIGURATION_SET_NAME` and `NOTIFICATIONS_SES_EVENTS_TOPIC_ARN`. |
| T004 | Add sns-validator dependency | implement | `apps/services/package.json` | — | R004 | Add `sns-validator` to `dependencies` and `@types/sns-validator` to `devDependencies`. |
| T005 | Implement email deliveries repository interface | implement | `apps/services/src/shared/repositories/interfaces/iEmailDeliveriesRepository.ts` | `IEmailDeliveriesRepository` | R001, R002, R003 | Export `EmailDeliveryState`, `TerminalEmailDeliveryState`, `ApplyDeliveryEventOutcome`, `EmailDeliveryRecord`, and `IEmailDeliveriesRepository` with `createQueued`, `findById`, `recordProviderMessageId`, `markSent`, `applyDeliveryEventByProviderMessageId`. |
| T006 | Implement fake email deliveries repository | implement | `apps/services/tests/mocks/fakeEmailDeliveriesRepository.ts` | `FakeEmailDeliveriesRepository` | R001, R002, R003, R005 | Implement `IEmailDeliveriesRepository` in-memory, applying the same state-guard semantics as the real repository (`markSent` no-ops outside `queued`, `applyDeliveryEventByProviderMessageId` no-ops on terminal states) so it can substitute for tests of dependent modules. |
| T007 | Test: createQueued persists a queued record | test | `apps/services/tests/unit/shared/repositories/emailDeliveriesDBRepository.test.ts` | `EmailDeliveriesDBRepository.createQueued` | R001 | Write failing acceptance test asserting: WHEN `createQueued({ id, templateId, to, userId })` is called against a mocked `Sql` THEN one `INSERT` is issued with `state = 'queued'` and the given `id`. |
| T008 | Test: recordProviderMessageId and markSent are idempotent | test | `apps/services/tests/unit/shared/repositories/emailDeliveriesDBRepository.test.ts` | `EmailDeliveriesDBRepository.recordProviderMessageId`, `EmailDeliveriesDBRepository.markSent` | R002, R005, NF002, EC003 | Add test cases asserting: `recordProviderMessageId` issues an `UPDATE` guarded by `provider_message_id IS NULL`; `markSent` issues an `UPDATE` guarded by `state = 'queued'`; both resolve without error when their guard excludes every row (0 rows affected). |
| T009 | Test: applyDeliveryEventByProviderMessageId discriminates applied/not_found/already_terminal | test | `apps/services/tests/unit/shared/repositories/emailDeliveriesDBRepository.test.ts` | `EmailDeliveriesDBRepository.applyDeliveryEventByProviderMessageId` | R003, NF001, EC001, EC002, EC004 | Add test cases asserting: a matching non-terminal row returns `'applied'` and updates `state`; no matching `provider_message_id` returns `'not_found'` without any write; a matching row already in a terminal state returns `'already_terminal'` without changing `state`. |
| T010 | Implement EmailDeliveriesDBRepository | implement | `apps/services/src/shared/repositories/emailDeliveriesDBRepository.ts` | `EmailDeliveriesDBRepository` | R001, R002, R003, NF001, EC001, EC002, EC004 | Implement all five interface methods using `postgres.js` tagged-template queries with the guards described in design.md, logging query duration and wrapping every failure as `ProviderError(502)` per the repository/adapter error-handling rule. |
| T011 | Refactor: EmailDeliveriesDBRepository | refactor | `apps/services/src/shared/repositories/emailDeliveriesDBRepository.ts` | `EmailDeliveriesDBRepository` | R001, R002, R003 | Extract shared try/catch/log/duration boilerplate into a small private helper if repetition across the five methods hurts readability; keep raw SQL only in this file. |
| T012 | Test: EmailSendMessage carries sendId | test | `apps/services/tests/unit/modules/notifications/providers/sqsEmailNotifier.test.ts` | `EmailSendMessageSchema` | R001 | Add a test case asserting: a decoded envelope missing `sendId` fails `EmailSendMessageSchema.safeParse`, and a valid envelope with `sendId` parses successfully. |
| T013 | Implement sendId on envelope and schema | implement | `apps/services/src/modules/notifications/entities/emailSendMessage.ts`, `apps/services/src/modules/notifications/dtos/emailSendMessageSchema.ts` | `EmailSendMessage`, `EmailSendMessageSchema` | R001 | Add `sendId: string` to `EmailSendMessage` and `sendId: z.string().min(1)` to `EmailSendMessageSchema`. |
| T014 | Test: send() persists a queued record before enqueueing | test | `apps/services/tests/unit/modules/notifications/providers/sqsEmailNotifier.test.ts` | `SqsEmailNotifier.send` | R001, NF003 | Add a test case asserting: WHEN `send()` is called with a mocked `IEmailDeliveriesRepository` and `SQSClient` THEN `createQueued` is awaited and resolves before `SendMessageCommand` is issued, and the enqueued envelope's `sendId` matches the id passed to `createQueued`. |
| T015 | Test: send() does not enqueue when persistence fails | test | `apps/services/tests/unit/modules/notifications/providers/sqsEmailNotifier.test.ts` | `SqsEmailNotifier.send` | R001 | Add a test case asserting: WHEN `createQueued` rejects THEN `send()` rejects and the mocked `SQSClient.send` is never called. |
| T016 | Implement SqsEmailNotifier persistence-before-enqueue | implement | `apps/services/src/modules/notifications/providers/sqsEmailNotifier.ts` | `SqsEmailNotifier.send` | R001, NF003 | Inject `IEmailDeliveriesRepository` via the constructor; generate `sendId` with `randomUUID()`; `await repository.createQueued(...)` before building/enqueueing the envelope; include `sendId` in the envelope. |
| T017 | Wire EmailDeliveriesDBRepository into resolveEmailNotifier | implement | `apps/services/src/modules/notifications/providers/resolveEmailNotifier.ts` | `resolveEmailNotifier` | R001 | Construct `new EmailDeliveriesDBRepository(db)` and pass it as the second constructor argument to `SqsEmailNotifier`. |
| T018 | Refactor: SqsEmailNotifier | refactor | `apps/services/src/modules/notifications/providers/sqsEmailNotifier.ts` | `SqsEmailNotifier` | R001 | Clean up constructor/method organization now that a second dependency is injected; no behavior change. |
| T019 | Test: SesEmailSender returns the provider message id | test | `apps/services/tests/unit/modules/notifications/providers/sesEmailSender.test.ts` | `SesEmailSender.send` | R002 | Write failing acceptance test asserting: WHEN a mocked `SESClient` resolves `SendEmailCommand` with `{ MessageId: 'abc' }` THEN `send()` resolves `{ providerMessageId: 'abc' }`, and the command input includes `ConfigurationSetName: notificationsConfig.sesConfigurationSetName`. |
| T020 | Implement SesEmailSender providerMessageId return | implement | `apps/services/src/modules/notifications/providers/sesEmailSender.ts` | `SesEmailSender.send` | R002 | Add `ConfigurationSetName` to the `SendEmailCommand` input; return `{ providerMessageId: response.MessageId! }` on success; error classification unchanged. |
| T021 | Update IEmailSender port and fake | implement | `apps/services/src/modules/notifications/providers/interfaces/iEmailSender.ts`, `apps/services/tests/mocks/fakeEmailSender.ts` | `IEmailSender`, `FakeEmailSender.send` | R002 | Change `IEmailSender.send()`'s return type to `Promise<{ providerMessageId: string }>`; update `FakeEmailSender.send()` to record calls and resolve `{ providerMessageId: 'fake-provider-message-id' }` (configurable) instead of `void`. |
| T022 | Test: DeliverEmailUseCase persists provider id then marks sent | test | `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` | `DeliverEmailUseCase.execute` | R002 | Add a test case asserting: WHEN delivery succeeds via `FakeEmailSender` THEN `FakeEmailDeliveriesRepository.recordProviderMessageId` is called with the returned `providerMessageId` before `markSent` is called for the same `sendId`. |
| T023 | Test: DeliverEmailUseCase skips a second dispatch when already dispatched | test | `apps/services/tests/unit/modules/notifications/useCases/deliverEmailUseCase.test.ts` | `DeliverEmailUseCase.execute` | R005, NF002, EC003 | Add a test case asserting: WHEN `findById(sendId)` resolves a record with a non-null `providerMessageId` THEN `execute()` never calls `sender.send()` and calls `markSent(sendId)` directly. |
| T024 | Implement DeliverEmailUseCase idempotent dispatch | implement | `apps/services/src/modules/notifications/useCases/deliverEmailUseCase.ts` | `DeliverEmailUseCase.execute` | R002, R005, NF002, EC003 | Inject `IEmailDeliveriesRepository`; look up `findById(message.sendId)` first; if `providerMessageId` is already set, call `markSent` and return without calling `sender.send()`; otherwise send, then `recordProviderMessageId` followed by `markSent`. |
| T025 | Refactor: DeliverEmailUseCase | refactor | `apps/services/src/modules/notifications/useCases/deliverEmailUseCase.ts` | `DeliverEmailUseCase` | R002, R005, NF002 | Tidy the branching between the idempotent-skip path and the first-dispatch path for readability; no behavior change. |
| T026 | Wire EmailDeliveriesDBRepository into emailWorker | implement | `apps/services/src/modules/notifications/worker/emailWorker.ts` | `processMessage` | R002 | Construct `new EmailDeliveriesDBRepository(db)` alongside `SesEmailSender` and pass it as `DeliverEmailUseCase`'s second constructor argument. |
| T027 | Implement SNS notification envelope schema | implement | `apps/services/src/modules/webhooks/ses/dtos/snsNotificationSchema.ts` | `SnsNotificationSchema` | R004 | Export a Zod schema validating `Type` (`Notification`/`SubscriptionConfirmation`/`UnsubscribeConfirmation`), `MessageId`, `TopicArn`, `Message`, `Signature`, `SignatureVersion`, `SigningCertURL`, optional `SubscribeURL`/`Subject`/`UnsubscribeURL`. |
| T028 | Implement SES event schema | implement | `apps/services/src/modules/webhooks/ses/dtos/sesEventSchema.ts` | `SesEventSchema` | R003 | Export a Zod schema validating `eventType: string` and `mail: { messageId: string }`, `.passthrough()` for all other fields. |
| T029 | Test: validateSnsMessage resolves on valid signature and rejects on invalid | test | `apps/services/tests/unit/modules/webhooks/ses/snsSignatureValidator.test.ts` | `validateSnsMessage` | R004 | Write failing acceptance test asserting: WHEN the underlying `sns-validator` callback invokes with `(null, message)` THEN the returned promise resolves with `message`; WHEN it invokes with `(err)` THEN the promise rejects with `err`. |
| T030 | Implement snsSignatureValidator | implement | `apps/services/src/modules/webhooks/ses/snsSignatureValidator.ts` | `validateSnsMessage` | R004 | Wrap a `sns-validator` `MessageValidator` instance's `validate(message, callback)` in a `Promise`, rejecting with the callback's error and resolving with the validated message. |
| T031 | Test: dispatchSesEvent maps known event types and ignores unknown ones | test | `apps/services/tests/unit/modules/webhooks/ses/sesEventHandlers.test.ts` | `dispatchSesEvent` | R003 | Write failing acceptance test asserting: `Delivery`/`Bounce`/`Complaint`/`Reject` events call `repository.applyDeliveryEventByProviderMessageId` with `delivered`/`bounced`/`complained`/`failed` respectively and `mail.messageId`; any other `eventType` never calls the repository. |
| T032 | Test: dispatchSesEvent logs discarded outcomes without throwing | test | `apps/services/tests/unit/modules/webhooks/ses/sesEventHandlers.test.ts` | `dispatchSesEvent` | NF001, EC002, EC004 | Add test cases asserting: WHEN the repository call resolves `'not_found'` or `'already_terminal'` THEN `dispatchSesEvent` resolves without throwing (both outcomes are logged, not surfaced as errors). |
| T033 | Implement sesEventHandlers | implement | `apps/services/src/modules/webhooks/ses/sesEventHandlers.ts` | `dispatchSesEvent` | R003, NF001, EC002, EC004 | Map `eventType` to a terminal state via a lookup table, call `repository.applyDeliveryEventByProviderMessageId(mail.messageId, state)` for known types, log and return for unknown types, and log each outcome (`applied`/`not_found`/`already_terminal`) at the appropriate level. |
| T034 | Test: SES webhook route rejects an unauthenticated notification | test | `apps/services/tests/unit/modules/webhooks/ses/routes.test.ts` | `sesEventsWebhookRoutes` | R004 | Write failing acceptance test asserting: WHEN `validateSnsMessage` rejects (invalid signature) THEN the route replies with an error status and `repository.applyDeliveryEventByProviderMessageId` is never called. |
| T035 | Test: SES webhook route rejects a notification for an unexpected TopicArn | test | `apps/services/tests/unit/modules/webhooks/ses/routes.test.ts` | `sesEventsWebhookRoutes` | R004 | Add a test case asserting: WHEN the validated message's `TopicArn` does not equal `notificationsConfig.sesEventsTopicArn` THEN the route replies with an error status and does not dispatch. |
| T036 | Test: SES webhook route confirms a SubscriptionConfirmation | test | `apps/services/tests/unit/modules/webhooks/ses/routes.test.ts` | `sesEventsWebhookRoutes` | R003 | Add a test case asserting: WHEN `Type === 'SubscriptionConfirmation'` and the message is validated THEN the route fetches `SubscribeURL` and replies `200` without calling `dispatchSesEvent`. |
| T037 | Test: SES webhook route dispatches a valid Notification and always replies 200 | test | `apps/services/tests/unit/modules/webhooks/ses/routes.test.ts` | `sesEventsWebhookRoutes` | R003, NF001, EC001, EC002, EC004 | Add a test case asserting: WHEN `Type === 'Notification'` and validation succeeds THEN `dispatchSesEvent` is called with the parsed inner SES event and the route replies `200 { received: true }` regardless of whether the dispatch outcome is `applied`, `not_found`, or `already_terminal`. |
| T038 | Implement sesEventsWebhookRoutes | implement | `apps/services/src/modules/webhooks/ses/routes.ts` | `sesEventsWebhookRoutes` | R003, R004, EC001, EC002, EC004 | Register the scoped `text/plain` raw-body parser; fail fast if `sesEventsTopicArn` is unset; parse and `SnsNotificationSchema`-validate the envelope; call `validateSnsMessage` and throw `UnauthorizedError` on failure or `TopicArn` mismatch; handle `SubscriptionConfirmation`/`UnsubscribeConfirmation`; for `Notification`, parse+validate the inner `Message` with `SesEventSchema` and call `dispatchSesEvent`; always reply `200` except on the authentication failures. |
| T039 | Refactor: SES webhook route and handlers | refactor | `apps/services/src/modules/webhooks/ses/routes.ts`, `apps/services/src/modules/webhooks/ses/sesEventHandlers.ts` | `sesEventsWebhookRoutes`, `dispatchSesEvent` | R003, R004 | Tidy branching/logging across the route and dispatcher for readability; no behavior change. |
| T040 | Register SES webhook plugin | implement | `apps/services/src/app.ts` | `createApp` | R003, R004 | Import and `await fastify.register(sesEventsWebhookRoutes)` before `clerkAuthPlugin`, alongside the existing `mobbexWebhookRoutes`/`clerkWebhookRoutes` registrations. |
