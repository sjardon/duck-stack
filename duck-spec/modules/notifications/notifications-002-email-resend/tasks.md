# NOTIFICATIONS-002 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Add `resend` dependency, remove `@aws-sdk/client-sesv2` | implement | R006 | In `apps/services/package.json`, remove `@aws-sdk/client-sesv2` from `dependencies` and add `resend`. |
| T002 | Test: `emailConfig` reads Resend key and sender from env | test | R004, EC004 | In `apps/services/tests/unit/shared/configs/emailConfig.test.ts`, assert: WHEN `RESEND_API_KEY`/`EMAIL_SENDER_ADDRESS` are absent THEN `emailConfig.resendApiKey`/`senderEmail` default to `''`; WHEN they are set THEN `emailConfig` reflects each value (mirrors `mobbexConfig.test.ts`'s reset-modules/env-override pattern). |
| T003 | Implement typed `emailConfig` for Resend | implement | R004, EC004 | In `apps/services/src/shared/configs/emailConfig.ts`, replace the `sesRegion` field with `resendApiKey` (from `RESEND_API_KEY`), keep `senderEmail` (from `EMAIL_SENDER_ADDRESS`, default `''`). |
| T004 | Test: `ResendEmailNotifier.send` returns control without waiting for delivery | test | R001, NF001 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert: WHEN `send()` is called with a valid template/context THEN it returns `undefined` synchronously and the mocked `Resend.emails.send` has not yet resolved at that point (deferred/never-resolved provider mock). |
| T005 | Implement non-blocking `ResendEmailNotifier.send` | implement | R001, NF001 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`, implement `ResendEmailNotifier.send()`: call the private `dispatch()` without `await`, attach a `.catch()` fire-and-forget guard, and return `void` immediately (same shape as the retired `SesEmailNotifier.send`). |
| T006 | Test: `ResendEmailNotifier` delivers via Resend with both HTML and text | test | R003, EC005 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert: WHEN `send()` is called with a valid template/context/recipient THEN the mocked `Resend.emails.send` is called once with `{ from: configured sender, to: [to], subject, html, text }` matching the rendered output — both `html` and `text` populated in the same call. |
| T007 | Implement Resend delivery in `ResendEmailNotifier.dispatch` | implement | R003, EC005 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`, implement the delivery branch of `dispatch()`: build the Resend send payload (`from`, `to: [to]`, `subject`, `html`, `text`) from the rendered `{subject, html, text}` and configured sender, and call `this.client.emails.send(...)`. |
| T008 | Test: unknown template id is logged and does not throw | test | R004, EC001, NF003 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert: WHEN `send()` is called with an unregistered `templateId` THEN `logger.warn` is called with a payload containing only the template id (no `to`/`context`), `Resend.emails.send` is never called, and `send()` does not throw. |
| T009 | Implement unknown-template guard in `dispatch` | implement | R004, EC001, NF003 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`, in `dispatch()`, after `renderEmailTemplate()` returns `undefined`, `logger.warn({ templateId }, ...)` and return (silent-fail, same as the retired SES adapter), logging only `templateId`. |
| T010 | Test: malformed/missing recipient is rejected before calling Resend | test | R003 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert: WHEN `send()` is called with an empty or malformed `to` THEN `logger.warn` is called, `Resend.emails.send` is never called, and `send()` does not throw — proving delivery (R003) only ever happens for a valid recipient, ported unchanged from the retired SES adapter. |
| T011 | Port recipient-validation guard into `dispatch` | implement | R003 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`, in `dispatch()`, validate `to` against the existing email regex before rendering/sending; on failure, `logger.warn` and return (silent-fail), unchanged from the retired `SesEmailNotifier`. |
| T012 | Test: Resend-returned error object is logged and isolated from the caller | test | R005, EC001, EC003, NF002, NF003 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert: WHEN the mocked `Resend.emails.send` resolves with `{ data: null, error: { name: 'rate_limit_exceeded', message: '...' } }` THEN `logger.error` is called with `{ templateId, err }` (`err` matching the returned error object, no PII), and the promise returned internally never rejects observably from `send()`'s perspective (no unhandled rejection, no throw). Repeat with `{ name: 'validation_error' }` to cover EC001 (unverified sender domain). |
| T013 | Test: Resend network-level rejection (e.g. invalid API key) is logged and isolated from the caller | test | R005, EC002, NF002, NF003 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert: WHEN the mocked `Resend.emails.send` rejects THEN `logger.error` is called with `{ templateId, err }` and no unhandled rejection occurs — one log entry per attempted send, no retry, no surfacing to the caller. |
| T014 | Implement delivery failure isolation with error-shape bridging | implement | R005, EC001, EC002, EC003, NF002, NF003 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`, wrap `this.client.emails.send(...)` in `dispatch()` in `try/catch`: after the call, `if (error) throw error;` to bridge Resend's returned-error shape into the catch; in `catch`, `logger.error({ templateId, err }, ...)` then re-throw `ProviderError('Failed to send email via Resend', 502, err)`, relying on `send()`'s fire-and-forget `.catch()` to swallow it. |
| T015 | Refactor `ResendEmailNotifier` error handling and logging fields | refactor | R005, NF003 | In `apps/services/src/modules/notifications/providers/resendEmailNotifier.ts`, confirm the adapter-style `try/catch` (in `dispatch()`) and the fire-and-forget wrapper (in `send()`) each have a single, clearly commented responsibility; ensure every `logger.warn`/`logger.error` call site uses a consistent `{ templateId, err? }` field shape with no other identifiers. |
| T016 | Delete retired SES adapter and its test | implement | R006, EC006 | Delete `apps/services/src/modules/notifications/providers/sesEmailNotifier.ts` and `apps/services/tests/unit/modules/notifications/providers/sesEmailNotifier.test.ts`. |
| T017 | Test: `resolveNotifier` builds and caches a `ResendEmailNotifier` singleton | test | R003, R004, R006, EC006 | In `apps/services/tests/unit/modules/notifications/providers/resolveNotifier.test.ts`, update env var setup to `RESEND_API_KEY`/`EMAIL_SENDER_ADDRESS` and assert: WHEN `resolveNotifier()` is called twice THEN both calls return the same `ResendEmailNotifier` instance, constructed from `emailConfig`'s `resendApiKey`/`senderEmail`; add a case asserting `resolveNotifier()` throws a descriptive `Error` when `RESEND_API_KEY` is missing, alongside the existing missing-`EMAIL_SENDER_ADDRESS` case. |
| T018 | Implement `resolveNotifier` factory for Resend | implement | R003, R004, R006, EC006 | In `apps/services/src/modules/notifications/providers/resolveNotifier.ts`, update `createResendEmailNotifier()` (renamed from `createSesEmailNotifier`) to read `resendApiKey`/`senderEmail` from `emailConfig`, throw fail-fast `Error`s if either is empty, and construct/cache one `ResendEmailNotifier` from them. |
| T019 | Refactor notifications providers naming/comments | refactor | R003, R006 | In `apps/services/src/modules/notifications/providers/`, align comments and singleton-cache naming in `resolveNotifier.ts` with the new `ResendEmailNotifier` (e.g. update the "mirrors `resolveProvider.ts`" comment), and remove any leftover reference to SES region/terminology. |
| T020 | Test: `ResendEmailNotifier` satisfies the unchanged `EmailNotifier` port | test | R002 | In `apps/services/tests/unit/modules/notifications/providers/resendEmailNotifier.test.ts`, assert (type-level, matching consumer call sites unchanged): `const notifier: EmailNotifier = new ResendEmailNotifier({ apiKey, senderEmail })` compiles, and `notifier.send({ templateId: 'welcome', to, context: { recipientName } })` — the exact shape `dispatchClerkEvent` already calls — compiles and executes without any change to `@repo/types`'s `EmailNotifier`/`SendEmailInput` or to the Clerk webhook call sites. |
