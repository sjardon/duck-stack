# LANDING-003 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: init when analytics config present | test | R009, NF001 | In `apps/landing/tests/analytics/analytics.test.ts` assert: WHEN `VITE_POSTHOG_KEY` is set THEN `initAnalytics()` calls the mocked `posthog.init` with that key, `api_host` from `VITE_POSTHOG_HOST`, and `capture_pageview: false`, `autocapture: false`, `capture_heatmaps: false`, `disable_session_recording: true`. |
| T002 | Implement `readAnalyticsConfig`/`initAnalytics`/`captureEvent`/`getDistinctId` | implement | R009, NF001 | Create `apps/landing/src/lib/analytics.ts` with `readAnalyticsConfig()` reading `VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST`, `initAnalytics()` calling `posthog.init(...)` wrapped in `try/catch`, `captureEvent(name, properties)` wrapping `posthog.capture`, and `getDistinctId()` wrapping `posthog.get_distinct_id()` in `try/catch`; add `posthog-js` to `apps/landing/package.json` dependencies. |
| T003 | Test: no-op and no throw when analytics config absent | test | R010 | In `apps/landing/tests/analytics/analytics.test.ts` assert: WHEN `VITE_POSTHOG_KEY` is absent THEN `initAnalytics()` returns without calling `posthog.init` and without throwing, and `getDistinctId()` returns `null` without throwing. |
| T004 | Implement early-return guards for absent config | implement | R010 | In `apps/landing/src/lib/analytics.ts`, return immediately from `initAnalytics()` when `readAnalyticsConfig()` is `null`, and make `getDistinctId()` return `null` on any thrown/undefined result from `posthog.get_distinct_id()`. |
| T005 | Refactor `analytics.ts` for clarity | refactor | R009, R010, NF001 | In `apps/landing/src/lib/analytics.ts`, keep each export a short, single-purpose function with no behavior change; ensure no network I/O happens outside `posthog.init`/`posthog.capture`. |
| T006 | Test: visit event recorded with route on mount and on navigation | test | R001 | In `apps/landing/tests/analytics/RouteVisitTracker.test.tsx` assert: WHEN `RouteVisitTracker` mounts at a given path THEN the mocked `captureEvent` is called with `'landing_page_viewed'` and `{ route: <path> }`, and WHEN the route changes THEN it is called again with the new route. |
| T007 | Implement `RouteVisitTracker` and mount it | implement | R001 | Create `apps/landing/src/components/analytics/RouteVisitTracker.tsx` using `useLocation()` and a `useEffect` keyed on `location.pathname` that calls `registerTrafficOrigin(...)` then `captureEvent('landing_page_viewed', { route: location.pathname })`, rendering `null`; mount `<RouteVisitTracker />` inside `<BrowserRouter>` in `apps/landing/src/App.tsx`. |
| T008 | Test: traffic origin parsed and registered as first-/last-touch | test | R002, EC003 | In `apps/landing/tests/analytics/attribution.test.ts` assert: `parseTrafficOrigin(search, referrer)` extracts `utm_source`/`utm_medium`/`utm_campaign`/`utm_term`/`utm_content`/`referrer`; `registerTrafficOrigin(origin)` calls the mocked `posthog.register_once` with `first_touch_*` fields and `posthog.register` with `last_touch_*` fields; and a second call with an empty origin still calls `register_once` (so an already-set first-touch value is never overwritten, per `register_once` semantics) while `register` reflects the new (possibly empty) last-touch values. |
| T009 | Implement `parseTrafficOrigin`/`registerTrafficOrigin` | implement | R002, EC003 | Create `apps/landing/src/lib/attribution.ts` with `parseTrafficOrigin(search, referrer)` and `registerTrafficOrigin(origin)` as described in T008; wire the call into `RouteVisitTracker` before `captureEvent`. |
| T010 | Test: distinct id is stable and null-safe | test | R004 | In `apps/landing/tests/analytics/analytics.test.ts` assert: `getDistinctId()` returns the same value as the mocked `posthog.get_distinct_id()` across repeated calls within a session, and returns `null` when analytics was never initialized. |
| T011 | Implement stable `getDistinctId` delegation | implement | R004 | Confirm/adjust `apps/landing/src/lib/analytics.ts`'s `getDistinctId()` to delegate directly to `posthog.get_distinct_id()` with no caching or derivation of its own, so continuity is guaranteed by PostHog's own persisted id rather than landing-owned state. |
| T012 | Refactor `attribution.ts` and `RouteVisitTracker.tsx` | refactor | R001, R002, R004, EC003 | Review both files for clarity and single responsibility (parsing vs. registration vs. React lifecycle); no behavior change. |
| T013 | Test: CTA navigates to the registration entry point | test | R007 | In `apps/landing/tests/analytics/CTA.test.tsx` assert: WHEN "Get early access" is clicked THEN `window.location.href` is set to `${VITE_WEB_URL}/sign-up` (with no `landing_id` param when analytics is unconfigured). |
| T014 | Implement CTA hand-off destination | implement | R007 | In `apps/landing/src/components/sections/CTA.tsx`, add `handleClick` navigating to `buildHandoffUrl('/sign-up')` and wire it to `<Button variant="primary" onClick={handleClick}>`; create `apps/landing/src/lib/handoff.ts` with a minimal `buildHandoffUrl(path)` that resolves `path` against `VITE_WEB_URL`. |
| T015 | Test: hand-off URL carries the distinct id, falls back cleanly when absent | test | R005, EC001, R010 | In `apps/landing/tests/analytics/handoff.test.ts` assert: WHEN `getDistinctId()` returns a value THEN `buildHandoffUrl(path)` appends `landing_id=<value>` to the resolved URL's query string (merged with any existing query params on `path`); WHEN it returns `null` THEN the URL is returned unchanged from today's hand-off shape, with no error thrown. |
| T016 | Implement `landing_id` propagation in `buildHandoffUrl` | implement | R005, EC001, R010 | In `apps/landing/src/lib/handoff.ts`, extend `buildHandoffUrl(path)` to call `getDistinctId()` and append `landing_id` as a query parameter when non-null, leaving the URL unchanged when `null`; update `apps/landing/src/components/sections/Pricing.tsx`'s `handleCTA` to navigate via `buildHandoffUrl(`/billing/subscribe?plan=${code}`)` instead of building the URL inline. |
| T017 | Test: conversion event recorded synchronously before navigation, no PII in payload | test | R003, NF001, NF002 | In `apps/landing/tests/analytics/CTA.test.tsx` and `apps/landing/tests/Pricing.test.tsx` assert: clicking the hand-off action calls the mocked `captureEvent('registration_started', {...})` before `window.location.href` is set, and the properties object contains only `action`/`plan`-shaped fields (no email, name, or other form-derived values). |
| T018 | Implement `registration_started` capture on hand-off | implement | R003, NF001, NF002 | In `apps/landing/src/components/sections/Pricing.tsx` and `apps/landing/src/components/sections/CTA.tsx`, call `captureEvent('registration_started', { action, plan? })` immediately before the `buildHandoffUrl` navigation. |
| T019 | Test: repeat hand-off from the same browser does not record a second conversion | test | R008, EC004 | In `apps/landing/tests/analytics/conversion.test.ts` and `apps/landing/tests/Pricing.test.tsx` assert: `hasConverted()`/`markConverted()` persist a flag across calls (via a mocked `localStorage`); and WHEN a hand-off action is clicked twice THEN `captureEvent('registration_started', ...)` is called only once, while `window.location.href` navigation still occurs on both clicks. |
| T020 | Implement conversion idempotency guard | implement | R008, EC004 | Create `apps/landing/src/lib/conversion.ts` with `hasConverted()`/`markConverted()` backed by `localStorage`; gate the `captureEvent('registration_started', ...)` call sites in `Pricing.tsx`/`CTA.tsx` behind `if (!hasConverted())`, calling `markConverted()` immediately after. |
| T021 | Refactor hand-off components and modules | refactor | R003, R005, R007, R008, EC001, EC004, NF001, NF002 | Review `Pricing.tsx`, `CTA.tsx`, `handoff.ts`, `conversion.ts` for clarity and consistent ordering (capture → mark converted → navigate); confirm `components/ui/Button.tsx` still receives no destination or analytics logic; no behavior change. |
| T022 | Test: `main.tsx` initializes analytics before render | test | R009, R010 | In `apps/landing/tests/analytics/main.test.tsx` assert: importing `main.tsx` calls the mocked `initAnalytics()` before `createRoot(...).render(...)`, alongside the existing `initErrorTracking()` call. |
| T023 | Implement `main.tsx` analytics wiring | implement | R009, R010 | Modify `apps/landing/src/main.tsx` to call `initAnalytics()` before `createRoot(...).render(...)`. |
| T024 | Refactor `main.tsx` bootstrap ordering | refactor | R009, R010 | Review `apps/landing/src/main.tsx` for readability of the init-call ordering; no behavior change. |
| T025 | Test: `apps/web` adopts a propagated landing identity | test | R006, NF003, EC001 | In `apps/web/tests/analytics/analytics.test.ts` assert: WHEN `window.location.search` contains `landing_id=<id>` THEN `adoptPropagatedIdentity()` calls the mocked `posthog.identify` with `<id>`; WHEN the param is absent THEN it returns without calling `posthog.identify` and without throwing. |
| T026 | Implement `adoptPropagatedIdentity` | implement | R006, NF003, EC001 | In `apps/web/src/lib/analytics.ts`, add `adoptPropagatedIdentity()` reading `landing_id` from `window.location.search` and calling `posthog.identify(landingId)` wrapped in `try/catch` when present; no-op when absent. |
| T027 | Test: `main.tsx` (web) adopts identity before render, after `initAnalytics` | test | R006, NF003 | In `apps/web/tests/analytics/main.test.tsx` assert: importing `main.tsx` calls the mocked `adoptPropagatedIdentity()` after `initAnalytics()` and before `createRoot(...).render(...)`. |
| T028 | Implement `main.tsx` (web) identity-adoption wiring | implement | R006, NF003 | Modify `apps/web/src/main.tsx` to call `adoptPropagatedIdentity()` immediately after `initAnalytics()`, before `createRoot(...).render(...)`. |
| T029 | Refactor `apps/web` analytics identity adoption | refactor | R006, NF003, EC001, EC005 | Review `adoptPropagatedIdentity()` in `apps/web/src/lib/analytics.ts` and its call site in `main.tsx` for clarity and error-handling consistency with `initAnalytics()`; confirm no coupling is introduced between `adoptPropagatedIdentity()` and the existing `useSyncAnalyticsUser()` identify(user.id) call — both remain independent, satisfying EC005; no behavior change. |
| T030 | Document new public config variables for `landing` | implement | R009, R010 | Modify `.cloudflare/.env.deploy.landing.example` to document `VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST` as optional public values for `landing`, mirroring the existing `VITE_ERROR_TRACKING_DSN` entry pattern from LANDING-002. |
| T031 | Document hand-off identity propagation behavior | implement | R005, R006, EC001 | Modify `.cloudflare/README.md`'s analytics/error-tracking documentation to describe the `landing_id` query-parameter hand-off from `landing` to `web` and its silent fallback when absent. |

## Coverage check

- R001: T006, T007, T012
- R002: T008, T009, T012
- R003: T017, T018, T021
- R004: T010, T011, T012
- R005: T015, T016, T021, T031
- R006: T025, T026, T027, T028, T029, T031
- R007: T013, T014, T021
- R008: T019, T020, T021
- R009: T001, T002, T005, T022, T023, T024, T030
- R010: T003, T004, T005, T015, T016, T022, T023, T024, T030
