# LANDING-002 — Tasks

| ID | Title | Type | Covers | Description |
|---|---|---|---|---|
| T001 | Test: config gate + init when DSN present | test | R001, R002, R006 | In `apps/landing/tests/error-tracking/errorTracking.test.ts` assert: WHEN `VITE_ERROR_TRACKING_DSN` is set THEN `initErrorTracking()` calls the mocked `Sentry.init` with that `dsn` and no `integrations`/`defaultIntegrations` override that would disable the SDK's default global error/unhandled-rejection handlers. |
| T002 | Implement `readErrorTrackingConfig` and `initErrorTracking` init branch | implement | R001, R002, R006 | In `apps/landing/src/lib/errorTracking.ts`, add `readErrorTrackingConfig()` reading `import.meta.env.VITE_ERROR_TRACKING_DSN` and `initErrorTracking()` calling `Sentry.init({ dsn, ... })` when config is present. |
| T003 | Test: no-op when DSN absent | test | R007 | In `apps/landing/tests/error-tracking/errorTracking.test.ts` assert: WHEN `VITE_ERROR_TRACKING_DSN` is absent THEN `initErrorTracking()` returns without calling `Sentry.init` and without throwing. |
| T004 | Implement early-return guard for absent config | implement | R007 | In `apps/landing/src/lib/errorTracking.ts`'s `initErrorTracking()`, return immediately when `readErrorTrackingConfig()` is `null`, before any `Sentry.init` call. |
| T005 | Test: environment and release stamped on init | test | R005 | In `apps/landing/tests/error-tracking/errorTracking.test.ts` assert: `initErrorTracking()` calls `Sentry.init` with `environment` read from `VITE_ENVIRONMENT` (defaulting to `"production"` when unset) and `release` read from `VITE_RELEASE` (defaulting to `"unknown"` when unset). |
| T006 | Implement environment/release fields in config | implement | R005 | In `apps/landing/src/lib/errorTracking.ts`'s `readErrorTrackingConfig()`, add `environment`/`release` fields sourced from `import.meta.env.VITE_ENVIRONMENT`/`import.meta.env.VITE_RELEASE` with the documented defaults, passed through to `Sentry.init`. |
| T007 | Test: init failure isolation | test | NF004 | In `apps/landing/tests/error-tracking/errorTracking.test.ts` assert: WHEN the mocked `Sentry.init` throws THEN `initErrorTracking()` does not throw and logs the error instead. |
| T008 | Implement try/catch around `Sentry.init` | implement | NF004 | In `apps/landing/src/lib/errorTracking.ts`'s `initErrorTracking()`, wrap the `Sentry.init` call in `try/catch`, logging via `console.error` on failure and never re-throwing. |
| T009 | Test: report payload never carries a visitor identity | test | NF003 | In `apps/landing/tests/error-tracking/errorTracking.test.ts` assert: WHEN an event carries a `user` object (however it got attached) THEN `scrubEvent(event)` returns an event with no `user` field; and that `Sentry.init` is called with `sendDefaultPii: false` and `beforeSend: scrubEvent`. |
| T010 | Implement `scrubEvent` and wire into `beforeSend` | implement | NF003 | In `apps/landing/src/lib/errorTracking.ts`, add `scrubEvent(event)` that deletes `event.user` when present, pass it as `beforeSend` in `Sentry.init`, and pass `sendDefaultPii: false`. |
| T011 | Refactor `errorTracking.ts` for clarity | refactor | R001, R002, R005, R006, R007, NF002, NF003, NF004 | In `apps/landing/src/lib/errorTracking.ts`, keep `initErrorTracking()` a short orchestration function over `readErrorTrackingConfig()` and `scrubEvent()`, with no network I/O of its own (NF002) and no behavior change. |
| T012 | Test: dependency-free fallback renders | test | EC004 | In `apps/landing/tests/error-tracking/ErrorFallback.test.tsx` assert: `ErrorFallback` renders static heading/copy and a reload button with no wrapping provider/context, and that clicking the button calls `window.location.reload()`. |
| T013 | Implement `ErrorFallback` | implement | EC004 | Create `apps/landing/src/components/error/ErrorFallback.tsx` rendering only static markup and a `window.location.reload()` button, with no hooks, no `@repo/types` import, and no provider-dependent state. |
| T014 | Test: render error caught, reported, and fallback shown | test | R004 | In `apps/landing/tests/error-tracking/AppErrorBoundary.test.tsx` assert: WHEN a child component throws during render THEN `AppErrorBoundary` renders `ErrorFallback` instead of unmounting, and the mocked `Sentry.captureException`/`Sentry.ErrorBoundary` reporting path is exercised. |
| T015 | Implement `AppErrorBoundary` | implement | R004 | Create `apps/landing/src/components/error/AppErrorBoundary.tsx` wrapping `Sentry.ErrorBoundary` with `fallback={ErrorFallback}` and `showDialog={false}`. |
| T016 | Refactor error-screen components | refactor | R004, EC004 | Review `ErrorFallback.tsx`/`AppErrorBoundary.tsx` prop typing (`ReactNode` children, no unused props) and ensure `ErrorFallback` has no accidental dependency on props Sentry's `ErrorBoundary` passes to its fallback; no behavior change. |
| T017 | Test: bootstrap initializes tracking and wraps the tree | test | R001, R002, R004, R006 | In `apps/landing/tests/error-tracking/main.test.tsx` assert: importing `main.tsx` (with a `#root` element present) calls the mocked `initErrorTracking()` before `createRoot(...).render(...)`, and that the rendered tree includes `AppErrorBoundary` wrapping `App`. |
| T018 | Implement `main.tsx` wiring | implement | R001, R002, R004, R006 | Modify `apps/landing/src/main.tsx` to call `initErrorTracking()` before `createRoot(...).render(...)` and to wrap `<App />` in `<AppErrorBoundary>` inside `<StrictMode>`. |
| T019 | Refactor bootstrap wiring | refactor | R001, R002, R004, R006 | Review `main.tsx` for readability (ordering of `initErrorTracking()` relative to `createRoot`/`render`), no behavior change. |
| T020 | Configure Node environment for the Vite-config test | implement | R003 | Modify `apps/landing/vitest.config.ts` to add `environmentMatchGlobs: [['tests/error-tracking/viteConfig.test.ts', 'node']]`, mirroring `apps/web/vitest.config.ts`, so the config test can inspect `process.env` without a DOM environment. |
| T021 | Test: sourcemaps and plugin gated on `SENTRY_AUTH_TOKEN`, deploy credential never `VITE_`-prefixed | test | R003, NF001, EC001, EC005 | In `apps/landing/tests/error-tracking/viteConfig.test.ts` assert: WHEN `process.env.SENTRY_AUTH_TOKEN` is set THEN the resolved config has `build.sourcemap === true` and includes a `sentry-vite-plugin` entry configured with `sourcemaps.filesToDeleteAfterUpload` and no `errorHandler` override, whose `authToken`/`org`/`project`/`url` options are sourced from `process.env` keys that are never `VITE_`-prefixed; WHEN unset THEN `build.sourcemap === false` and no such plugin is present. |
| T022 | Implement conditional `sentryVitePlugin` wiring | implement | R003, NF001, EC001, EC005 | Modify `apps/landing/vite.config.ts` to compute `sourceMapsEnabled = Boolean(process.env.SENTRY_AUTH_TOKEN)`, set `build.sourcemap = sourceMapsEnabled`, and conditionally include `sentryVitePlugin({ org, project, authToken, url, release: { name: process.env.VITE_RELEASE }, sourcemaps: { filesToDeleteAfterUpload: ["**/*.map"] } })` without a custom `errorHandler`, reading all deploy-only values from `process.env` (never `import.meta.env`). |
| T023 | Test: release identifier matches between build and runtime | test | R005, EC002 | In `apps/landing/tests/error-tracking/viteConfig.test.ts` assert: the `release.name` passed to `sentryVitePlugin` reads from the same `VITE_RELEASE` variable name that `apps/landing/src/lib/errorTracking.ts`'s `readErrorTrackingConfig()` reads for the runtime `release` field (single source of truth, no divergence). |
| T024 | Implement shared `VITE_RELEASE` usage | implement | R005, EC002 | Confirm/adjust `apps/landing/vite.config.ts` and `apps/landing/src/lib/errorTracking.ts` both read the literal `VITE_RELEASE` key so the build-time and runtime release identifiers cannot diverge. |
| T025 | Refactor `vite.config.ts` plugin options | refactor | R003, R005, NF001, EC001, EC002, EC005 | Extract the `sentryVitePlugin` options object construction into a small named function (e.g. `resolveSentryVitePluginOptions()`) for readability and testability; no behavior change. |
| T026 | Add Sentry dependencies | implement | R001, R002, R003, R004, R006, R007 | Modify `apps/landing/package.json` to add `@sentry/react` as a runtime dependency and `@sentry/vite-plugin` as a devDependency. |
| T027 | Document new deploy variables | implement | R006, R007, EC005 | Modify `.cloudflare/.env.deploy.landing.example` to document `VITE_ERROR_TRACKING_DSN`, `VITE_RELEASE`, `VITE_ENVIRONMENT` as optional public values, and add comments noting `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_URL` belong only in the per-environment, git-ignored values file. |
| T028 | Document source-map publish behavior in README | implement | R003, EC001, EC002, EC005 | Modify `.cloudflare/README.md`'s "Error tracking variables" section to also cover `landing` (LANDING-002) alongside `web` (WEB-002): the same opt-in variable split, that source maps are uploaded and removed from `dist` before deploy when `SENTRY_AUTH_TOKEN` is set, and that a failed upload aborts the deploy. |

## Coverage check

- R001: T001, T002, T011, T017, T018, T019, T026
- R002: T001, T002, T011, T017, T018, T019, T026
- R003: T020, T021, T022, T025, T026, T028
- R004: T014, T015, T016, T017, T018, T019, T026
- R005: T005, T006, T011, T023, T024, T025
- R006: T001, T002, T011, T017, T018, T019, T026, T027
- R007: T003, T004, T011, T026, T027
