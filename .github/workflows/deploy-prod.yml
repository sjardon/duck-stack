name: Deploy prod

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-apps.yml
    concurrency:
      group: deploy-prod
      cancel-in-progress: false
    with:
      environment: prod
      commit_sha: ${{ github.sha }}
