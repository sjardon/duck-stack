name: Deploy dev

on:
  push:
    branches: [develop]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-apps.yml
    concurrency:
      group: deploy-dev
      cancel-in-progress: false
    with:
      environment: dev
      commit_sha: ${{ github.sha }}
