# BACKEND SERVICES

Conventions, architecture, layer rules, SQL/query rules, logging rules, configuration rules, and naming conventions for everything under `apps/services/` live in `duck-spec/docs/BACKEND.md`.

Read that file before designing or implementing anything in this directory. The rules there are hard constraints.
